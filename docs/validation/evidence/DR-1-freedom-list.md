# DR-1 执行自由度清单（§1.7.3）

> 路径为切片定义（`docs/t027/REMAINING_SLICES{,_EN}.md` 的 DR-1 块）声明的固定路径；本清单在 ⑦ 终审之前落盘，并纳入 ⑦ 的输入包。

## 一、执行自由度类（主控自行决定，未越界）

| 编号 | 内容 | 依据 |
|---|---|---|
| F-1 | 常量命名 `copilotCLICreditFloor` 与其落点（文件顶部常量块） | §1.7.1 单文件实现选择 |
| F-2 | 取值表达用 Go 内建 `max(floor, budget)` 而非自写分支 | §1.7.1 单文件实现选择 |
| F-3 | 新增两个测试用例的名称、子例划分（`floor-raised` / `floor-exact` / `above-floor`）与断言写法 | §1.7.1 单文件实现选择 |
| F-4 | 变异检验的操作方式（断言式脚本要求"恰好 1 处"命中 + 逐字节还原 + sha256 前后比对） | §1.7.3 留痕义务范围内的执行方式 |
| F-5 | 证据工件命名与包内布局（`probe-01-ai-check.json` / `probe-01-ai-verify.json` / `README.md` / `SHA256SUMS.txt`） | §1.7.1 已授权包内的实现方式 |
| F-6 | **执行顺序选择**：先 ⑨ 真探针、后写报告初稿（⑧a），以避免报告出现占位符（`§6.3` 的 G2 禁止占位形态） | §1.7.1；**已在本报告 §3 逐条登记为偏离项** |

## 二、用户裁定类（非自由度，逐条留痕）

| 编号 | 内容 | 留痕位置 |
|---|---|---|
| D-1 | R1=A：只改 CLI 侧传参、floor 为全局常量 30、记录字段与 schema 零变化、不触发 ②a、差异如实标注为已知不修 | 本报告 §3 / §11；`docs/t027/REMAINING_SLICES{,_EN}.md` 的 DR-1 块 |
| D-2 | R2=a（floor 取全局常量 30）/ R3=a（F8 不并入）/ R4=1（探针沿用 host profile）/ R5=a（成功探针记 1 premium，标注"BYOK 归属未证"） | 本报告 §2 / §12 |
| D-3 | ⑦ 降级为 `deepseek-v4-pro`：按 OB-37 元规则以 **ADR** 形式独立留痕（不占 OB 号、不再升指令版本） | `docs/ADR_REGISTER{,_EN}.md` 的 ADR-014 |
| D-4 | ⑥ 层 `FINDINGS` 的处置 = 方案 (b)：维持 R1=A + 加强留痕（不修、逐字附报告附录、走 §7.2 替代路径转入 ⑦） | 本报告 §6 / §16 |
| D-5 | 候选 OB 预留号：**OB-51**（BYOK / secretStorage 五点确认，留待收尾后）、**OB-52**（费用维度的差异，合并 ⑥ 的 High/Medium 与 Low④） | 本报告 §11 |
| D-6 | 试点继续顺延第 3 次（业务切片验证，非准则治理）：本片**不作为试点**；四要素见本报告 §4 | 本报告 §4 |

## 三、覆盖边界

- 本片**没有独立的 ① 架构方案书**：方案基线 = 用户 R1–R5 裁定（§1.2 的偏离，已在本报告 §3 登记）。
- 本清单只覆盖本片执行期；后续切片需各自落盘。
