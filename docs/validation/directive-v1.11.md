# T027 · DIRECTIVE-V1.11 准则修订验证报告（§1.7 执行自由度 + §3.4 编辑后诊断 + ⑧c 收尾清洁）

> 状态：**已完成（提交与推送须同轮授权）**。切片类型：**中等**（纯文档，行为变更 / 多文件 / 涉及规则语义）；档位：`high`；⑦ 固定 Extra High。

## 1. 变更概要（含执行自由度清单）

| 项 | 内容 |
|---|---|
| 变更文件 | `docs/DELIVERY_DIRECTIVE.md` 与其严格同位镜像 `docs/DELIVERY_DIRECTIVE_EN.md` |
| 新增节 | §1.7 执行自由度与升级边界；§3.4 编辑后诊断扫描（硬步骤） |
| 新增环节 | §5.1 流水线新增 **⑧c 收尾清洁**（⑧b 后、⑩ 前） |
| 配套 | §5.2 矩阵加 ⑧c 列；§5.3 加 ⑧c 行；§7.1 加 ⑧c 说明；§11.3 加 ⑧c 记录；附录 B.7.4 补入 §1.7 与 §3.4 节号 |
| 台账 | §0.3 变更日志 v1.11 条目 + 头部版本号 v1.11；附录 C 新增 **C.0g** |
| 未触碰 | G1–G7 判据本体、§5.1 既有步骤序列、§7.5 上限、角色集与载体、代码 / CONTRACTS / schema / fixtures / CI |

### 1.1 执行自由度清单（§1.7.1；由 ⑦ 终审复核）

| 序号 | 实质性选择 | §1.7.1 依据 | 影响范围 | §1.7.3 判定问答 |
|---|---|---|---|---|
| F-1 | 用户声明中的「§4.9」在本准则**不存在**（仅存于已废止的 v3.1 准则），改指现行锚点 | 契约措辞的补全 | §3.4.4 与 §5.1 的 ⑧c 说明 | 该决定若错了要改的是**措辞**，不是代码或契约语义 ⇒ 属执行自由度 |
| F-2 | §1.7.2 两条目措辞归一（「超切片范围」改为「超出切片范围」；「判定为」改为「判定」） | 契约措辞的补全 | §1.7.2 | 同上 |
| F-3 | ⑧c 说明中的「不只是变更集」压缩为「不只变更集」 | 契约措辞的补全 | §5.1 说明段 | 同上 |
| F-4 | 附录 B.7.4「所有」行补入 §1.7 与 §3.4 节号 | 契约措辞的补全 | 附录 B.7.4 | 同一口径的**节号索引**同步，无规则实体 |
| F-5 | 附录 C 新增 C.0g 台账块 | 契约措辞的补全（并依 OB-12 口径与 SW-3 / SW-4 / SW-5 先例） | 附录 C | 台账登记，不改执行契约语义 |
| F-6 | 落盘净增行数与 ① 方案书的预测不一致（方案书写于 F-4 与 F-5 决策之前） | — | 本报告 | 仅如实披露，无契约影响 |
| F-7 | §3.4.1 保留显式命令字面量（与 §6.1 表重复）；⑤ 曾建议改引用式 | — | §3.4.1 | 属实现表达选择；登记为有意保留 |
| F-8 | §3.4 未声明是否适用于 `[ROUTINE]` 档 | — | §3.4 | 公开登记为未决项，未静默改义 |
| F-9 | ② 实现由主控落盘、⑧ 不另派 | 用户启动声明的预算行只列 ①⑤⑥⑦ ⇒ 本片判定 ⑧ 由主控承担 | ② 实现步骤 | 若错了要改的是**执行角色**而非代码或契约语义 ⇒ 属执行自由度；但 ⑤ 第 2 轮指出该选择**未列入本清单、因而未经 ⑦ 裁定** ⇒ 补登记并提请裁定 |

> ⑦ 第 1 轮对 F-1 至 F-8 的逐项裁定：**全部 `in-bounds`**（四问全否）。

## 2. 执行流水线

| 环节 | 角色与模型 | 结果 |
|---|---|---|
| ① 前置分析 | `deep-reasoner` / `DeepSeek V4 Pro (deepseek)` | 方案书（逐处落盘清单、镜像策略、G1–G7 风险清单、5 项未决 U1–U5） |
| ② 实现 | 主控（本片 ⑧ 不另派） | 两文件落盘；U1 与 U2 与 U4 按自主裁定执行并登记（见 §1.1） |
| ④ 集成与门禁 | 主控 | `SELFTEST: PASS`；`--all --scope=tree` 与 `--scope=index` 均 `TOTAL_FAIL=0` |
| ⑤ 预审 | `deep-reasoner` / `DeepSeek V4 Pro (deepseek)` | `PASS WITH FIXES`：High 一项 + Low 与 Info 若干 |
| ⑥ 独立扫描 | `independent-reviewer` / `MAI-Code-1.1-Flash (copilot)` | `INDEPENDENT SCAN: PASS`、0 条发现 |
| ⑦ 终审 | `independent-reviewer` / `GPT-5.3-Codex (copilot)` | 第 1 轮 `FINDINGS` 一项 Medium；第 2 轮 `INDEPENDENT SCAN: PASS`、0 条发现；第 3 轮（用户本轮授权、收窄三问）`PASS WITH FIXES`、无 Medium+，三问均裁定成立 |
| ⑧a 与 ⑨ 与 ⑧b 与 ⑧c | 主控 | 见 §4 与 §5 |

## 3. 审查摘要与逐条处置

| 来源 | 发现 | 处置 |
|---|---|---|
| ⑤ 预审 | High：§0.3 的 v1.11 条目声称附录 C 新增 C.0g 而磁盘上尚无 | 已修：按 ⑤ 自己给出的选项 a **立即落盘 C.0g**，使该断言成真 |
| ⑤ 预审 | Low：§1.7.2 两条目与用户原文非逐字；⑧c 说明一处措辞压缩 | 登记为 F-2 与 F-3 并保留（条目数与语义未变） |
| ⑤ 预审 | Low：§3.4.1 与 §6.1 表命令字面量重复 | 接受但保留（§3.4.1 的目的即给出可执行命令）；登记为 F-7 |
| ⑤ 预审 | Low：「新增告警」缺基线定义，该硬约束部分不可机械核验 | 登记为未决项（与 §3.4.3 的聚合器实现同批处理） |
| ⑤ 预审 | Low：§3.4 未声明是否适用于 `[ROUTINE]` 档 | 登记为 F-8 未决项 |
| ⑥ 独立扫描 | 无发现（`PASS`） | — |
| ⑦ 终审 | Medium：§1.7 留痕句与 §5.1 时序不可同时满足 | 已修：改写该句，复核载体明确为 ⑦ 审查输入包中的同一清单、⑧b 定稿回写；⑦ 第 2 轮确认**已闭合且无新增缺陷** |
| ⑤ 预审 | 复审（第 2 轮，按 §5.3 补跑）：仍为 `PASS WITH FIXES` —— M-1 与 M-2 与 L-1 与 L-2 与 L-3 | 处置见 §5 与 §6：M-1 已以全仓逐文件编码核验补证；M-2 已补登记为 F-9；L-1 与 L-2 与 L-3 已改措辞 |
| ⑦ 终审 | 第 3 轮（收窄三问）：(a) F-9 是否 `in-bounds`；(b) ⑧c 口径由变更集扩为全仓逐文件是否可接受；(c) 结论行改判 `FINDINGS` 是否成立 | 三项均裁定**成立**：(a) `in-bounds`（属记实补登，非新增规则实体）；(b) **可接受**（⑧c 的基线口径本就如此，本轮只是从「字面扩张未执行」纠正为「真执行并如实披露」，豁免边界与自洽性核验已落盘，残余为 OB-27 待正式化）；(c) **成立**（既已实测发现 3 个既有违规，结论行不可继续为 `PASS`）。本轮**无 Medium+**，红线未触发 |

> **一处如实上报**：⑤ 报出 High 后本片**未重跑 ⑤**（预算上限 ⑤×1）。理由：该 High 已按其自身给出的选项以**确定性动作**闭环（落盘 C.0g 使断言成真），其余为 Low 与 Info 登记项，且**本次修复未改动 §1.6 与 §1.7 与 §3.4 与 §5.x 与 §6.3 等规则正文**。**⑤ 复审已按 §5.3 补跑**（见 §2 与 §3）。

## 4. 门禁与原生验证

| 项 | 结果 |
|---|---|
| `node tools/gates/gate.js --selftest` | `SELFTEST: PASS` |
| `--all --scope=tree` 与 `--scope=index` | 均 `TOTAL_FAIL=0`（G1–G7 与基础门禁） |
| 编码门禁 | 变更文件均为 UTF-8 with BOM 加 LF，违规 0 |
| 双语同位 | 行数、标题位置与层级、加粗、竖线、空行五项全等；逐行 0 失配 |
| 链接与引用可解析性 | 新增引用的节号（§1.6 与 §6.1 与 §6.3 G4 与 §5.1 与 §5.2 与 §5.3 与 §7.1 与 §11.3 与 附录 B.7.4 与 附录 C）均在本准则内存在；用户声明引用的 §4.9 不存在，已按 F-1 改写 |
| 文档型切片的退化形态 | 按 §5.2 说明，⑨ 退化为「编码门禁 加 G1–G7 自检 加 链接与引用可解析性检查」 |

## 5. ⑧c 收尾清洁记录（首次执行）

| 字段 | 结果 |
|---|---|
| 执行时刻与执行者 | 2026-09-23；主控（0 付费调用） |
| 全仓诊断（按 §3.4.1 类别） | 编译型：`go build ./...` 与 `go vet ./...`（经 `gate.js` 基础门禁）；脚本语言：本轮无变更；标记数据：两文件编码门禁与 `gate.js` 解析；纯文本其他：无；未覆盖类型：无 |
| IDE 诊断汇总 | 见 §6 未覆盖项 |
| `gate.js --all --scope=tree` | `TOTAL_FAIL=0`；`--scope=index` 与 `--scope=ci` 同为 `TOTAL_FAIL=0` |
| 编码门禁（全类型逐文件） | 全仓 `git ls-files` 逐文件核验：文本文件 2820 个，**违规 3 个**，全部位于冻结证据包（`b2-2026-09-17/` 与 `b3b-2026-09-21/`）；按**临时豁免**处置，见 §6 |
| 冻结证据包自洽性核验（豁免条件二） | b2 41 条、b3a 128 条、b3b 2158 条的 `SHA256SUMS.txt` 与实际文件**逐条一致、0 缺失、0 失配**；`b1-20260917` 无 `SHA256SUMS.txt` ⇒ **不可自核**（如实披露） |
| 临时目录与探针残留与未跟踪文件 | `git status --porcelain -uall` 仅本片 4 个暂存变更、无未跟踪残留；`tmp/` 下本片材料为 `tmp/v111/**` 与 `tmp/b4check/**`（仓库不跟踪） |
| 结论行 | `WRAP-UP CLEANUP: FINDINGS`（全类型逐文件编码核验发现 3 个**冻结证据包内**的既有违规；修复会破坏证据包完整性 ⇒ 属 §1.7.2 的升级边界，已停下登记并取得用户裁定，按临时豁免与证据包自洽性核验处置，见 §6） |

**三个里程碑**：① **⑧c 首次自指验证执行完成**——本片是「首个新增 ⑧c 后立即被 ⑧c 检查的切片」，⑧c 首跑即给出 `FINDINGS`（见上表与 §6）；② **⑧c 首跑当场抓出 3 个历史遗留**——全仓逐文件核验发现 3 个既有违规，证明该硬项**确实生效**；③ **⑦ 对 §1.7 执行自由度清单 F-1 至 F-8 逐项裁定全部 `in-bounds`**——新增的留痕与越界判定机制首次运行即生效（清单随 ⑦ 输入包交付并获裁定）。

## 6. 未执行事项与未覆盖项

| 事项 | 原因 |
|---|---|
| IDE 诊断汇总的实际计数 | 本会话无 Problems 面板读数接口；登记为未覆盖，不宣称已核验 |
| ⑤ 第 3 轮 | 用户裁定**不授权**：M-1 的整改超出 ⑤ 的审范围（范围界定属规则语义裁决），M-2 与 L 属台账元数据与文案类，按 **OB-11 例外标准**免复审 |
| §3.4 对 `[ROUTINE]` 档的适用性 | 未决项 F-8，交 §12 修订评估 |
| 冻结证据包内的 3 个既有编码违规（b2 的 `probe10-dir-listing.raw.txt` 为 CRLF；b3b 的两个 `pin.json` 带 BOM） | 用户裁定**选 A**：① 这 3 个文件**不违反 G4a**（G4a 作用域明写「变更集」），违反的是 §5.1 ⑧c 的「全类型逐文件」⇒ 属「⑧c 扩范围撞上历史遗留」；② **临时豁免**（待 `DIRECTIVE-V1.12` 正式化）——豁免严格限于「**已归档、带 MANIFEST/SHA256SUMS 的冻结证据包**」，**新增证据包提交前仍须过编码门禁**；③ ⑧c 对证据包改做**自洽性核验**（见 §5）；④ 登记 **OB-27**，含「协调 G4a 与 ⑧c 与 B2 保真裁决」待议项（`.raw.txt` 的 CRLF 与 `.gitattributes` 的 `*.raw.txt -text` 直接冲突） |
| 「新增告警」的基线定义 | 未决项，与 §3.4.3 聚合器实现同批 |

## 7. 成本与计量

预算：①×1 加 ⑤×1 加 ⑥×1 加 ⑦×2；实得：①×1 加 ⑤×1 加 ⑥×1 加 ⑦×3（⑦ 终审一轮、复审一轮，另加用户本轮**另行授权**的第 3 轮——收窄为三问；⑤ 第 3 轮**未授权**）。探针 0 次。

## 8. 下一步建议

- ⑩ 停点：⑦ 第 3 轮已通过（三问均成立，无 Medium+）⇒ 本片可提交与推送，推后按 §6.4 观察 CI 双绿；F-8 与「新增告警」基线与 OB-27 三项交 `DIRECTIVE-V1.12` 评估。
- 下一片候选（用户已列，不并入本片）：`GATES-TIGHTEN`（OB-17 与 OB-20）；`DIRECTIVE-V1.12`（OB-10 与 OB-12 与 OB-16 与 §5.3 与 §7.5 冲突标准 与 OB-23）。

## 9. 产物清单

```artifacts
docs/DELIVERY_DIRECTIVE.md	repo
docs/DELIVERY_DIRECTIVE_EN.md	repo
docs/validation/directive-v1.11.md	repo
docs/validation/directive-v1.11_EN.md	repo
tmp/v111/**	local-only
tmp/b4check/**	local-only
```
