# SW-5 工作分级与启动授权（准则 v1.9）

日期：2026-09-22。状态：`COMPLETE`。提交：`fb12ff9`（v1.9 正文）+ `b738e6b`（台账收尾）。
CI：run `35632485437`、`35633761736`（**均首跑双腿绿**）。推送：`origin/main`（**未推 gitee**）。

## 0. 诚实准则

1. 本片**只做**三件事：准则新增 **§1.6 工作分级与启动授权**并接线（§1.1 / §5.0 / 附录 B.7 / §0.3 / 版本号 / 附录 C.0e）、新增 OB-10 / OB-11 与 §12.5 缺口行、按 G4b **自带的**白名单登记流程补 1 字（`竖`）。**不改** G1–G5 判据、不改 §7.5 上限数值、不改角色集 / 载体、不碰代码 / CONTRACTS / schema / fixtures / CI。
2. 报告中的提交号、run 号、门禁结果与计数**全部取自实际证据**；未取得者显式标注「**历史缺失**」，**不推断、不编造**。本片是**纯文档切片**（按 §5.2「说明（文档型切片）」退化执行），故不含代码类变异检验。

## 1. 变更概要

1. **§1.6（新增）工作分级与启动授权**：三档启动——`[ROUTINE]`（默认、无触发词）/ `[SLICE]` / `[PILOT]`；**触发词语法全文仅此一处定义**（形态、位置、全角半角、大小写、多触发词处置）。
2. **日常档不可绕过边界**：① 为**对 §7.2 ③ 的引用**（状态机 / 门禁判据 / 证据模型 / 角色 / 授权契约），**不重复定义**；②–⑥ 为**增量边界**——CONTRACTS / schema / fixtures 语义、任何 `.agent.md`、涉及状态机 / 崩溃路径 / 所有权 / 并发的代码、`.github/workflows/**`、**新增 / 删除 / 升级 / 降级依赖（含锁文件变化）**。
3. **主控的判定与建议义务**：**先判定级别**并在**回执首行**声明；**可建议、不擅自升级**；**级别单向**不得半途降级——用户要求降级 ⇒ **停止切片**并以 `[ROUTINE]` 重启；**停止态三则**：工件保留原位但不得按已完成切片提交、报告头须标 `**状态：已停止——未完成**` 并列未完成清单、台账该行保持 `🔄` 或改回 `⬜`（由用户裁决）。**不因日常档豁免 §10**；日常档留痕 = diff + §6.1 + §6.3，**0 次付费调用**。
4. **接线**：§1.1 加指针；§5.0 加「启动前置」一句（流水线只在触发词出现时启动）＋**步 0 前置硬门补入 `SW-3`**；附录 B.7 按三档扩充（标题由「切片启动卡」改称「**启动卡**」，B.7.1 起跑消息 / B.7.2 开场回执 / B.7.3 停点回执均分档）；§0.3 新增 v1.9 行；头部版本号 v1.8 → v1.9；附录 C.0e 记录本片（含**首次角色裁剪降级判断留痕**）。
5. **观察项与门禁数据**：新增 **OB-10**（§1.6 新条款缺 G1–G5 机械判据）、**OB-11**（台账元数据类整改免再跑 ⑦ 的例外判定标准）；§12.5 新增「§5.3 与 §7.5 冲突裁决标准」缺口行；**G4b 白名单 +1 字**（`竖`，属该判据自带的**数据登记流程**、每字须附理由与上下文摘录，**非判据变更**）。

## 2. 流水线执行

| 环 | 角色 / 载体 | 状态 | 备注 |
|---|---|---|---|
| ① 架构 / ⑤ 预审 | —（V4 Pro） | **豁免** | 用户裁决：本片无技术架构面、无测试点、不涉代码 |
| ② 实现 | 文档工程师（⑧a） | 完成 | 按 §5.2「说明（文档型切片）」退化为**文档实现** |
| ③ 测试 | 文档工程师（⑧a） | 完成 | 退化为**文档一致性检查** |
| ④ 主控集成 + 门禁 | 主控 | **全绿** | §6.1 基础门禁 + §6.3 G1–G5 |
| ⑥ 独立扫描 | MAI-Code-1.1-Flash | **×1 `PASS`** | Section A–E 齐备，**0 条发现** |
| ⑦ 独立终审 | Codex（GPT-5.3-Codex） | **×2**（终审 1 + 复审 1） | 首轮 7 条 → 复审 1 条，全部闭合 |
| ⑧a / ⑧b 文档回写 | 文档工程师（⑧a） | 完成 | CN 权威 + `_EN` 镜像严格同位 |
| ⑨ 原生验证 | 主控 | 退化执行 | 编码门禁 + G1–G5 + **引用可解析性**检查 |
| ⑩ 停点 | — | **等授权** | commit / push 按 §10 求同轮授权 |

**返工次数**：⑦ 终审后整改 **1** 轮（3 High + 4 Medium）；复审后整改 **1** 轮（1 Medium）；**⑦ 无效轮次 = 0**。

**环境与档位**：Windows 主控 + PowerShell（BOM / EOL / 门禁核验脚本）；Go 1.22 工具链原生执行 `gofmt` / `build` / `vet` / `test`；付费审查载体 ⑥ `MAI-Code-1.1-Flash (copilot)`、⑦ `GPT-5.3-Codex (copilot)`。**档位（§8.2）：历史缺失**——未取得 operator 侧档位记录；按 §1.6，级别声明**不代替**档位设定，故不作推断。

**异常与兜底记录**：本片无 ①⑤ 派发、无原生实验、⑦ 无效轮次 0；**兜底行动计数未取得记录 ⇒ 历史缺失**（§9.2 口径）。已发生并已整改的异常（非兜底）：⑦ 终审 7 条、⑦ 复审 1 条、我方台账类缺陷同片内 3 次（§4.4）、⑦ 审查包缺件（M4）。

## 3. 角色裁剪降级判断留痕（本片**首次**出现）

- 按 **§5.2 字面**：§1.6 涉及**授权契约语义** ⇒ **§7.2 ③ 不成立** ⇒ 落「**中等**」行，而中等行要求**有测试点**——本片无。
- **实质落「简单」**，三条理由：① 只有「复杂」行才涉**技术架构面**，SW-5 无；② 「中等」行要求**有测试点**，SW-5 无；③ §1.6 的「授权契约语义」是**规则语义**、**非技术架构**。⇒ 结论：**豁免 ①架构与 ⑤预审**；⑥ 因 §7.2 ③ 不成立**不得跳过**，实测已跑。该判断与理由已同步记入附录 C.0e。

## 4. 审查摘要

**⑥（MAI ×1）**：`INDEPENDENT SCAN: PASS`；Section A–E 齐备；**0 条发现**（Medium+ = 0）；首轮**不附任何清单**（隔离证明见本节末段）。

**⑦ 终审（Codex ×1）**：`FINDINGS` = **3 High + 4 Medium**，逐条闭合：

| # | 严 | 发现（摘要） | 处置 |
|---|---|---|---|
| H1 | High | 日常档边界只覆盖「新增或删除依赖」，未覆盖升级 / 降级 / 锁文件 | 改为「新增 / **删除 / 升级 / 降级**依赖（含锁文件变化）」；用户裁决属「**补全意图**」 |
| H2 | High | C.0e 自称「不改 §5.0 步 0 硬门」却实际上改了它 | 台账**三行如实改写** |
| H3 | High | C.0e 内「待 ⑥ / 未过 ⑦」与「⑦ `PASS`」自相冲突 | 拆分为「验收证据（**目标**）」与「⑥ 独立扫描（**实得**）」 |
| M1 | Medium | 首行多触发词无消解规则 | 新增「**不生效 + 停下要求重发**，不得自行择一」 |
| M2 | Medium | B.7 出现**规则口吻**句 | 三处**去规则化** |
| M3 | Medium | 交付物未声明**第 3 个**改动文件 | 补记 G4b 白名单项 |
| M4 | Medium | T3/T4 审查包**缺角色文件 hunk** | 实测头注上移字节在 **`e0b08ec`（v1.5）** 而非 `52c28af`（docs-only）；已补 `T4-note-move-e0b08ec.diff` |
| R1 | 拒绝 | 建议为 §1.6 新条款补 §6.3 机械判据 | **越 SW-5 边界，不采纳**；登记 **OB-10** |

**⑦ 复审（Codex ×1）**：`FINDINGS` = **1 Medium**（C.0e 状态行「待 ⑥」与下方「⑥ 实得 `PASS`」**时态冲突**）⇒ 已修（并去掉会过期的 `995/995` 字面量）。**首轮 7 条经其独立核验全部「已闭合」**（给出 CN / `_EN` **同位行号**证据）；CN / `_EN` **未见语义漂移**。

**负面发现（如实记录）**：① **我方台账类缺陷在同一片内出现 3 次**——H3、复审 Medium、收尾前自查**又漏 4 处**；② **我方 ⑦ 首轮交付的审查包缺件**（M4）；③ **审查方的修法建议有 1 条越界**（OB-10）。

**审查输入包摘要与隔离证明**：⑥ 与 ⑦ **首轮均不附任何清单**（§7.1 / §7.2；§7.3 锚定硬隔离，仅**复审轮**附清单并标注「完成独立扫描后再读」）；输入 = 原始 diff + 只读仓库，按 §7.3 消毒（**逐项记录未取得 ⇒ 历史缺失**）。按 **OB-9**，本次 ⑦ 输入**一并纳入**四类历史欠账（T3/T4 判据改动 diff、v1.7 改动 diff、v1.8 改动 diff、`_EN` 镜像）；审查包落盘路径 **历史缺失**。盲审（§7.7）：本片**未触及**所有权 / 停机 / 身份语义 ⇒ 必抽不触发；**按比例抽查记录 ⇒ 历史缺失**。

## 5. 门禁与证据

| 门禁 | 结果 |
|---|---|
| G1-a 表格结构完整性 | PASS ✅ |
| G1-b 版本元数据一致性 | PASS（头部 v1.9 = §0.3 末行 v1.9） ✅ |
| G2 占位符残留 | PASS ✅ |
| G3 双语对称 (a.1) / (a.2) / (b) | PASS ✅ |
| G4a 编码（BOM + LF） | PASS ✅ |
| G4b 异常字（差集 ⊖ 白名单 = 空） | PASS ✅ |
| G5-a ① / ② | PASS ✅ |
| G5-b 变更集 `model` 取值闸 | PASS ✅ |
| R2.5 头注位置锚定 | PASS ✅ |
| 基础门禁 | `gofmt -l .` 空；`go build ./...` / `go vet ./...` / `go test ./...` = `ok=14 FAIL=0` ✅ |
| **TOTAL_FAIL** | **0** ✅ |

- CN / `_EN` **严格同位**：提交时 **1000/1000**；收尾后 **1001/1001**；**标题行号序列 drift = 0**；**加粗 / 竖线 / 空行 / ID 计数逐项相等**；两文件编码 UTF-8 **BOM + LF**（G4a 逐文件核验）。
- 门禁执行方式：`tools/gates/` **尚未脚本化**（仅白名单落盘），故本片门禁由主控**临时脚本**执行，**已用完清除**（见 §8）。

### 5.1 可证伪性

本片为纯文档切片，**无代码类变异检验**（**不适用**）；机械守卫的反证如下（**设计层面陈述，本片未逐条实跑**）——G4b 白名单项 `竖` 被移除 ⇒ 差集非空 ⇒ **G4b 红**；只改 CN 或只改 `_EN` ⇒ 改动行数不对称 ⇒ **G3 红**；头部版本号 ≠ §0.3 末行 ⇒ **G1 红**；头注移入正文（非 frontmatter 后首个非空行）⇒ **R2.5 位置判据红**。**A11 对称抽查 / A12 独立佐证：记录未取得 ⇒ 历史缺失**。已知**无机械判据**的两处（停止态报告头标记、`[PILOT]` 试点指标槽位）= **OB-10** 所指缺口。

## 6. 成本与计量（R7.1 / §11.3 字段）

| role | model | stage | 次数 | 备注 |
|---|---|---|---|---|
| 主控 | `DeepSeek V4.1 Flash (deepseek)` | ⑧ 门禁 / 集成 / 报告装配 | 全程 | 自举产出本片工件 |
| 文档工程师（⑧a） | `DeepSeek V4.1 Flash` | §1.6 草稿 / v1.9 镜像 / 整改行 / OB-11 + §12.5 / 收尾行 | **5** | **非 §7.5 计费类别** |
| 文档工程师（⑧a） | `DeepSeek V4.1 Flash` | 本报告 + 其 `_EN` 镜像 | **2** | 同上 |
| 独立扫描（⑥） | `MAI-Code-1.1-Flash (copilot)` | ⑥ 独立扫描 | **1** | `INDEPENDENT SCAN: PASS`（额度 3 内） |
| 独立终审（⑦） | `GPT-5.3-Codex (copilot)` | ⑦ 终审 | **1** | `FINDINGS` 3 High + 4 Medium |
| 独立终审（⑦） | `GPT-5.3-Codex (copilot)` | ⑦ 复审 | **1** | `FINDINGS` 1 Medium |

- **付费调用合计 = ⑥ ×1（MAI）＋ ⑦ ×2（Codex：终审 1 + 复审 1）= 3 次**，均在 §7.5 额度内（⑥ ≤ 3；⑦ 1 + 1）；①⑤ **豁免** ⇒ **V4 Pro 0 次**；⑦ **无效轮次 = 0**；探针额度使用 **0 次**（无真实外部调用）。
- 与启动时预算声明的差异：原声明 ⑥×1 + ⑦×1，实际 ⑥×1 + ⑦×2（复审属 §5.3 义务），已在附录 C.0e「成本」行如实记录；`startedAt` / `retryOf` 字段**未逐条记录 ⇒ 历史缺失**（`role` / `model` / `stage` / 次数按 §7.1 逐项列出）。

## 7. 例外与观察项登记

| # | 内容 | 处置 |
|---|---|---|
| **OB-9** | v1.7 / v1.8 / T3 / T4 的独立验证缺口 | 本轮 ⑦ 已按 OB-9 要求把这四类输入**一并纳入** ⇒ 缺口**闭合** |
| **OB-10** | §1.6 新条款缺 G1–G5 机械判据 | **拒绝 + 登记**（越 SW-5 边界）；含「提交前又漏 4 处」的**实证注记** |
| **OB-11** | 台账元数据类整治免再跑 ⑦ 的**例外判定标准** | 登记，并入下次修订的 ⑦ 输入；附带教训：台账**不写会过期的字面量** |
| **§12.5 缺口行** | 「§5.3 与 §7.5 冲突裁决标准」 | 本片落地后即可起草；下次 §12 修订正式写入 |
| **SW-4 CI 记录** | `SW-4` 的 CI run 在 §0.3 v1.8 行与附录 C.0d 中**均无记录** | 记「**历史缺失**」 |

## 8. 未执行事项

1. **`tools/gates/` 的 G1–G5 脚本化仍待授权切片**（本片门禁由主控临时脚本执行，已用完清除）。
2. **`DEV_PLAN` 回写（口径裁定）**：本片**不回写** `DEV_PLAN{,_EN}.md`。理由：`DEV_PLAN` 的体例为 **T027 前置状态更新 / prfrail 项目实施计划**，**不涵盖“编制执行准则”本身**（用户 2026-09-22 明确口径）；准则治理切片的权威台账是**准则附录 C.0–C.0e**，两套台账分域、不混写。⇒ `SW-1` / `SW-2` / `SW-3` / `SW-4` **同样未写 `DEV_PLAN`**，不是遗漏，是本就不该写。
   **实测依据（推翻了原假设）**：`DEV_PLAN{,_EN}.md` 内没有任何 `SW-*` 段落（只有 T027 的 A0–A7 / B3a–B3c）；`SW-1` 当时的回写是 **§1 的新准则指针**（现存，`DEV_PLAN.md:17`，可用 `git grep` 复核），**非切片段落**。
   **待议**：是否在 §11.2 增一句“准则治理切片的回写范围（附录 C 而非 `DEV_PLAN`）”，留待下次修订评估（登记为 **OB-12**）；**§11.2 本片不动**（规则本体变更不得由收尾顺手做）。
3. **DR 修复切片（试点）未开**；**未推 gitee**（按规则：gitee 仅镜像备份）。
4. ⑦ 复审后的**台账元数据类整改未再跑 ⑦**（例外判定标准见 **OB-11**）。

## 9. 下一步建议

- 进 **DR 修复切片（试点）**：按用户定性「**同包系统性时序问题**」**合并修复 DR-2 / DR-3 / DR-4 / DR-5**；其输入**必须**含 ① **累计发生次数表** ② **3 条不同测试**的 `AUDIT TRAIL BROKEN` **交织证据的归属判定**。
- 一并纳入 **OB-10 / OB-11** 的待办输入（§6.3 机械判据是否补入、例外判定标准）。
- §12.1 五指标以 **B3c** 为基线；角色档位建议：DR 切片属**行为变更类**（中等 / 复杂）⇒ ①（V4 Pro）与 ⑤ 恢复启用、⑥ 按 §7.2 判定（行为变更是启用条件）、⑦ 保留终审 + 复审各 1。
