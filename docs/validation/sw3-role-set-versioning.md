# SW-3 · 角色集入库与编码归一 — 验证报告

日期：2026-09-21。状态：`IN_REVIEW`（⑥ 与 ⑦ 见 §6；提交与推送见 §8）。

## 0. 诚实准则

1. 本片**只做**三件事：11 个既有角色文件的**编码归一（no-BOM+CRLF → BOM+LF）**、**14 个文件统一头注**、**准则 R2.5 措辞修订**。**不改**任何 `model` / `tools` 取值，**不改**职责/约束正文。
2. 报告中的字节数、对账结果、门禁退出码**全部取自实际命令输出**；未执行者写入 §7。
3. **已知不修项**：**8** 个既有文件的默认 `model` 属 §2.4 黑名单（Terra 3 / Luna 2 / Gemini 1 / GPT-5.4 1 / GPT-5.6-Sol 1）⇒ 已登记为 **OB-8**，本片**不动**（不扩大范围）。

## 1. 范围与边界

- 授权：用户 2026-09-21 裁决**选项 A**（归一化 + 全部入库），并给出头注文本与 R2.5 新措辞。
- 边界遵守：未改 `model` / `tools` 取值；未改职责/约束正文；未触碰 `sol-orchestrator/` 工具链。

## 2. 交付物

| # | 交付物 | 内容 |
|---|---|---|
| 1 | 11 个既有角色文件 | BOM+LF 归一化 + 头注一行；**全部入库** |
| 2 | 3 个 `prfrail-*` 角色文件 | 头注统一为用户指定文本（替换原措辞） |
| 3 | `docs/DELIVERY_DIRECTIVE.md` | R2.5 措辞修订（用户口径）+ 头注硬规则与机械判据 + OB-8 + C.0c 状态；版本 v1.4 → **v1.5** |
| 4 | `tools/gates/cjk-newwords.txt` | 补录 4 个新合法字（诱/横/幅/犯）与 2 个既有字（刀/月） |

## 3. 可证伪性（本片最关键的两条）

| # | 断言 | 机械判据 | 结果 |
|---|---|---|---|
| 1 | **仅行尾/BOM/头注变化**（无语义变更） | 字节对账：`原字节 − 换行数 + 3(BOM) + 124(头注) + 2 = 现字节` | **11/11 精确成立** ✅ |
| 2 | 同上（独立佐证） | 与生成器自带副本 `.github/agents/sol-orchestrator/generated/sub/_*.agent.md` 比对：**去掉头注块（头注行 + 紧邻空行）并统一行尾后，全文串必须逐字相等**（不折叠空行、不 trim） | **10/10 逐字相等** ✅（第 11 个 `sol-orchestrator.agent.md` 无 `sub/` 副本，仅靠断言 1） |
| 3 | 14 个文件均带头注 | 逐文件 `includes(头注文本)` | **14/14** ✅ |
| 4 | 全部入库且未被忽略 | `git check-ignore -v` | `prfrail-*` 与 `deep-reasoner` **无输出**；`sol-orchestrator/_config.yaml`、`*.managed.json` **仍命中** ✅ |
| 5 | `prfrail-*` 无 `model` 键（G5-a） | frontmatter 正则 | **0 命中** ✅ |

> **反证**：断言 1 的守卫 = 「归一化脚本同时删 `\r` 并前置 BOM」；若脚本只补头注而不动行尾，对账式会得 `原字节 + 124 + 2`（差 21/95 与 3）⇒ **会红**。
> **对账式的两处陷阱（实测）**：① 必须计入 BOM 的 **3** 字节；② 22 个 `split('\n')` 元素对应 **21** 个换行（`sol-orchestrator` 为 96/95）。二者任一漏算即得"假 FAIL"。

## 4. 门禁结果

| 门禁 | 结果 |
|---|---|
| `gofmt -l .` | 空 ✅ |
| `go build ./...` / `go vet ./...` / `go test ./...` | exit 0 / 0 / 0；`ok=14`、`FAIL=0` ✅ |
| G1-a 表格结构（变更行） | PASS ✅ |
| G1-b 版本元数据一致 | PASS（头部 v1.5 = 日志末行 v1.5；6 行无重复） ✅ |
| G2 占位符残留 | PASS（0） ✅ |
| G4a 编码（16 文件） | PASS ✅ |
| G4b 异常字（⊖ 白名单） | PASS（0） ✅ |
| G5-a 角色 frontmatter 硬闸 | PASS ✅ |

## 5. 一致性回写

- 准则 §0.3 新增 **v1.5** 行；附录 **C.0c** 由「待授权」改为 **✅ 已完成**，并同步「交付物/验收证据」口径以匹配实际范围（含头注），避免"状态与范围不一致"的旧疾复发。
- R2.5 同时写入**头注硬规则**（逐字文本 + `grep -L` 机械判据）与 **OB-8** 已知隐患。

## 6. 审查

### 6.1 ⑥ ③.5 独立扫描（SW-3 自有额度，第 1 次）

**结论行**：`INDEPENDENT SCAN: PASS`（0 发现）。**首轮不附清单**；Section A–E 格式合规，Section D 逐项给出“删掉某守卫谁会红”，Section E 列明“无法验证的部分”与“什么会推翻 PASS”。输入：`tmp/sw3-review.diff` + 只读仓库。计量：SW-3 自有额度 **1/3**。

### 6.2 ⑦ 独立终审（第 3 轮，用户授权一次性突破 §7.5 上限）

**结论行**：`RE-REVIEW: FINDINGS`（3 Medium + 1 Low）。**R1–R8 复核：7 条闭合、R7（G5-a 鲁棒性）部分闭合**；本轮 4 条发现**逐条复核后全部成立并已整改**：

| # | 严 | 发现（摘要） | 处置 |
|---|---|---|---|
| T1 | Medium | “9 个既有文件默认 `model` 属黑名单”**计数错误**，实为 **8**；且未给族分解 | 已改：准则 R2.5 / OB-8 / 本报告三处均改 **8**，并补族分解（Terra 3 / Luna 2 / Gemini 1 / GPT-5.4 1 / GPT-5.6-Sol 1） |
| T2 | Medium | 断言 2 的“10/10 逐行等价”口径与实测不符——须去掉**头注块**（头注行 + 紧邻空行），而非仅去头注行 | 已改：措辞改为“去头注块 + 统一行尾后全文串逐字相等（不折叠、不 trim）”，并固定副本路径；**重跑后 10/10 成立** |
| T3 | Medium | G5-a 只匹配固定形态且只覆盖 `prfrail-*` ⇒ 空白变体与其他 11 个文件漏检 | 已改：G5-a 加固为**两条判据**（① `prfrail-*` 无 `model` 键；② **全体** `.agent.md` 无空值 `model`，含 `^\s*model\s*:\s*$` / `model: ""` / `model: ''` 三种形态） |
| T4 | Low | 头注判据只查“文内出现”，未锚定位置 ⇒ 头注移到正文仍可通过 | 已改：判据改为**位置锚定**（frontmatter 后首个非空行必须等于头注文本）；并**上移 3 个 `prfrail-*` 的头注块**至 H1 之前，使 14 个文件位置统一（实测 **14/14**） |

**⑦ 第 3 轮的授权与成本口径（用户要求显式记录）**：**`⑦ 第 3 轮为例外授权：§5.3 与 §7.5 冲突，经用户裁决按闭环优先；本次为一次性突破，不构成新上限`**——⑦ 额度按 §7.5 本应为 2（终审 1 + 复审 1），本片实际用 **3**（含经授权的第 3 轮）；该优先关系**仅适用于本次且不写入准则**（避免被当作新上限）。

**第 4 轮**：按用户裁决**不自动授权**；本轮 4 条整改**仅经我方机械自证**（未再走独立复审）⇒ 该残留交用户裁决。

## 7. 明确未执行事项

1. **未修 8 个默认 `model` 属黑名单的既有文件**（OB-8，待用户裁决）。
2. **未重跑生成器**，也未把 `_config.yaml` 纳入版本控制（R2.5 已写明"若日后启用"的前置条件）。
3. **`DELIVERY_DIRECTIVE_EN.md` 未生成**（用户裁决：等 ⑦ 第 3 轮 PASS 后生成）。

## 8. 提交与推送

- 提交：`git add <具体文件>` 逐个（**禁止 `-A`**）。
- 推送：**仅 `origin`**（gitee 不动）；按用户裁决**先 ⑦ 后推送**。
- CI：推送后观察并**双绿回报**（§6.4）。
