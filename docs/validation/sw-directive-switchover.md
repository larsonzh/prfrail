# 准则切换 · SW-1（引用改写）+ SW-2（角色载体就绪）— 验证报告

日期：2026-09-21。状态：`COMPLETE`（SW-2 实机探针通过；SW-1 改写完成；⑦ 独立终审见 §7）。

## 0. 诚实准则（本报告所有结论的前提）

1. 本片只做**准则切换与角色载体建设**：不改任何 `.go`、`schema/`、fixtures，不改契约语义，不改任何既有角色文件的语义。
2. 报告中的字节数、命中数、退出码、残留计数**全部取自实际命令输出**；未执行者一律写入 §9。
3. `docs/DELIVERY_DIRECTIVE_EN.md` **尚未生成**（§11.1 要求 CN 冻结后再出镜像）。故本片所有 `_EN` 文档中的引用**指向 CN 权威文件**并已注明；这是**已知临时状态**，不是遗漏。
4. 本片由**主控直接产出产品工件**——这是角色就绪预检的引导期例外（**锚点 = 准则附录 C.2**；豁免对象是 **§3.2 的“不原创产品工件”**规则），且**不豁免**独立审查。

## 1. 范围与边界

**SW-2（角色载体就绪）**——解除 Codex 盲审判定的执行死锁（Critical）：

- 新建 3 个角色文件：`.github/agents/prfrail-{implementer,tester,documenter}.agent.md`（`tools` 含 `edit`）。
- `.gitignore` 由目录级 `.github/agents/` 收窄为 `.github/agents/sol-orchestrator/` + `.github/agents/*.managed.json` + `**/__pycache__/`。
- 边界遵守：**未改动**任何既有角色文件（含 `sol-orchestrator.agent.md` 与生成器工具链）；未改代码。

**SW-1（引用改写）**——消除双准则并行（Codex C1）：触碰 **17 个文件**（§2.2）。**未改** v3.1 正文（仅文件头加指针），**未改**历史验证报告正文（仅“关联文档”行）。

## 2. 执行流水线

### 2.1 阶段表

| 阶段 | 角色 | 载体 + 模型 | 结果 |
|---|---|---|---|
| ① 架构 | — | — | **不适用**：无架构决策 |
| ② 实现 | 主控（自举） | 会话本体 `DeepSeek V4.1 Flash` | 3 个角色文件、`.gitignore`、17 处引用改写、`tools/gates/cjk-newwords.txt` |
| ③ 测试 | — | — | **退化**（§5.2 文档型切片）：以“引用断言 + 实机探针”替代 |
| ④ 集成 + 门禁 | 主控 | — | §6.1 四门 + G1–G5，见 §6 |
| ⑤ 预审 | — | — | **跳过**（§5.2：文档型切片 ⑤ 可选） |
| ⑥ ③.5 扫描 | — | — | **跳过**，理由见 §7.1 |
| ⑦ 独立终审 | 独立审查员 | `independent-reviewer` + `GPT-5.3-Codex (copilot)` | §7.2 |
| ⑧ 文档 | 主控（自举） | 会话本体 | 本报告 + 逆向回写 |
| ⑨ 原生验证 | 主控 | — | **退化**：编码门禁 + G1–G5 + 引用可解析性 |
| ⑩ 停点 | — | — | 等用户授权提交 |

### 2.2 SW-1 实际改写清单

| 文件 | 改动 |
|---|---|
| `docs/t027/REMAINING_SLICES{,_EN}.md` | 闭环纪律段改指 §5.3（原按 v3.1 的 ①–⑥ 步骤编号，与新流水线编号不兼容，故不复述）；观察项 `§3.10`→`§7.2`、`④`→`⑦` |
| `docs/DEV_PLAN{,_EN}.md` | §1 补新准则指针（`git grep` 实测原为 0 条命中） |
| `docs/DOCUMENTATION_PLAN{,_EN}.md` | 权威分域表新增“交付执行纪律”行 |
| `.github/copilot-instructions.md` | “定位与权威”段加指针 |
| `docs/t027/FLASH_OPERATING_DIRECTIVE.md`、`_EN.md`、`_v3.1.md`、`_v3.1_EN.md` | 文件头加 supersede 指针（正文未动，+1/-0） |
| `docs/t027/FLASH_T027_BRIEFING.md`、`_v3.1.md` | 文件头加 supersede 指针（+1/-0） |
| `docs/t027/FLASH_OPERATING_DIRECTIVE_v3.1.html` | 顶部加历史版本横幅（原表未列 `.html`） |
| `docs/t027/A6_PILOT_LAUNCH.md` | 文件头加历史稿指针（**超出原表**；实测 4 条命中） |
| `docs/validation/B2-EXTERNAL-ENFORCEMENT{,_EN}.md` | “关联文档”行补新准则并标注 v3.1 已被取代（**超出原表**） |

## 3. 异常与兜底记录

### 3.1 自举豁免

`SW-1`/`SW-2` 的执行角色写的是文档工程师（⑧a），但**产品层载体正是 `SW-2` 要创建的对象** ⇒ 自举依赖。故由主控直接产出（§3.2 的“不原创产品工件”在此不适用），但**不豁免** ⑦。

### 3.2 A-1 主控误伤 11 个生成型角色文件（已按字节还原）

主控在“变更集编码归一化”时**误把作用域写成整棵脏树**，因而改动了 11 个既有生成型 `.agent.md`（no-BOM+CRLF → BOM+LF）。**当场发现并按字节还原**，逐个核对字节数：**11/11 `RESTORED`**（576 / 603 / 576 / 580 / 601 / 629 / 528 / 527 / 591 / 4616 / 565）。

- **根因**：门禁作用域未在实现前写明（§6.3 G4a 原表述未区分“变更集 vs 脏树”）。
- **已修**：§6.3 G4a 明写「**作用域 = 本次待提交的变更集**（≠ 整棵脏树）」。
- **兜底计数**：1（见 §8）。

### 3.3 A-2 终端假中断

本次会话主控有 2 条终端命令回显 `^C` 但未执行；探针子代理报告 `Remove-Item -Force` 连续 2 次被中断、改用 `[System.IO.File]::Delete()` 成功。**已登记** §9.3“终端假中断”（该行在准则定稿前已存在，本次为其第 2 次实测复现）。

### 3.4 A-3 准则自指缺陷（3 项，已就修）

G2 / G3 / G4b 三条判据在**本片首次真实执行**时全部暴露为不可满足 ⇒ OB-2 / OB-3 / OB-5。详见 §7.1 与准则 §12.4。

### 3.5 A-4 写文件工具产出 CRLF（新故障模式）

探针实测：新建文件经写文件工具落盘后为 CRLF（31 B 含 `0d 0a`）。**已按 R9.1 追加**至准则 §9.3。

## 4. 环境与档位

- 主控：会话本体 `DeepSeek V4.1 Flash`（档位由 operator 在 UI 侧设定；**本报告未能取得档位记录 ⇒ 标注“历史缺失”**，符合 §2.3“模型与档位自报不可信”）。
- 探针子代理：`agentName='prfrail-implementer'` + `model='DeepSeek V4.1 Flash (deepseek)'`（**显式传 model**，R2.1）。
- ⑦ 终审子代理：`agentName='independent-reviewer'` + `model='GPT-5.3-Codex (copilot)'`。

## 5. 可证伪性

| # | 验收断言 | 机械命令 | 结果 |
|---|---|---|---|
| 1 | 新角色文件未被忽略 | `git check-ignore -v .github/agents/prfrail-implementer.agent.md` | **无输出** ✅ |
| 2 | 生成器仍被忽略 | `git check-ignore -v .github/agents/sol-orchestrator/_config.yaml` | 命中 `.gitignore:18` ✅ |
| 3 | 产物仍被忽略 | `git check-ignore -v .github/agents/sol-orchestrator.managed.json` | 命中 `.gitignore:19` ✅ |
| 4 | 既有角色文件未被忽略 | `git check-ignore -v .github/agents/independent-reviewer.agent.md` | **无输出** ✅ |
| 5 | 角色文件被版本控制命中 | `git status --short -uall` | 3 个 `prfrail-*` 显示为 `??`（新增）✅ |
| 6 | **载体真能写文件**（SW-2 核心） | **四探针矩阵**（⑦ 首轮 F6 + 第 2 轮 R6） | 三载体各跑一次“创建→读回→删除”均绿；**第 4 探针**专测 `prfrail-documenter` 写 `.md`：保真后 55 B、`EF BB BF` 开头、`0D=0`、`0A=2`、CJK 解码无乱码；`tmp/` 前后条目全等，**残留 0** ✅ |
| 7 | 引用改写无遗漏 | `git grep -n "FLASH_OPERATING_DIRECTIVE\|FLASH_T027_BRIEFING" -- docs` | 命中 **21 行 / 8 文件**，**未带 supersede 说明 = 0** ✅ |
| 8 | 双语改动对称 | `git diff --numstat` 逐对比较 | **6 对全 PASS**（`+1/-0` ×3、`+2/-0` ×1、`+2/-2` ×1、`+1/-1` ×1）✅ |
| 9 | 旧**节号**无遗漏（F7） | `git grep -n "3\.10\|3\.11\|第五章\|Section 5" -- docs` | 命中 **120 行 / 24 文件**；逐文件分类见下方记录，**无未分类项** ✅ |

**F7 节号级断言的分类记录**：120 行命中分布于 24 个文件，全部已分类：

- **变更集内（已处理）**：`docs/t027/A6_PILOT_LAUNCH.md`（文件头指针）+ 8 个旧准则/启动语文件（C.0a 白名单 + 文件头指针）。
- **变更集外（已冻结历史记录，按 SW-1 边界“不扩大改写范围到历史验证报告”处理）**：16 个文件，均在 `docs/validation/`（历史验证报告 8 个×双语、`evidence/b3*/` 证据 6 个、`s0-spikes` 双语）。

**反证（“删掉守卫会不会变红”）**：

- 断言 1/2 的守卫 = `.gitignore` 的两条规则；把 `.github/agents/` 还原为目录级屏蔽 ⇒ 断言 1 由“无输出”变为命中（**会变红**）⇒ 断言有效。
- 断言 6 的守卫 = 载体的 `edit` + `execute` 权限；本片用**三载体全覆盖**证明该权限**确实存在**（对照：`quick-verifier` 经实测 `CAN_EDIT=no`，无该权限时无法完成探针）。

## 6. 门禁结果

### 6.1 基础门禁（§6.1）

| 门禁 | 命令 | 结果 |
|---|---|---|
| 格式 | `gofmt -l .` | 输出为空 `[]` ✅ |
| 构建 | `go build ./...` | exit 0 ✅ |
| 静态检查 | `go vet ./...` | exit 0 ✅ |
| 单元测试 | `go test ./...` | exit 0；`ok=14`、`FAIL=0`、无测试文件包 `4` ✅ |
| 契约夹具 | — | **N/A**：本片未触碰 `schemas/` 与 fixtures |
| 编码 | 逐文件字节核验 | 30 个变更文件全部符合（`.md` = BOM+LF；`.html`/`.gitignore`/`.txt` = 无 BOM+LF）✅ |

### 6.2 收尾自检 G1–G5

| # | 检查 | 结果 |
|---|---|---|
| G1 | 状态标记一致性 | **PASS**：准则附录 C.0 ✅ / C.0b ✅ 与 §0.3 变更日志一致；`REMAINING_SLICES` 无 `SW-*` 行 ⇒ N/A（SW 切片台账在准则附录 C） |
| G2 | 占位符残留 | **PASS**（修正后 = 0）。**首次执行得 4 条假阳性**，全部来自 G2 规则行自身 ⇒ OB-2 |
| G3 | 双语对称 | **PASS**（6/6 + 6/6）。**首次按原判据得 3 条 DIFF**，经查为**既有历史不对称**（501/488、270/315）⇒ OB-5；已加 (b) 关键字段对齐（F8）。**(b) 的作用域已再收紧为“变更行”**：全文比对在 `B2-EXTERNAL-ENFORCEMENT` 上误报 10/9（历史遗留 `✅`），改为 `git diff -U0` 新增行后 **7 对全 0/0 PASS** |
| G4 | 编码 + 异常字 | **PASS**。(a) 30 个变更文件 BOM/EOL 全绿；(b) 差集 10 字（飞/咨/肉/忘/百/摸/纠/诱/横/幅）**逐字复核全为合法新词**，已入白名单 ⇒ 差集 ⊖ 白名单 = **0** |
| G5 | 变更集契约 | 见 §10（提交前用 `git add <具体文件>` 逐个暂存；**禁止 `-A`**）；另执行 **G5-a**：`grep -l "^model:" .github/agents/prfrail-*.agent.md` **无输出**（无输出 = PASS） |

**R6.2 触发记录**：G2/G3/G4b 首跑不过 ⇒ **已停止提交**，修判据 / 登记观察项后重跑全绿。

## 7. 审查

### 7.1 ⑤ 与 ⑥ 的取舍（含一次被 ⑦ 纠正的误判）

- ⑤ **预审**：§5.2 明确“简单切片 ⑤ 可选”，且本片**无行为变更**，预审的“并发/崩溃/边界反例”面为空 ⇒ 跳过（⑦ 第 2 轮未反对）。
- ⑥ **③.5 扫描**：**主控最初以“纯文档切片”为由跳过，该判断被 ⑦ 第 2 轮判为不成立**——本片虽只改 `.md` / `.gitignore` / `.txt`，但**修订了 §6.3 门禁判据与证据模型**，触发 §7.2 的 ③ ⇒ **不得跳过**。**已补跑 ⑥**（结果见 §7.2b）。
- **⑦ 不可省**（R5.3），且本片触碰了**准则自身**（自指修订）⇒ 必须走外部独立审查。

### 7.2 ⑦ 独立终审（第 1 轮：盲审，首轮不附清单）

**结论行**：`FINDINGS`（3 High + 5 Medium）。**我方逐条复核：8 条全部成立并已整改**（处置见下表）；另有 4 项被审查方明确判定“已核查、非问题”。

| # | 严 | 发现（摘要） | 处置 |
|---|---|---|---|
| F1 | High | §2.2 “载体尚未建立” 与 C.0b “SW-2 已✅” 互斥 | 已改：§2.2 改为“七个载体全部就绪”，仅留 SW-3 待授权 |
| F2 | High | SW-1 触发（需 `_EN` 镜像）与状态（已✅）冲突 | 已改：明写执行期口径（CN 冻结即可执行，`_EN` 镜像列为后续切片） |
| F3 | High | 角色文件缺 `model` 键，与 §2.6 不一致；建议补空值键 | **实测推翻修法方向**：`model: ""` 使载体**完全无法加载**（隔离验证）⇒ 改为**修准则**：§2.6 规定键**必须省略** → OB-6 |
| F4 | Medium | SW-1 验收断言“每条命中”与“历史稿只加文件头”冲突 | 已改：改为**文件级 + 节号级双断言**，并新增 **C.0a 历史保留白名单** |
| F5 | Medium | §6.3 未声明脚本化状态，“机械检查”易被误读为已全自动 | 已改：§6.3 新增“脚本化状态（仅白名单已落盘）”段 |
| F6 | Medium | 可写探针只覆盖 1 个载体 | 已改：升为**三载体全覆盖探针矩阵**，3/3 通过（C.0b 验收证据 ③） |
| F7 | Medium | 引用断言只搜旧文件名，旧**节号**可能漏检 | 已改：C.0 断言增节号关键词检索；本片实检 120 行/24 文件并完成分类（§5） |
| F8 | Medium | 双语对称只比行数，语义漂移仍可通过 | 已改：G3 增 (b) **关键字段对齐**（提交号 / run 号 / 状态枚举 / 结论枚举） |

**审查输入包**：密封变更集 `tmp/sw-blind-review.diff`（93474 B；**剔除**本报告以保证盲审隔离）+ 仓库只读访问。**盲审隔离证明**：首轮**未附**任何我方清单；未提供 ⑤/⑥ 结论。

### 7.2b ⑥ ③.5 独立扫描（补跑；共 3 轮，用满上限）

| 轮 | 结论行 | 发现 | 处置 |
|---|---|---|---|
| 1 | `INDEPENDENT SCAN: FINDINGS` | 1 Medium：§12.4 的 OB-6/OB-7 被拼成一行（与 ⑦ 第 2 轮 R3 **独立重叠**）⇒ OB-7 | 已修（拆行）+ 新增 **G1-a** 表格结构完整性判据 |
| 2 | `INDEPENDENT SCAN: FINDINGS` | 1 Medium：文件头版本 v1.3 与 §0.3 日志 v1.4 **不同步** | 已修（头部升 v1.4）+ 新增 **G1-b** 版本元数据一致性判据 |
| 3 | `INDEPENDENT SCAN: PASS` | 0 | — |

- 输入：`tmp/sw-mai-rerun.diff` + 只读仓库；**三轮均不附任何清单**（含整改后重跑）。
- **计量**：⑥ 用满 §7.2 上限 **3** 次（首轮 + 2 次整改后重跑）。
- **OB-1 数据点**：三轮 Section A–E 输出**格式全部合规**、Section D/E 均有实质内容 ⇒ **“文档类任务必偏离格式”的归因进一步被削弱**。
- **证据清理**：三个密封包（`tmp/sw-*.diff`）属 `tmp/` 临时证据，按 §1.5 **用完已删除**（不随提交入库；本报告只保留命令与统计值）。

### 7.3 第 2 轮复审（附整改清单，按 §7.3“复审轮可附清单”规则）

**结论行**：`FINDINGS`（2 High + 5 Medium + 1 Low）。**我方逐条复核：8 条全部成立并已整改**。

| # | 严 | 发现（摘要） | 处置 |
|---|---|---|---|
| R1 | High | 报告宣称 `COMPLETE` 却写“复审结论待回填”，且已把第 2 轮计入调用次数 | 已改：本节回填第 2 轮结论 ⇒ 状态与计量自洽 |
| R2 | High | ⑥ 跳过理由不成立（本片改了 `.gitignore`/`.txt`，且**修订了门禁与证据模型**） | 已改：**补跑 ⑥**（§7.2b）；并**收紧 §7.2 判据** + 新增**反面样例** |
| R3 | Medium | v1.1 与 v1.2 变更日志被拼成同一表行（结构破坏） | 已改：拆为两条独立表行（**⑥ 亦独立报出同一条**） |
| R4 | Medium | C.0b 仍写“当前不存在”与“已 ✅”并存（时态未收口） | 已改：改为“**在 SW-2 立项时**不存在” |
| R5 | Medium | 报告把自举豁免锚到 §3.1，准则实际锚点是附录 C.2 / §3.2 | 已改：双语报告的锚点与豁免对象已更正 |
| R6 | Medium | 三载体探针只建 `.txt`，未验证 `documenter` 对 `.md` 的 BOM+LF 保真 | 已改：新增**第 4 探针**（`.md` 保真）；实测工具链**对 `.md` 同样产 无 BOM+CRLF** ⇒ 已补记 §9.3 |
| R7 | Medium | F3 的防线仅靠人工预检，无脚本化硬闸 | 已改：新增 **G5-a**（`grep -l "^model:"` 必须无输出），并列入脚本化必检项 |
| R8 | Low | 断言 8 的计数分解（×4）与“6 对”不符 | 已改：更正为 `+1/-0`×3、`+2/-0`×1、`+2/-2`×1、`+1/-1`×1 |

**附带自查发现**：准则三处引用 `Section A–E`（§4.2 / §7.2 / B.6）却**从未定义**（v3.1 原有定义未继承）⇒ 已按继承来源补齐定义（v1.3）。

**§7.5 上限状态**：⑦ 已用 2 次（终审 + 复审），**正好用满**；§5.3 要求“整改后重跑 ⑦ 直至无 Medium+”，若再跑第 3 轮则**超出 §7.5 上限** ⇒ 按 §7.5『超出上限 ⇒ 升级用户裁决』**提交用户裁决**（见 §11）。

## 8. 成本与计量（R7.1）

| role | model | stage | startedAt | reason | retryOf |
|---|---|---|---|---|---|
| 主控 | `DeepSeek V4.1 Flash (deepseek)` | ②④⑧⑨ | 会话全程 | 自举产出 + 门禁 + 报告 | — |
| 探针 | `DeepSeek V4.1 Flash (deepseek)` | ②（SW-2 探针） | 2026-09-21 | 验证载体写权限（验收证据 ③） | — |
| 独立审查 | `GPT-5.3-Codex (copilot)` | ⑦ | 2026-09-21 | 独立终审（首轮盲审） | — |
| 独立扫描 | `MAI-Code-1.1-Flash (copilot)` | ⑥ | 2026-09-21 | 补跑 ③.5 独立扫描（⑤⑥ 误判纠正） | — |

- **兜底次数 = 1**（A-1；阈值 3，未触发 §9.2 高风险标记）。
- **⑥ 调用次数**：**3**（补跑首轮 + 2 次整改后重跑），**已用满**上限 3（§7.2）。
- **⑦ 调用次数**：**3**（第 1 轮盲审终审 + 第 2 轮复审 + **第 3 轮（例外授权）**）。§7.5 的 ⑦ 上限为 2 ⇒ 第 3 轮属**例外**；“必抽盲审 +1”未触发（本片不触及所有权/停机/身份语义），且 §7.3 规定 ⑦ 首轮本就不附清单，故首轮盲审不额外计费。
- **`⑦ 第 3 轮为例外授权：§5.3 与 §7.5 冲突，经用户裁决按闭环优先；本次为一次性突破，不构成新上限`**（用户 2026-09-21 裁决；该优先关系**不写入准则**，以免被当作新上限）。
- **付费调用上限**：Codex **3 次**（§7.5 终审 + 复审 + 经授权的第 3 轮）；MAI **3 次**（⑥ 补跑 + 2 次整改后重跑，用满 §7.2 上限）；V4 Pro 0 次（跳过 ①⑤）。
- **历史缺失**：主控档位、token 计数未记录（§2.3：自报不可信）。

## 9. 明确未执行事项

1. **`SW-3`（角色集入库与编码归一）**：11 个既有生成型角色文件仍为 no-BOM+CRLF 且未入库 ⇒ **待授权**（两方案见准则附录 C.0c）。
2. **`docs/DELIVERY_DIRECTIVE_EN.md` 未生成**：§11.1 要求 CN 冻结后出镜像；本片仅在已存在的 `_EN` 文档中改引用。
3. **`tools/gates/` 脚本未实现**：G1–G5 本片由主控**手工执行**；除白名单文件外未落脚本（准则 §12.5 已列缺口）。
4. **未跑试点切片**（§12.1 要求的量化指标需在试点采集）。
5. **未推送、未观察 CI**（等授权）。
6. **`ADR_REGISTER` 未回写**：本片未产生新裁决（准则切换属工程纪律，非 ADR 级决策）⇒ N/A。

## 10. 提交与 CI

- 暂存：`git add <具体文件>` 逐个（**禁止 `-A`**）；G5 三集合一致见提交记录。
- 推送：**仅 `origin`**（gitee 不动）。
- CI：推送后观察并**双绿回报**（§6.4）；首跑红即按 §6.4 四步处置并登记 DR-N。
- **CI 实测（2026-09-21）**：本片与 SW-3 一并推送 head `52c28af` → run **`35606581371`**：**首跑 Windows 腿 `Test` 步红**（`tools/agent-probe/enforcement-proxy` · `TestConnectUsesUpstreamProxy`，`main_test.go:450: target accepts = 0, want 1`；Ubuntu 腿全绿、其余 14 包全 `ok`）；`gh run rerun --failed` → **attempt 2 双腿全绿**、终态 `success`。**不得以“复跑绿了”掩盖首跑红** ⇒ 已按 §6.4 登记为 **DR-4**（与 DR-2/DR-3 均不同测试）；该片无 `.go` 改动 ⇒ 与本片改动**无因果关联**。

## 11. 下一步建议

1. **裁决 `SW-3`**（推荐方案 = 归一化 11 个文件后入库；更保守方案 = 把 `.gitignore` 例外收窄为 `prfrail-*.agent.md`）。
2. **生成 `DELIVERY_DIRECTIVE_EN.md` 镜像**（CN 已冻结；之后 `_EN` 引用可改为指向镜像）。
3. **实现 `tools/gates/` G1–G5 脚本**（含本片修正后的 G2 / G3 / G4b 口径 + 白名单）。
4. **跑试点切片 = DR-2 + DR-3 修复**，按 §12.1 采集 5 项指标，并作 **OB-1** 的代码切片对照。
5. 之后回填 `docs/validation/` 的 DR-2/DR-3 报告与台账 B4 前置状态。
