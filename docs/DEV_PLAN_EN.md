# ProofRail Iterative Development Plan

> T026 completed on 2026-09-11 with **ten verified, two unsupported, zero unknown, blocked**. Seven of ten additionally authorized invocations were used, confirming six premium requests plus unknown cancellation cost; three remain unused. Job Object cancellation passed, while tool-alias and shell-network allow-all controls proved CLI-denial bypasses, making the candidate incompatible. T027/AT-23 still requires an external enforcement boundary or compatible candidate; do not add model calls automatically. See the [final T026 conclusion](validation/t026-agent-runner-contract_EN.md).

[简体中文](DEV_PLAN.md)

Date: 2026-09-08; S1 implementation remains incomplete. Inputs: [requirements](PRODUCT_REQUIREMENTS_EN.md), [architecture](ARCHITECTURE_EN.md), [contracts](CONTRACTS_EN.md), [security](SECURITY_EN.md), [ADRs](ADR_REGISTER_EN.md). This follows revised RFC sections 14/19; added S1 minimum scope requires renewed budget approval, without pulling S2/S3 enhancements into the first release.

## 1. Workflow and Constraints

Go 1.22 module compatibility, pure Go, CLI/TUI and content-addressed files; dependencies freeze in S0. No framework migration applies, so Java/.NET transformation rules are inapplicable. Read-only source, single writer, first-fail-stop, independent approval and no default Git writes have no architectural exceptions.

Handle one task per session. Split anything exceeding one verifiable change into subtests instead of generating every module. Red test, minimal implementation, same test, package tests, full gates, bilingual docs/evidence. Offline deterministic checks precede authorized model calls. Estimate dates after measuring the first two tasks, not from unmeasured assumptions.

WIP=1. Agree time/cost caps per session; no paid calls by default. Two same-class failures without new evidence pause/escalate. Reserve stronger models for contract/security ambiguity; cheaper models execute frozen small slices. Git commit/push still require same-turn authorization, origin only by default.

Delivery execution discipline (slice model, role and model allow-list, serial pipeline, gates and evidence, review system, cost caps) is owned by the [Delivery directive](DELIVERY_DIRECTIVE.md); the tasks in this plan execute under it. **As of 2026-09-21 the retired T027 directives `docs/t027/FLASH_OPERATING_DIRECTIVE*.md` and `docs/t027/FLASH_T027_BRIEFING*.md` are superseded** (historical reference only; map section numbers via the new directive's Appendix C.1).

## 2. Iterations and Tasks

Paths not yet present are planned creation locations. T001-T004 are S0; T004 is isolated experimentation, not S1 production code. Every RFC section 17 gate and owner approval must precede T005.

### P0.1 Documents and Approval (REQ-001/014/017/023/026/028)

- [x] T001 [Plan:P0.1] Review docs/PRODUCT_REQUIREMENTS*.md, docs/SECURITY*.md and docs/ADR_REGISTER*.md; record owner/security reviewer, budgets, support/signing and shadow scope. Depends: this package. Accept: section 17.1/6/8 sign-off and license decisions. Exclude: approving for the user. Completed 2026-09-06: owner approved S0 specification freeze and ADR section 4; independent security conclusions remain a separate T003/gate 17.3 closure.

T001 status: `COMPLETE`. This approves only S0 specification freeze and allows T002 to begin. It does not authorize new paid calls, concrete whois data access, Git operations, release or S1 production coding. Exact dependencies and independent security review remain later gates; the simplified release-trust policy approved on 2026-09-08 does not require an S1 signer or public key.

### P0.2 Machine Contract Freeze (REQ-001/004/005/007/008/010/012/013/017/018/020/021/022/024/025/028)

- [x] T002 [Plan:P0.2] Revise docs/RFC-proofrail-unattended-ai-engineering-product.md first, synchronize docs/CONTRACTS*.md/ADRs, create schemas/*.schema.json; freeze ADR-002/004 fields, versions, canonical vectors, transitions, errors and queue. Depends: T001. Accept: structural/local consistency checks complete; T003 owns independent fixtures/validator. Exclude: production code using guessed fields.
- [x] T003 [Plan:P0.2] Create testdata/contracts/{valid,invalid}/ and tools/contracts/ independent validator/semantic fixture oracle: three tasks, four kinds, dual/triple language, handoff, docs/waiver and all receipts/manifests. Depends: T002. Accept: AT-01 and section 17.2/4/9-12. Exclude: candidate-generated expected results. Completed 2026-09-07: Ajv 2020 independently compiled all 29 schemas; all 82 current hand-authored fixtures pass, and `canonicalize` independently reproduced both JCS/digest vectors.

T002/T003 status: `COMPLETE`. Common version, ID, path, JCS and UTF-8 rules are frozen, with twenty-nine Draft 2020-12
structural schemas for chain, hook, target, workspace, state-event, error, adapter-envelope, adapter-receipt,
snapshot-manifest, evidence-manifest, review-receipt, promotion-receipt, handoff-receipt, hook-result, run-manifest, signature-receipt, error-set, ticket-ledger, repair-transaction, harness, toolchain, change-set, verification-report, plan-preview, export-record, authorization-record, effect-record, cost-ledger and lifecycle-record. Initial canonical vectors,
single-record transitions, 37 error codes/primary ordering and CLI exits, file-queue framing/claim/result/dispatch/takeover receipt rules,
snapshot boundaries, the pre-review evidence root, the approve/reject/waive review verdict, the atomic promotion reader
contract, the manual-handoff step discriminator with its return receipt, the hook execution/gate/disposition split, effective run configuration origins/bindings, harness/toolchain registries, the detached Ed25519 signature receipt, failure-fingerprint/ticket-ledger/three-phase repair budget, four-stage repair transaction, deterministic configuration merge, change-set, cross-event/cross-record/trust-chain report and PC-01–PC-06 records are frozen. T003's independent structural validator, static semantic checker, vector fixtures and positive/negative goldens have run; the S0 contract portion of AT-01 and RFC section 17.2/4/9-13 now has machine evidence. Real files, processes, capabilities and platform facts remain T004/S1 runtime gates, so overall S0 remains `NOT_READY`.

T001 also reviews ADR-008 and PC-01–PC-06 workload/budgets. T002 freezes RFC section 19 authorization/preview/export/effect/cost/backup records; T003 has added independent positive/negative goldens for gate 17.13. This freezes contracts, not S1 runtime acceptance.

### P0.3 Technical Spikes (REQ-006/007/009/015/017/021/023/024/027)

- [x] T004 [Plan:P0.3] Probe Go/TUI, Windows atomic replacement/process-tree stop, Linux paths and IPC/file queue in root tmp; record docs/validation/s0-spikes_EN.md and delete experiments. Depends: T002; S1 also requires all T003/T001 gates. Accept: reproducible commands, actual platforms and explicit unavailable capabilities. Exclude: unauthorized paid calls or production claims. Completed 2026-09-07: native Windows 11/NTFS and Ubuntu 24.04/ext4 probes, an isolated Bubble Tea v1.3.4 prototype, and 40 no-model SessionBridge tests passed; see the report for limitations and fail-closed rules.

T004 status: `COMPLETE`. Technical feasibility is established, but overall S0 remains `NOT_READY`; dependency supply chain, bootstrap, CI release evidence and production crash injection remain T017/later gates. This result does not authorize T005.

### P1.1 Evidence and Snapshots (REQ-004/023/024/025/028)

- [x] T005 [Plan:P1.1] Implement internal/evidence/{canonical,event,receipt,verify}.go and matching _test.go for canonical/event chains/references/read-only verification. Depends: all S0 gates. Accept: the evidence portion of AT-04. Exclude: free-text PASS or unknown-version writes. Completed 2026-09-07: frozen vectors, strict JSON/JCS, projected event chains, evidence manifests, and content-store reread verification passed; see `docs/validation/t005-evidence_EN.md`.

T005 status: `COMPLETE`. This covers only the evidence-layer subset of AT-04. Journal, write transaction, rollback, and crash-point acceptance remain T008 work and cannot be inferred as passed here.

- [x] T006 [Plan:P1.1] Implement internal/snapshot/{capture,restore,store,types,reparse_*}.go/tests for paths/exclusions/quotas/retention/GC dry-run. Depends: T005. Accept: AT-02. Exclude: Git-history recovery or referenced-object deletion. Completed 2026-09-07: uncommitted file tree capture, CAS object store, reserved/case/long-path/link checks, concurrent modification validation, quota limits, Git-free restore, and Windows/Ubuntu dual-platform validation passed; see `docs/validation/t006-snapshot_EN.md`.

T006 status: `COMPLETE`. Full AT-02 acceptance passed; Git-free restore and immutable snapshot baseline are closed; workspace write transactions and crash rollback points belong to T008.

### DG-01 Independent Product Documentation Gate

- [x] DG-01 Establish the independent ProofRail product boundary; add `BUSINESS_WORKFLOWS*.md` and `INSTALLATION_PLAN*.md`; synchronize the RFC, requirements, index, README, and operations docs. Completed 2026-09-07. Artifact, location, signer, and upgrade choices remain open; no fictional installation instructions are introduced. Stable T IDs remain unchanged; DG-01 gates T009 without consuming an implementation task number.

### DG-02 Domain Authority Migration Gate

- [x] DG-02 Reclassify the former RFC as the project proposal and historical design source; establish the domain authority matrix in `DOCUMENTATION_PLAN*.md`; and migrate the chain/task/step transitions, event projection, ordered scheduling, pause/cancel, and crash recovery required by T009 into `CONTRACTS*.md` section 2.1. Completed 2026-09-07. Old sections remain for provenance; conflicts block implementation and require correction rather than implementer choice.

### P1.2 Writes and Ordered Loop (REQ-002/003/004/012/018/023/024/026)

- [x] T007 [Plan:P1.2] Implement internal/guard/{process,lease}.go, platform files/tests for tree termination, takeover and leases. Depends: T005/T004. Accept: AT-03/06. Exclude: killing user processes or PID/expiry-only takeover. Validation: [中文](validation/t007-process-lease.md) / [English](validation/t007-process-lease_EN.md).
- [x] T008 [Plan:P1.2] Implement internal/taskdef/checker.go and internal/applier/{apply,journal,recover}.go/tests: sequential memory validation, transaction and rollback. Depends: T005/T006/T007. Accept: AT-04 at every crash point. Exclude: validate-while-writing or omitted assertions. Validation: [中文](validation/t008-applier.md) / [English](validation/t008-applier_EN.md).
- [x] T009 [Plan:P1.2] Implement internal/chain/{engine,state,recover}.go/tests: three tasks, four kinds, both change modes, projection/pause/cancel, initially fake agent/runner. Depends: T008, DG-01, and DG-02. Accept: AT-03. Exclude: candidates as downstream parents. Completed 2026-09-07. All 14 slices and native Windows 11/NTFS plus Ubuntu 24.04/ext4 validation passed. Validation: [中文](validation/t009-chain.md) / [English](validation/t009-chain_EN.md).
- [x] T010 [Plan:P1.2] Implement internal/gates/{runner,policy,result}.go/tests and wire chain. Depends: T007/T009. Accept: AT-06. Exclude: weakening unavailable network/resource enforcement. Completed 2026-09-07. All 12 slices and native Windows 11/NTFS plus Ubuntu 24.04/ext4 validation passed; unavailable network/resource enforcement remains blocked at preflight. Validation: [中文](validation/t010-gates.md) / [English](validation/t010-gates_EN.md).

### P1.3 Independent Acceptance and Bounded Repair (REQ-005/014/022/023/024/025/026)

- [x] T011 [Plan:P1.3] Implement internal/chain/{review,publish}.go/tests, bound candidate/review/receipt, rejection and valid waiver. Depends: T010. Accept: AT-05. Exclude: agent self-approval or technical-success PASSED. Completed 2026-09-07. Coverage includes refusal/rejection, self-approval blocking, expired/wrong-candidate waiver blocking, and exactly-once publish after valid independent approval. Validation: [中文](validation/t011-chain.md) / [English](validation/t011-chain_EN.md).
- [x] T012 [Plan:P1.3] Implement internal/tickets/{ledger,budget,fingerprint}.go and internal/repair/transaction.go/tests. Depends: T011. Accept: AT-07. Exclude: budget resets or direct formal-definition repair. Completed 2026-09-07. Coverage includes same-fingerprint exhaustion, attempt/wall-clock/cost exhaustion, fake repair blocking, stale candidate blocking, and interrupted Promote handling. Validation: [中文](validation/t012-tickets-repair.md) / [English](validation/t012-tickets-repair_EN.md).

### P1.4 Agents and Handoff (REQ-006/007/008/016/021/023/024/025/026)

- [x] T013 [Plan:P1.4] Implement internal/adapters/{filequeue,sessbridge}.go/tests with consumer-owned ports. Depends: T012. Accept: AT-08 offline then authorized host. Exclude: SessionBridge edits, GUI/auto fallback or repeated application. Completed 2026-09-07. Coverage includes request/claim/result envelopes, file-queue claim and fencing checks, idempotent conflict protection, and sessbridge silent dispatch mapping for accepted/rejected/uncertain outcomes. Validation: [中文](validation/t013-adapters.md) / [English](validation/t013-adapters_EN.md).
- [x] T014 [Plan:P1.4] Implement internal/chain/handoff.go and internal/console/handoff.go/tests. Depends: T013. Accept: AT-09/10. Exclude: model-mediated secrets, expiry-based success or concurrent automatic writers. Completed: 2026-09-07. Coverage includes handoffPolicy constraints, complete/abort/request-agent receipt semantics, WAITING_FOR_OPERATOR state transitions, and structured/secret-direct input policy enforcement with secret hashing. Supplemental validation: on 2026-09-08, Ubuntu VM remote full build/vet/test and race validation (gcc for full repository, clang for critical packages) passed through a temporary proxy chain. Validation: [中文](validation/t014-handoff.md) / [English](validation/t014-handoff_EN.md).

### P1.5 Harnesses and Usable CLI/TUI (REQ-009/010/011/013/015/016/018/020/022/027)

- [x] T015 [Plan:P1.5] Create harnesses/{generic,c,go}/, internal/taskdef/documentation.go/tests for template A, impact rules, freshness and C+Go tasks. Depends: T014. Accept: AT-11. Exclude: whois core terminology or arbitrary script B. Completed: 2026-09-08. Coverage includes documentation impact-rule enforcement, locked template A and generated freshness checks, blocking for self-approval and manual generated edits, and task-level recovery when any C+Go language scope fails. Validation: [中文](validation/t015-taskdef-documentation.md) / [English](validation/t015-taskdef-documentation_EN.md).
- [x] T016 [Plan:P1.5] Complete cmd/prfrail/main.go, internal/console/, config parsing/explanation/tests; sync README.md and docs/OPERATIONS*.md. Depends: T015. Accept: AT-12. Exclude: direct view-state writes or successful placeholder commands. Completed: 2026-09-08. Coverage includes no-IDE `init/validate/config explain/run/report`, stable `--json` output, config search order (`--chain` → `proofrail.chain.json` → `proofrail.json`), a noop-only local run/report loop, and fail-closed non-zero exits for executable steps. Validation: [中文](validation/t016-cli-console.md) / [English](validation/t016-cli-console_EN.md).

### P1.6 Trusted Release and Shadow Acceptance (REQ-001/002/006/011/014/015/019/024/025/028)

- [x] T017 [Plan:P1.6] Create .github/workflows/ci.yml, testdata/selfhost/ and docs/validation/s1-selfhost.md; pin tooling and Actions by full SHA, isolate generations, use an earlier trusted verifier commit and external oracle, and verify native platforms, a fixed candidate commit, SHA256SUMS, SBOM and licenses. Depends: T024 and bootstrap approval. Accept: AT-13/14. Exclude: candidate-built/overwritten verifier or seed, checksum-as-identity claims, or unauthorized release. Signing/attestation is a later enhancement and does not block S1. Completed: 2026-09-09. After baseline `12fa05d` failed on both Windows and Linux in push CI run `34253473081`, the remediation received `PASS FOR BASELINE REVIEW` from both DeepSeek V4 Pro and GPT-5.3 Codex and established `18c9340c60deb30049554491f8905df51d0a3180` as the new verifier baseline. Both Ubuntu and Windows jobs succeeded in push CI [run `34268076912`](https://github.com/larsonzh/prfrail/actions/runs/34268076912). Maintainer dispatch from `main` then evaluated the distinct docs-only candidate `1e7af676e8e84028c7ecfef1ccde91213738c20c`; all eight test, candidate-build, candidate-probe, and selfhost Windows/Linux jobs succeeded in [run `34277671704`](https://github.com/larsonzh/prfrail/actions/runs/34277671704), with names, sizes, and GitHub archive digests for all six artifacts archived. Validation: [中文](validation/s1-selfhost.md) / [English](validation/s1-selfhost_EN.md); initial Codex review: [中文](validation/t017-baseline-review.md) / [English](validation/t017-baseline-review_EN.md); initial DeepSeek review: [中文](validation/t017-deepseek-baseline-review.md) / [English](validation/t017-deepseek-baseline-review_EN.md); CI-remediation DeepSeek review: [中文](validation/t017-ci-remediation-deepseek-review.md) / [English](validation/t017-ci-remediation-deepseek-review_EN.md); CI-remediation Codex review: [中文](validation/t017-ci-remediation-codex-review.md) / [English](validation/t017-ci-remediation-codex-review_EN.md).
- [ ] T018 [Plan:P1.6] Create examples/{whois-shadow,go-minimal}/ and docs/validation/s1-exit.md; compare fixed read-only input/result/failure classifications and summarize all AT/risks. Depends: T017, T025, T026, T027, T028, and whois input authorization. Accept: AT-15/22/23/24 and every RFC S1 exit gate. Exclude: production whois cutover or partial-pass completion.

T018 status: `IN_PROGRESS / BLOCKED`. On 2026-09-09, a redacted frozen whois fixture was exported under a one-session read-only authorization; all 9/9 parity cases and input/result/failure-classification drift counterexamples pass. All three minimal generic/C/Go CLI loops pass, as does a Windows candidate portable-package drill covering first run, upgrade, rollback, and evidence-preserving uninstall. The product owner approved manual portable-ZIP extraction without PATH modification, and bilingual installation, support-matrix, and release-note drafts now exist. T025/AT-22 completed on 2026-09-10. AT-01–AT-24 and remaining gates are summarized in the [S1 exit report](validation/s1-exit_EN.md). T026/T027/AT-23, T028/AT-24, final-ZIP binding/rerun, the EOL notification channel, and product-owner S1 exit approval remain absent, so T018 stays unchecked.

> **T018 open question (2026-09-22, measured by the B4 ①)**: the remaining T018 gates (AT-24 / final ZIP binding and re-verification / EOL notification / the product-owner S1-exit approval) need re-evaluation for "**do they also depend on candidate / AgentRunner assembly progress**" - if they do, T018 completion is affected by F1 / F2 as well. **Out of B4's scope**; same origin as **OB-21** and the same timing.

### P1.7 Product Flow and Lifecycle (Before P1.6 Release Acceptance)

Keep IDs stable; execution order is T016 → T019–T024 → T017 → T025 → T026 → T027 → T028 → T018. These run under S0 approval; T019–T024 are complete. Split tasks by individual negative tests rather than generating whole features at once.

- [x] T019 [Plan:P1.7] PC-01: implement internal/taskdef/preview.go, internal/console/preview.go, tests and no-AI example. Depends: T016. Accept: AT-16. Exclude: preview commands/network/models/automatic installation. Completed: 2026-09-08. Covers offline read-only preview record generation, visible unknowns, zero call counters, and JSON/text fact parity. Validation: [中文](validation/t019-preview.md) / [English](validation/t019-preview_EN.md).
- [x] T020 [Plan:P1.7] PC-02: implement internal/snapshot/export.go, internal/evidence/delivery.go/tests. Depends: T019, T006/T011. Accept: AT-17. Exclude: candidate-as-accepted export, target overwrite, source/Git writes. Completed: 2026-09-08. Covers accepted-hash binding, destination absent precondition, source/run/store overlap blocking, secret-path blocking, missing-object/tamper blocking, and interrupted writes without a completed export record. Validation: [中文](validation/t020-export.md) / [English](validation/t020-export_EN.md).
- [x] T021 [Plan:P1.7] PC-03: implement internal/chain/authorization.go, internal/console/approvals.go/tests, wire guard stopping. Depends: T020, T007/T011. Accept: AT-18. Exclude: self-authorization, hard-gate waiver, expiry-based approval. Completed: 2026-09-08. Covers authorization/revocation hash binding, revocation/expiry/hash-mismatch blocking for next actions and acceptance, and pause-preserving uncertain in-flight stops. Validation: [中文](validation/t021-approvals.md) / [English](validation/t021-approvals_EN.md).
- [x] T022 [Plan:P1.7] PC-04: implement internal/gates/effects.go, internal/evidence/diagnostics.go and chain recovery-plan tests. Depends: T021, T010. Accept: AT-19. Exclude: external-write runners, blind unknown-effect retries, diagnostic lock/log deletion. Completed: 2026-09-08. Covers effect classification/admission, fail-closed blocking for unknown/external writes, and read-only redacted diagnostics with recovery actions. Validation: [中文](validation/t022-effects-diagnostics.md) / [English](validation/t022-effects-diagnostics_EN.md).
- [x] T023 [Plan:P1.7] PC-05: implement internal/tickets/cost.go, internal/adapters/cost_usage.go, internal/console/cost.go, and tests. Depends: T022, T012/T013. Accept: AT-20. Exclude: restart-released unknown holds, cross-run cap bypass, default telemetry. Completed: 2026-09-08. Covers durable pre-call reservations, deduplicated settlements, restart-preserved unknown holds, shared cap blocking across runs, and local cost reporting with no default telemetry export. Validation: [中文](validation/t023-cost-ledger.md) / [English](validation/t023-cost-ledger_EN.md).
- [x] T024 [Plan:P1.7] PC-06: implement internal/snapshot/backup.go, internal/evidence/disposition.go, lifecycle controls/tests and release inventory tooling. Depends: T023. Accept: AT-21, SBOM/support documentation. Exclude: unknown-format migration, default evidence/shared-tool deletion, unauthorized signing/release. Completed: 2026-09-08. Covers complete object/event/reference-closure backup, new-store restore verification, unchanged source store, active-writer/unknown-version/incomplete-closure blocking, evidence/shared-tool retention by default, explicit deletion authorization/retention-conflict decisions, and offline revocation status `unknown`. Validation: [中文](validation/t024-lifecycle.md) / [English](validation/t024-lifecycle_EN.md).

### P1.8 AI Execution, Black-Box Candidates, and Operator Loop (REQ-006/007/008/009/016/021/023/024/025/026/027)

- [x] T025 [Plan:P1.8] Freeze the `operator-interaction` Schema, positive/negative goldens, hash rules, and replay contract; implement the TUI pending-interaction inbox/structured response, return of control, and restart recovery. Depends: T014, T016, T021, T024. Accept: AT-22. Exclude: free text causing state transitions, chat or Agent history as authority, dependency on `@sbr-review`, authorization/handoff bypass, or secrets entering model/evidence. Completed: 2026-09-10. Covers strict wire/hash validation, append-only JSONL replay, a focused ANSI-free inbox, structured responses, Engine single-writer open/resume, rejection of wrong binding/duplicates/tampering, and fail-closed convergence after partial event-write failure. The complete unified TUI and real Agent-session continuation remain later work. Validation: [中文](validation/t025-operator-interaction.md) / [English](validation/t025-operator-interaction_EN.md).
- [x] T026 [Plan:P1.8] Freeze `AgentRunner` request/capability/event/completion Schemas, canonical/hash rules, positive/negative goldens, dedup/replay, and adapter capability matrix. Pin at least one candidate CLI Agent version/digest and probe noninteractive use, cwd, events/complete logs, session create/resume, cancel/process-tree stop, tool/network/permission controls, usage, and confirmation behavior. Depends: T025. Accept: AT-23 contract/capability portion. Exclude: production adapter, unauthorized paid calls, or help text/prompt promises as runtime evidence. Status: `COMPLETE / CANDIDATE INCOMPATIBLE`. The four base Schemas, 96 contract cases, RFC 8785 domain-separated hashes, strict binding/replay-conflict checks, and digest-pinned GitHub Copilot CLI 1.0.83 probes are complete. The final matrix is ten verified, two unsupported, zero unknown: managed Job Object evidence verifies cancellation; allow-all controls prove that the `gci` alias bypasses tool denial and shell `Invoke-WebRequest` bypasses URL denial. Seven of ten additional authorizations were used, confirming six premium requests plus unknown cancellation cost; three were unused. The candidate remains blocked and AT-23 has not passed. A subsequent prerequisite slice freezes the `agent-runner-enforcement` Schema, binding/freshness/combined admission, and fail-closed tests, bringing the current contract gate to 35 Schemas and 98 cases; this does not mean a real external boundary exists. Validation: [中文](validation/t026-agent-runner-contract.md) / [English](validation/t026-agent-runner-contract_EN.md).
- [ ] T027 [Plan:P1.8] Implement the core-owned `AgentRunner` port, at least one pinned CLI Agent adapter, isolated-workspace start/stop/resume, event/log/usage/manifest/diff evidence, and post-exit independent gates/review. Depends: T026, T007, T010, T012. Accept: AT-23. Exclude: source/store/policy/acceptance writes, self commit/push/publish, exit-zero PASS, or blind unknown-state retry. Status: `BLOCKED / NOT IMPLEMENTED`. The first product slice targets `windows/amd64` only, while the port and wire remain platform-neutral. Before launch, normalize detected `os`/`arch` and select a platform strategy only after exact capability/platform/enforcement matching. After the Windows version is complete, develop and validate `linux/amd64` independently on an Ubuntu 24.04 VM. macOS, ARM, and other OS/architecture combinations retain extension points but have no schedule; later probes may use native environments or GitHub Actions runners. No platform inherits Windows capability conclusions. The external-enforcement wire and combined preflight are frozen, and B2 has since measured and archived an **external OS enforcement** boundary (a zero-network-capability AppContainer plus a host-side allowlist proxy; see the [validation report](validation/B2-EXTERNAL-ENFORCEMENT_EN.md)), but that boundary is **not yet wired into the product** (the in-container tool path and the AgentRunner integration are still unimplemented); the pinned candidate must not start as AgentRunner. A prerequisite slice freezes the three-channel `ai-availability` Schema, RFC 8785 digest, replay, and profile/channel/freshness/request-budget admission, bringing the unified contract gate to 36 Schemas and 100 cases. The non-secret ProviderProfile port, strict persistent configuration with redacted explain output, Windows Credential Manager backend, and no-echo `secret set/status/delete` CLI are implemented; tests prove static validate/explain/preview does not read the SecretStore. `agent-runner-cli` now has a managed-process live probe and an `ai check` entrypoint; offline tests cover exact reply, zero tools, one request, structured result/usage, environment allowlisting, credential clearing, and redacted evidence. Every verdict can be published as non-replacing canonical JSON through `--out`, then replayed by `ai verify` against the configuration digest, channel, freshness, and request budget without reading the SecretStore or invoking a model. No live model request was made, so there is no actual availability verdict. The Linux SecretStore and live silent/visible probes remain unimplemented, and T027 enforcement plus AgentRunner itself remain blocked.
- [ ] T028 [Plan:P1.8] Implement explicit `supervised-black-box`: versioned risk/responsibility notice with bound acknowledgment, isolated workspace, SessionBridge visible delivery, human supervision and explicit return, full post-return scan, independent gates/review, and reduced/unknown report. Depends: T025, T026, T014, T022. Accept: AT-24. Exclude: automatic AgentRunner downgrade, visible receipt as completion, risk acknowledgment waiving safety gates, or unknown cost/process/effects shown safe. Status: `PLANNED / NOT IMPLEMENTED`.

T027 prerequisite final update (2026-09-14): this paragraph supersedes both preceding 2026-09-13 prerequisite notes. The approved offline prerequisite slice is complete: the v1 mapping from `allowedEffects` to grant `effectClasses` is frozen and implemented, with both `local-process` and `workspace-write` mapping to `local-discardable`; requests bind the mapping version/hash, and Schema, canonical-vector, semantic-validator, and Go composite-admission checks fail closed for missing, unknown, or tampered bindings, unknown operations, and grant-class under-coverage before replay identity consumption or dispatch. Grant-class supersets may pass. `AgentRunnerReplayStore` now handles orphan completions/corruption, bounded reread convergence, and a single first-dispatch across concurrent stores sharing one root; other requests are blocked, orphan completions never heal a missing request, and unresolved convergence remains uncertain/fail-closed. Windows publication durability is `unproven` (**the level and its premise must be read together**: every level is one of `proven`/`disproven`/`unresolved`, under premise P = process-level crash, no power loss, single host Windows 11 / NTFS on the local volume, which is the premise this `unproven` holds under): any write that could return `first-dispatch` is blocked at the `pre-write` stage and creates no request record. The Unix proven path requires an already-existing stable absolute replay root and successful parent-directory `fsync`; this verifier ran on Windows, so it provides no native Unix crash-injection evidence. Linux/Darwin results are `cross-compile` only and do not raise Unix runtime/crash status. Replay is not yet wired into `AgentRunnerPort` or the Engine; a terminal receipt, including `uncertain`, proves only terminal-receipt presence/duplicate-dispatch blocking, never task `PASS` or AT-23. The final verifier reported Node contract 4/4, replay 32/32, adapters, build/vet/test all passing on Windows; Linux/Darwin cross-compilation passed; no real CLI, network, or SecretStore access occurred. T027 remains `BLOCKED / NOT IMPLEMENTED`, and AT-23 does not pass. Validation: [Chinese report](validation/t027-replay-convergence.md) / [English](validation/t027-replay-convergence_EN.md); the effect-mapping prerequisite evidence remains [Chinese](validation/t027-agent-runner-effect-mapping.md) / [English](validation/t027-agent-runner-effect-mapping_EN.md).

T027 prerequisite update (2026-09-14, A0 replay-root composition): building on the preceding paragraph, A0 is complete and passed both the V4 Pro pre-review and the Codex independent final review plus closing re-review. The production replay store is now constructed only by `NewAgentRunnerReplayStoreForRun(runRoot, runID)`, which derives `<runRoot>/agent-runner-replay` from the durable run root and no longer accepts an arbitrary caller root (the arbitrary-absolute-path constructor became package-internal); the run root must already exist and carry the regular file `events/state-events.jsonl`; loading an R or C with a different `runId` fails closed as corruption while writing one is rejected as a root conflict; writer and protected roots are rejected by the bidirectional-disjointness and containment-chain rules, covering 8.3 short names, case aliases, and `\\?\` spellings; symlink/reparse component checks run at construction and again after the lock in every read/write entry point, with the residual TOCTOU window recorded in CONTRACTS; `ownership.json` is published monotonically without replacement and is not gated on publication durability `proven`, a moved or copied directory or an ownership mismatch fails closed, and a failed construction (including bootstrap and the low-level constructor) leaves no ownership metadata behind. Validation: native Windows `gofmt`/`go build`/`go vet`/`go test -count=1 ./...`, focused replay tests, Node contract 4/4, and all platform counterexample tests (junction swap, case/short-name/`\\?\` aliases, runtime link swap, concurrent first open) pass; Unix-side tests passed the GitHub Actions Ubuntu native regression (run 34799747443, commit `7ad1ac0`). A0 introduced no real dispatch and replay is still not wired to `AgentRunnerPort`/Engine; T027 remains `BLOCKED / NOT IMPLEMENTED` and AT-23 has not passed. Validation: [中文](validation/t027-replay-root-composition.md) / [English](validation/t027-replay-root-composition_EN.md).

T027 prerequisite update (2026-09-14, A1 pure admission): `AgentRunnerCompositeAdmission` is now pure preflight and immutable binding; the `RequestIndex` in-memory consumption, `ErrAgentRunnerRequestReplayBlocked`, and the `AgentRunnerRequestIndex` type were removed, while same-key idempotency/conflicts, first-dispatch, and replay identity consumption remain with the durable replay store and the dispatch path, with the boundary frozen in the bilingual CONTRACTS text, the `AgentRunnerAdmission` interface docs, and the implementation docs. Focused tests cover repeated and concurrent passes on one admission, no consumed state after failure, same-requestId different-digest ownership by the dispatch/store boundary, and input-invariance assertions. The retroactive V4 Pro pre-analysis and pre-review, plus the Codex final and remediation reviews, all passed; R1 (a contract amendment stating admission is not a lock and dispatch must reconfirm authorization and budget after publishing R) and R2–R4 were remediated; the Linux CI `-race` gate was added with the frozen workflow contract synced; committed as `452d173` and pushed, GitHub Actions Ubuntu (including the Race step)/Windows green (run 34809403487). T027 remains `BLOCKED / NOT IMPLEMENTED` and AT-23 has not passed. Validation: [中文](validation/t027-pure-admission.md) / [English](validation/t027-pure-admission_EN.md).

T027 prerequisite update (2026-09-14, A2 dispatch and launch ambiguity): building on the preceding paragraph, A2 is complete; both the V4 Pro pre-review and the Codex independent final review (including a per-test counterexample audit) left no High/Medium findings, and all remediation is closed. `AgentRunnerReplayDispatcher` (implementing `chain.AgentRunnerPort`) publishes R first, then performs the narrow `AgentRunnerLaunchReconfirmer` recheck (authorization unrevoked, budget reservation still outstanding; failures are fail-closed with R retained, no receipt, no spawn, no auto-retry), and then governs crash ambiguity through a two-file store-local receipt protocol: pre-spawn `launches/intent.<requestId>.jsonl` (no-replace single winner, bounded reread convergence, pre-write rejection on unproven platforms) and post-spawn `launches/identity.<launchId>.jsonl` (binding the intent digest and the opaque process identity), with the invariants “no intent means never spawned, a missing identity means unproven, and neither permits a blind relaunch”. R-only, existing-intent, intent+identity, and R+C terminal states all block and never relaunch; the only permitted in-process retry (a port-contractual unspawned failure) is conservatively not auto-executed by this slice. The U1 extraction is complete with A1 behavior guards unchanged; `launches/` is part of the production constructor, bootstrap, the locked runtime path-safety recheck, and the ownership-publish preflight enumeration. The Windows native gates (full and focused gofmt/build/vet/test) and the concurrency, collision, swap-chain, revocation, and start-failure counterexamples all pass; committed as `aeb504e` and pushed, GitHub Actions Ubuntu (including the Race step)/Windows green (run 34844216048), with the Unix-specific tests and `-race` actually executed by that run. T027 remains `BLOCKED / NOT IMPLEMENTED` and AT-23 has not passed. Validation: [中文](validation/t027-dispatch-launch-ambiguity.md) / [English](validation/t027-dispatch-launch-ambiguity_EN.md).

T027 prerequisite update (2026-09-14, A3 terminal publication and settlement): building on the preceding paragraph, A3 is complete; the V4 Pro pre-analysis and implementation pre-review plus the Codex independent final review (including a per-test counterexample audit) left no High findings and all remediation is closed. `AgentRunnerTerminalPublisher` binds completion publication and cost settlement into a four-phase chain under a store-local two-file protocol: no-replace publication of `terminals/terminal-intent.<requestId>.jsonl` (embedding the full C record and a deterministic settlement plan with the `settle-<20hex>` idempotency key bound to (requestId, C.recordHash), evidence carrying the C/R digests) → the ledger's `Settle` (deduplicated by idempotencyKey, a different key for the same reservation rejected) → `RecordCompletion` publishing C → `terminals/terminal-closure.<requestId>.jsonl` completing the chain; crash recovery relies on store-local records only (the intent is the sole recovery source; the ledger is an enforcer), and recovery is an idempotent completion of a slot we already own, not takeover. Rejection matrix: a C without an intent is an orphan fail-closed; a reservation settled under a different key receives no C; charged>reserved, invalid evidence, and negative observations are rejected before the intent is written; a different outcome for the same request conflicts; an unknown settlement keeps the hold (observable through `UnknownHoldReservations` and shared caps, never reusable, never relaunchable) and never fakes completion; on unproven platforms any new publication is rejected before any settlement, and a complete chain allows strictly write-free replay only. Windows native gates all green; committed as `c6f8585` and pushed, GitHub Actions Ubuntu (including the Race step)/Windows green (run 34900979883), with the Unix-specific tests and `-race` actually executed by that run. T027 remains `BLOCKED / NOT IMPLEMENTED` and AT-23 has not passed. Validation: [中文](validation/t027-terminal-publication.md) / [English](validation/t027-terminal-publication_EN.md).

T027 prerequisite update (2026-09-15, A4 chain terminal routing and resume): building on the preceding paragraph, A4 is complete; the V4 Pro pre-analysis and implementation pre-review plus the Codex independent final review (including a per-test counterexample audit) are fully remediated and closed (the pre-review found 1 Medium, re-reviewed with no Medium+ after the fix; the final review found 1 Medium, re-reviewed PASS by Codex after the fix). The new step state `TERMINAL_PENDING` (protocol-first: the bilingual CONTRACTS §2.1 transition row and §7 "Chain terminal routing", the schema `stepTransition`, and 126 contract fixtures) means "dispatched, awaiting terminal" and is not a schedulable resting state; after a successful AgentRunner dispatch the step only enters that state and returns `ErrAwaitingAgentRunnerTerminal` instead of taking the step-passed path. A terminal receipt must pass the one-way adapters translation (`ToChainAgentRunnerTerminal`/`LoadChainAgentRunnerTerminal`, binding every request→terminal-intent→closure field including the settlement slot, with chain reading no replay store) and be routed explicitly by the core `SubmitAgentRunnerTerminal`: completed only permits step `TERMINAL_PENDING→PASSED` (the task still walks freeze/gates/review/promotion and the chain completes only after every task is accepted), failed lands task/chain `FAILED`, uncertain lands the task `FAILED` and keeps the chain `PAUSED` (so `Run` refuses to resume blindly), cancelled archives stop evidence before its terminal states, and operator-action-required writes task/step `WAITING_FOR_OPERATOR` plus `RUNNING→PAUSED` before handing over to the T025 machinery (accepted additively on the open side with the classic paths unchanged; the ledger still owns interaction exclusivity). A repeated terminal for the same request converges idempotently, including both partial-write windows (step written/task not, and task written/chain not), while a different terminal conflicts with zero writes; resume must bind `priorSessionId` and `priorCompletionHash`, the latter appearing in that step's event evidence history, and anything unproven is refused with zero writes; `TERMINAL_PENDING` also joins the recover uncertainty list and the cancel cleanup. The Windows native gates (full and focused gofmt/build/vet/test) and the contract-fixture gate are green; committed as `a21335d` and pushed, GitHub Actions Ubuntu (including the `Race` and `Contract fixtures` steps)/Windows green (run 34920155025), with the Unix-specific tests and `-race` actually executed by that run. T027 remains `BLOCKED / NOT IMPLEMENTED` and AT-23 has not passed. Validation: [中文](validation/t027-terminal-routing.md) / [English](validation/t027-terminal-routing_EN.md).

T027 prerequisite status update (2026-09-15, A5 chain postflight acceptance): building on the paragraph above, A5 is complete, with two rounds of V4 Pro pre-review (1 High + 1 Medium + 3 Lows remediated, then cleared) and three rounds of independent Codex final review (High 1 + Medium 2 + one later Medium, all remediated; the third round returned `PASS`) fully closed. A new chain-owned postflight gate (`internal/chain/postflight.go`) requires a completed terminal to carry five mutually distinct frozen-fact digests (manifest/diff/log/usage/process-stop) that are also distinct from the request and completion digests, and extends the routing evidence to a fixed positional layout (prefix 2..6, at least seven digests) so the facts can be rebuilt across restarts; after every step has passed and before `REVIEW_PENDING` is written, the chain requires the mandatory `PostflightPort` to re-derive them independently (re-capture the manifest, diff it against the parent, run scope/secret/file-type/side-effect checks) and reconcile field by field — **only a pass that carries all five fact digests writes `REVIEW_PENDING`**, after which the existing freeze/gates/review/completed-promotion sequence runs; an exit code, a completed receipt or any adapter record can no longer produce a task `PASSED` on its own. Failure routing: rejection → task `REPAIR_PENDING` + chain `PAUSED` (protocol-first `STEPS_RUNNING→REPAIR_PENDING`, 128 fixtures); uncertain → task `FAILED` + chain `PAUSED` without retry; port error → `postflight-failed`; invalid decision → `postflight-invalid`; missing facts, multi-step attribution ambiguity or too-short evidence → `postflight-facts-missing`. Idempotency and crash windows: the gate is not re-run once `REVIEW_PENDING` is durable, and a crash after a passed gate but before the review write re-runs the gate once on re-entry and converges; a completed replay with identical facts converges, while divergent facts or unrebuildable evidence are refused as a conflict with zero writes. Boundaries written into the contract: a no-change execution's manifest digest may legitimately equal the parent snapshot digest, so that discrimination belongs to the port alone and the chain never refuses on digest relations; pre-A5 completed routing evidence and a review state without a gate trace are both refused with zero writes (new `ErrUnqualifiedReview`) and may only be re-executed as a new attempt. All Windows-native gates (full and focused gofmt/build/vet/test plus five mutation checks) and the contract-fixture gate are green; committed as `b8bad47` and pushed, with GitHub Actions Ubuntu (including the Race and Contract fixtures steps) / Windows fully green (run 34950623175). Report: [中文](validation/t027-postflight-acceptance.md) / [English](validation/t027-postflight-acceptance_EN.md). T027 remains `BLOCKED / NOT IMPLEMENTED` and AT-23 has not passed.

T027 prerequisite status update (2026-09-15, A6 offline pinned CLI run): building on the paragraph above, A6 is complete (committed as `c2d2819` plus the fix commit `6db5a10`, both pushed to `origin/main`). It adds the offline external-run stack: `internal/adapters/agent_runner_process_registry.go` (a live handle registry, the stage ladder, an opaque `ProcessID`, the same identity mirror as the console, `managed-process.identity.json`, and the run root's single launch slot `managed-process.launch.slot`), `agent_runner_timeout.go` (a watchdog owning the **sole deadline**, where a simultaneous timeout and cancellation converge on cancellation through a bounded settling window), `agent_runner_evidence.go` (raw log bytes with a completeness marker, strictly decoded JSON(L) events, the closed usage shape, the pre/post manifests and their diff injected by the port, and the `FrozenFacts` gap gate), `agent_runner_pinned_cli.go` (version precheck through `guard` → exclusive launch-slot claim → exactly one spawn with `PROOFRAIL_EVIDENCE_DIR` pointing at this run's evidence directory → identity mirror → registration; a stop resolves identity by ownership; a negative verdict waits for the run side to finish reconciling), `agent_runner_run.go` (orchestration and the terminal mapping, the settlement mapping, and the process-tree re-verification after a natural exit), plus the deterministic offline CLI `tools/agent-stub`. Protocol-first: the bilingual CONTRACTS §7 gained the A6 paragraph (artifact layout and the five fact digests, no gap may be expressed as completed or as an empty field, a non-zero natural exit outranks gap classification, `exitCode` only on a natural exit, the bounded watchdog and stop verification, the single-winner settling window, the identity mirror with no new mechanism, the run root's single-launch exclusive create with freshness and token ownership, tree re-verification after both a natural exit and an external stop, the version precheck, the ownership boundary, and the offline boundary throughout). Key semantics: only “four Complete flags + five digests + a verified-gone process tree” may be completed; an operator request outranks both success and timeout; any gap, unclassified stop or unreconciled run is uncertain and carries no facts; the settlement is observed only for completed plus complete usage. Review: seven V4 Pro pre-review rounds, all closed (High 2 + Medium 10 order of magnitude; final `PASS WITH FIXES` with nothing blocking). ④ ran the pilot under the user's same-turn authorisation with the low-cost combination — Claude Haiku 4.5 scanned 0 items (flagging its own zero as suspicious) and MAI-Code-1.1-Flash independently found 1 High + 1 Medium, **both confirmed true by actually executing them** (a new counterexample plus a 200-round simultaneous-race experiment) and both remediated with a `RE-REVIEW: PASS` (Codex fallback usage during the pilot stage: 0). After ④ the user authorised **one additional** Codex blind audit (GPT-5.3-Codex) in the same turn, followed by two re-reviews (closing the remediation loop, then the “no self-audit” review of the test delta the driver had rewritten — 3 calls in total, above pilot document §6's cap and registered as such); the blind audit **found 1 High the combination had missed** (`refuseForeignSlot` failed open on “any read error other than NotExist” and on “an empty owner”, now a three-arm fail-closed switch) and 1 Medium (it claimed “two stop mechanisms”; the driver adjudicated “one guard identity mechanism with two entry points” and tightened the contract wording in both languages), and the reviews returned `RE-REVIEW: PASS` — so the pilot's residual miss is now measurable: this slice's combination missed 1 High while ③ missed 0, which is recorded as **the combination being usable as a low-cost supplementary scan layer for regular slices, with the hard-gate slices A7/B2/B3/B4 keeping Codex as their final review** (promotion remains the user's decision). 52 mutations all RED / SURVIVED 0 (48 in part 1 plus M52–M55 in the Codex round; re-run after each of the two test strengthenings, every restore verified by SHA-256), the native experiments E1–E8 against real processes are green (validated on Windows only; `a6native` does not run in CI), and the Windows gates (full gofmt/build/vet/test) plus the contract fixture gate (128) are green. Report: [Chinese](validation/t027-offline-process.md) / [English](validation/t027-offline-process_EN.md). CI evidence: the first run `35074301616` failed the Test step on the Ubuntu leg — 12 pinned-CLI cases exposed a **Linux zombie-semantics** defect (the aliveness probe read a process that had exited but had not been reaped by its parent as alive, so the stop path that resolves by persisted identity reported `uncertain` once the grace ran out; Windows has no zombie state, hence its green leg). The fix in `internal/guard/process_linux.go` parses the `/proc` state and pgrp (a `Z`/`X`/`x` process is not running; when signal 0 still succeeds the group is scanned for a non-terminal member, keeping the conservative EPERM semantics) and adds the Linux-only regression test `TestLinuxStopProvesAnUnreapedTerminatedChild` (a deliberately unreaped child). With fix commit `6db5a10` pushed, run `35076045445` is **fully green** — Ubuntu actually executed the `Race` and `Contract fixtures` steps while Windows skips both under the `runner.os` condition. Local gates reinforced with a `GOOS=linux` (`CGO_ENABLED=0`) cross `build`/`vet`. T027 is still `BLOCKED / NOT IMPLEMENTED` and AT-23 has not passed.

T027 pre-state update (2026-09-16, A7 publication-durability candidates and their falsification): building on the previous paragraph, A7 is complete (an **offline falsification and API-observability** slice; it changes neither the production publish primitive nor the frozen contract sentences). **The level and its premise must be read together**: every level is one of `proven` / `disproven` / `unresolved` (`observed`/`falsified`/`disproven-by-policy` are evidence annotations only, never levels), under premise **P = process-level crash, no power loss, single host Windows 11 / NTFS on the local volume**. Under that premise the visibility and arbitration layer is `proven` (P1–P5, constructive argument plus reproducible measurement); power-loss durability is `unresolved` (U1–U5, handed to B3); candidate-level D1–D6 are the set of falsified **claims** (all `disproven`; candidates C2/C3 remain `unresolved` on power-loss durability and go to B3; C1 is the shipped baseline; the C5 marker is `disproven` as a durability mechanism and C6 awaits a contract revision); **Windows publication durability stays `unproven` with first-dispatch still refused**, T027 remains `BLOCKED / NOT IMPLEMENTED` and AT-23 has not passed. On the production side the only addition is a semantics-free test seam `replayStorePublishStageHook` (nil by default; reads no environment variable, writes nothing, changes no default behaviour) that injects crash points at the five stages of the unified no-replace publish primitive (`temp-written`/`temp-synced`/`temp-closed`/`record-linked`/`parent-synced`). Measurements: under real kills the request matrix gives `absent`×5 for the first three stages and `dispatched-unknown`×5 for the last two, with CP4 and CP5 equivalent key by key (so whether the durability step took effect **cannot be decided by a kill**, which is why no candidate can rise to `proven`); the completion matrix killed inside the `completions/` path gives `dispatched-unknown` for the first three stages (C invisible, R alive, zero orphans) and `terminal-receipt-present` for the last two; 8-way true concurrency inside one process yields exactly one `first-dispatch` (a killed winner stays `dispatched-unknown` and any re-dispatch is refused as `unknown-block`); same-round two-process concurrency uses **two-way rendezvous fencing** (each child writes its own ready file and the parent releases them once after seeing both), giving exactly one first-dispatch in each of 5 rounds (concurrent attempts, **not** proven cross-process strong synchronisation); the E4 directory-handle `FlushFileBuffers` probe overturns the assumption the slice started from (read-write and write-only handles succeed unprivileged, a read-only handle gets `Access is denied`), so C3 now shares first place with C2 as a B3 injection candidate, while "is the directory entry actually durable" stays `unresolved`; E6 `MoveFileEx(MOVEFILE_WRITE_THROUGH)` publishes unprivileged and refuses an existing target (with or without an open reader) with `fs.ErrExist` as a **hard assertion** (unmeasured causality is kept out of the report); E7's two-phase marker changes no durability level and creates no new state (it is `disproven` as a durability mechanism); E8 `.tmp` residue is completely inert for visibility and arbitration yet accumulates (**deliberately not fixed in this slice**, recorded as an open item). Review: V4 Pro pre-analysis plus a pre-review `PASS WITH FIXES` and two re-review rounds to `RE-REVIEW: PASS`; the ③.5 low-cost combination scanned independently over two rounds to `INDEPENDENT SCAN: PASS`; ④ Codex's independent final review ran round by round until closed (**report §9 is the single authority for the rounds and their findings**; this section deliberately does not enumerate them, and every round's findings are fixed before the next round starts, with the closure state tracked in that report's §9). Commits `cc9adf4` (the production seam plus four test files) and `dee0066` (report, ADR-013, dev plan) are pushed to `origin/main`, and GitHub Actions is fully green (run `35118531930`, ~2m04s): the Ubuntu leg executed the Race step (`CGO_ENABLED=1 go test -race ./internal/adapters/... ./internal/chain/...`) and the Contract fixtures step (4/4) while the Windows leg skipped both by `runner.os`, with Build/Vet/Test passing on both legs - so the **five in-suite A7 tests really ran under the Linux race detector**. Boundaries: **only** the `a7native` experiments stay out of CI (they must be re-run by hand on a Windows host, and this report's evidence is single-host); E4/E6/E9/E10 are Windows-only; U1–U4 (real power loss plus a per-round crash journal on a different physical device plus a restart inventory) go to B3. Verification: [Chinese](validation/t027-windows-publication-durability.md) / [English](validation/t027-windows-publication-durability_EN.md). T027 is still `BLOCKED / NOT IMPLEMENTED` and AT-23 has not passed.

T027 pre-state update (2026-09-17, B1 candidate live capability and availability discovery): building on the previous paragraph, B1 is complete (the user granted **10 probe accesses** in the same turn, 1-2 of which were allowed for the "does a newer candidate version cover T026's two unsupported items" question). This slice has **no production change**; its deliverables are live probe evidence plus a bilingual report. **Verdict: no acceptable candidate within the assessed scope** (the stable channel of the GitHub-hosted Copilot CLI, 1.0.83 and 1.0.85) - T026's two unsupported items (`toolControl`: the `gci` alias bypassing a `shell(Get-ChildItem)` deny; `networkControl`: the shell bypassing an exact URL deny) **still reproduce on 1.0.85**, with the 1.0.83 control agreeing. To answer ④'s round-1 High (transcripts do not retain argv), both decisive scenarios were **replayed with runtime argv capture**: the command line polled through `Get-CimInstance Win32_Process` contains `--deny-tool=shell(Get-ChildItem)` / `--deny-url=https://example.com`, while the covered command was still executed by a child process the CLI started itself (`pwsh.exe` carrying `gci -Name` / `[Net.WebClient]::new().DownloadString('https://example.com')`). ⇒ **B1 closes on its acceptance branch and stays blocked** (no candidate-native route for B2); **B2 should move to an external OS enforcement proof** using this slice's scenarios as the counterexample baseline. **Neither assessed nor excluded**: BYOK `deepseek-anthropic` and non-Copilot-CLI families. **Product defect DR-1 (unfixed, registered)**: `ai check` forwards `--max-requests` straight into the CLI's `--max-ai-credits` (`internal/adapters/ai_probe_copilot.go:111`) while the CLI requires ≥30 and `ai check` forces `--max-requests == 1` (`internal/console/ai.go:46`), so the product availability probe **cannot drive this candidate** (the record is always `unknown` with `requestsUsed=0`) ⇒ **no real availability verdict exists yet**; fixing DR-1 is a production behaviour change needing its own slice with contract-first work. Probe metering: **9/10 attempts, 6 paid premium requests** (#3/#4/#5 and one excluded artifact were zero-cost). Review: ③ V4 Pro pre-review over two rounds (`PASS WITH FIXES`: 1 High + 2 Medium + 5 Low → `PRE-REVIEW: PASS`), ③.5 MAI `INDEPENDENT SCAN: PASS`, ④ Codex final review over three rounds (1 High + 1 Low, then 2 Medium + 1 Low, all fixed → round 3 **`RE-REVIEW: PASS`**); **tool adoption**: the probe runner was promoted from `tmp/` to the repository tool `tools/agent-probe/Invoke-CandidateProbe.ps1` (dry run by default; **only `-Run` executes the candidate**; SHA-256 pin; the artifact root is constrained to the `tmp/` subtree and refuses escapes, existing roots and reparse points) plus the hermetic self-test `tools/agent-probe/Test-CandidateProbe.ps1` (stub candidate, zero model calls; last observed run fully green: `selftest: checks=35 failures=0`), a change that went through six further ④ rounds to `RE-REVIEW: PASS` (no Medium+), with the decisive artifacts sanitised into `docs/validation/evidence/b1-20260917/`; ⑤ gates (no production change; tree-still-green check): `gofmt -l` empty, `go build`/`go vet` clean, `go test -count=1 ./...` 13 packages ok, contract 4/4. Boundaries: single host, single account, host authentication; T026's full analyzer was not re-run; the BYOK channel was not exercised; nothing lifts any `unproven`, and this is not AT-23 evidence. Verification: [Chinese](validation/t027-b1-candidate-live-discovery.md) / [English](validation/t027-b1-candidate-live-discovery_EN.md). T027 is still `BLOCKED / NOT IMPLEMENTED` and AT-23 has not passed.

T027 pre-state update (2026-09-18, B2 external OS enforcement decision and proof): building on the previous paragraph, B2 is complete. **Decision: option B, "external OS enforcement"** - the managed process runs inside a zero-network-capability AppContainer (the kernel blocks public/LAN/DNS traffic) and the container holds a full-loopback exemption, so the only usable egress is the host-side allowlist proxy `127.0.0.1:39877` (default deny, fail-closed when the audit write fails, exits on an occupied port, loopback-only listen); policy is enforced at the OS boundary and in the proxy, neither of which the managed process controls. The boundary definition, terminology clarifications ("only egress" means the managed process can leave the host only through the proxy, while the proxy's own egress is out of boundary) and the three process requirements are in the bilingual report `validation/B2-EXTERNAL-ENFORCEMENT.md` (13 sections); the sanitised bundle `validation/evidence/b2-2026-09-17/` (41 artifacts + `MANIFEST.md` + `SHA256SUMS.txt`, 0 mismatches) carries the managed-candidate identity digest `sha256 d3f3bb7b…0f671ee2`, the `schemaVersion 2` baseline and exemption-table snapshot, and the machine-discipline scripts are now repository tools `tools/agent-probe/appcontainer-b2/` (hermetic `Test-B2Tooling.ps1`: `checks=8 failures=0`) and `tools/agent-probe/enforcement-proxy/` (stdlib-only Go proxy + 15 tests, port 39877 frozen). **Rehearsal and restoral**: on 2026-09-18 the user ran `Invoke-B2LoopbackRehearsal.ps1` in an elevated session - all six steps `exit=0`, `rehearsal-ok=True` and `restoral: zero-difference` after the revert; the close-out revert saw zero difference, the container profile was deleted and the read-only check returned `expect=absent ok=True`; the transcript is archived as `restorability/rehearsal.txt`, which clears the last open item in report section 13. **Review**: ④ independent third-party model (Codex) final review rounds to `RE-REVIEW: PASS` (0 blockers); the remediated findings include the baseline identity gate, legacy-baseline schema compatibility, profile existence now read from the registry (name-only derivation is not existence evidence), the PowerShell 5.1 BOM-less-UTF-8 read defect, and the `normalize-encoding.ps1` traversal/output defect (33.7 minutes with no output -> 17 seconds listing only violations). **Gates**: `gofmt`/`go build`/`go vet`/`go test` all green (including the 15 proxy tests), tooling self-test 8/8, `normalize-encoding.ps1` reports `violations=0`. **Boundaries**: this slice proves that the **external OS enforcement boundary** is measured and not trivially bypassed; it is not AT-23 evidence, lifts no `unproven` state and does not mean the product side has been wired to the AgentRunner or to an in-container tool path; B4 must deliver the AT-23 end-to-end evidence and independent review on top of this boundary, and any reuse of the same port must take a fresh baseline snapshot and a read-only confirmation first (report section 9). **Repository**: the deliverables (report + bundle + the two `tools/agent-probe/` directories + `.gitignore`) were committed and pushed under the user's same-turn authorization (see below). **Commits and CI**: `58757e8` (tool adoption plus the `/tools/tmp/` guard in `.gitignore`), `fd99dc3` (report + bundle + ledger write-back) and `408bd99` (narrowing the byte-exact `*.raw.txt` rule) are pushed to `origin/main` (`c5c22ad..408bd99`); GitHub Actions is **green twice** - run `35259806285` (head `fd99dc3`, about 1m48s) and run `35261204021` (head `408bd99`, about 1m52s): the Windows leg passed Build/Vet/Test and the Ubuntu leg passed Build/Vet/Test plus Race (`internal/adapters`/`internal/chain`) and Contract fixtures; the new proxy package's 15 tests run in the default suite on both legs and are not in the `-race` subset. One repository-fidelity defect was fixed before pushing: `* text=auto eol=lf` normalised the raw capture `measuring/probe10-dir-listing.raw.txt` to LF, which would make its `SHA256SUMS.txt` entry fail after a clone; the scoped rule `docs/validation/evidence/**/*.raw.txt -text` was added and the bundle re-verified with clone semantics (`git worktree`) at **41/41 entries, 0 mismatches**. T027 remains `BLOCKED / NOT IMPLEMENTED` and AT-23 has not passed. Validation: [中文](validation/B2-EXTERNAL-ENFORCEMENT.md) / [English](validation/B2-EXTERNAL-ENFORCEMENT_EN.md).

T027 prerequisite status update (2026-09-20, B3a power-loss injection rig + two-way calibration): on top of the previous paragraph, B3a is complete with the machine verdict **`CALIBRATED`** (`docs/validation/evidence/b3a-2026-09-20/calibration-verdict.json`, `gate = CALIBRATED`) - the rig discriminates "lost" from "survived" in both directions, so B3b could start. Key measurements (**all taken from `docs/validation/B3A-CALIBRATION.md`**): the negative control (deliberately not flushed) lost the record in **4 of its 5 valid rounds** (`lost-zero-filled` ×2, `lost-absent` ×2) with 1 survival (r03 `temp-written` `survived`); the positive control (explicit flush plus directory-entry commit) **survived in all 5 valid rounds** (`positiveLosses = 0`); discrimination 0.8 / 0.0; **all 12 rounds reported `auditVerdict = HARD-POWER-LOSS`** (no round had its survival faked by a clean shutdown); the **two inventories agreed byte for byte in every round** (`inventoryIdentical = true` ×12, with reconciliation comparing content and size only, never time); the journal chain holds **36 records** (12 each of `plan`/`cut`/`reconcile`) and reconciles round by round across a reboot; the volumes stayed clean. Frozen protocol: S1–S9 (S7 hard power-off plus a `cut` record → S8 reboot, power-loss audit and two inventories → S9 reconciliation) and the **2.0 s injection delay**; **changing any of them requires recalibration** (B3b reused them unchanged). The slice found and fixed one Critical defect of its own (`Get-B3aReconcile.ps1` carried a top-level `param()` while being dot-sourced, so same-named caller variables were rebound and the whole calibration was invalid, stalling on `r00`) ⇒ modules must carry no top-level parameters, the CLI was split into `Invoke-B3aReconcile.ps1`, and the self-test `module-load-preserves-vars` pins it. **Boundary (verbatim from the report)**: **a loss rate is not a candidate conclusion** - the 4/5 negative-control figure only says "the rig can manufacture an observable loss"; the five-stage × N × candidate durability verdict belongs to B3b. Evidence pack `docs/validation/evidence/b3a-2026-09-20/` (128 files, `SHA256SUMS.txt` 126 entries, **0 mismatches**); live data outside the repository at `D:\VirtualBox VMs\Win11B3\b3a-runs\2026-09-20\`. Commits `136352b` (rig and self-test) and `babc041` (T027 working documents) were pushed to `origin/main` (`815c03f..babc041`, **never to gitee**), CI run `35482066033` (head `babc041`, 1 m 48 s) green. (Every field in this paragraph comes from that report; fields it does not contain are omitted rather than inferred.)

T027 prerequisite status update (2026-09-21, B3b candidate injection matrix and durability verdict): on top of the previous paragraph, B3b is complete (the real power-loss injection matrix and the three-tier verdict). **The tier and the premise must be read together**: tiers are `proven` / `unresolved` / `disproven` only; premise **P′ = VirtualBox 7.2.18 r175117 / guest Windows 11 25H2 build 26200.8037 / NTFS on SATA-AHCI with `useHostIOCache=false` / injection volume `R:`=INJECT (data.vdi 7.98 GB, 4096 B clusters) / record-level ~1 KiB / 2.0 s pre-cut freeze window / N=5**; injection is a guest hard power-off (`controlvm poweroff`). Results: the main matrix ran **17 cells × 5 = 85 rounds, all valid, 0 void**; the U4 ordering sub-matrix ran **6 cells × 3 = 18 rounds, all valid, 0 true orphans**; the discrimination gate holds (negative control 5/5 lost, positive control 5/5 survived); the three-tier verdict is **C2 `proven`** with C1/C3/C2C3 `unresolved` (the verbatim reasons for C3/C2C3 are `mechanism evidence missing in rounds 77 / 81` - a survival whose mechanism was never shown to run must not, fail-closed, be cashed into a durability claim, which is **not** the same as "not durable"); U4 is `unresolved` (a finite sample can only falsify ordering, never prove it). **Earlier conclusions stand**: Windows publication durability is still `unproven`, first-dispatch is still refused, T027 is still `BLOCKED / NOT IMPLEMENTED`, AT-23 has not passed; rising to `ACCEPTED` and lifting the lock still require **first revising the frozen CONTRACTS durability sentences** (protocol first, its own slice, **B3c** - that wording revision completed on 2026-09-21, see the next paragraph; lifting still stays bounded by that clause's **unlock boundary**). On the production side only two **zero-semantic test seams** were added (`replayStorePublishPrimitiveHook`, `replayStoreParentSyncHook`, nil by default, no environment reads, no change to default behaviour); a manual **variant arm (device self-falsification)** was added too (the rig refuses to cash `proven` when the primitive is silently degraded, corroborated offline and reproducibly). Reviews: V4 Pro pre-review `changes-required` → closing `PASS`; ③.5 MAI `PASS`; ④ Codex **6 calls** (1 final + 1 re-review + 2 closing + 2 delta; why this exceeds the §2.3 cap and each call's findings have the report §8 as their single authority). Nine live-run defects (P1–P9) were found and fixed in place, each with a falsification experiment, all recorded in the design note. Evidence: `docs/validation/t027-b3b-durability.md` (+ `_EN`) and the evidence pack `docs/validation/evidence/b3b-2026-09-21/` (2157 files, `rigMismatches=0`). **Committed as `abfcecc` and pushed to `origin/main` (never to gitee)**, CI run **`35562559401`** green on both legs (Ubuntu including Race/Contract fixtures, and Windows). Ledger note: neither B3a nor B3b had a DEV_PLAN paragraph before; this is the first addition since A7, and **the B3a paragraph is still owed** (to be back-filled from `docs/validation/B3A-CALIBRATION.md`).

T027 prerequisite status update (2026-09-21, B3c CONTRACTS durability-upgrade clause revision): B3c is complete (documentation only, protocol first; no `.go`/`.json`/schema/fixtures change). **New clause**: `docs/CONTRACTS{,_EN}.md` §7 gains **Publication-durability tiers and upgrade conditions** - a tier is `proven` / `disproven` / `unresolved` (as in A7; `observed`/`falsified` are evidence annotations only); the unit of judgment is **(platform × premise P)**, every tier statement must carry its platform, premise and evidence together, and a tier earned under a premise must never be restated as a premise-free platform-level tier; recording `proven` requires, under the **same premise**, all three of (a) reproducible injection or control evidence, (b) a **discrimination gate** (under the same rig, the same writer and the same record-level payload, negative controls show at least one loss and positive controls none; a failed gate caps the verdict at `unresolved`), and (c) **mechanism evidence**; missing any one leaves `unresolved` (and `unresolved` must never be read as "not durable" or "already falsified"), while an attributable counterexample is `disproven`; a candidate mechanism's tier and the **platform publication-durability tier** are different objects (closing the short-circuit reading "candidate `proven` ⇒ platform `proven`"); at runtime `unresolved` and `disproven` behave as `unproven`; "durable forever / long-term guarantee" wording is forbidden. **The three frozen sentences are parameterised** (the replay-store publication-durability sentence / the launch-intent-follows-R sentence / the terminal-publication refusal sentence): from a **platform constant** into a rule **parameterised by the platform's durability tier**, each stating that an upgrade changes the value only **for that platform under that premise**. **Unlock boundary**: an upgrade lifts the corresponding refusals only on that platform, within the deployment environments covered by that premise; AT-23 is not waived, independent review and protocol-first revision are not waived, and no gate beyond `first-dispatch` is relaxed. **Iron boundary (kept)**: this slice only freezes the wording and **does not lift Windows `unproven`**, does not lift first-dispatch, T027 stays `BLOCKED / NOT IMPLEMENTED` and AT-23 has not passed; per the B3b verdict ledger the Windows verdict is **tiered** (C2 `proven`, the others `unresolved`) and not a wholesale upgrade. The ADR-013 row completion condition and the §5 adoption precondition now point at that clause. Reviews: ① V4 Pro rulings U1–U14; ③ pre-review `PASS WITH FIXES` → fixes → re-review `PASS` → delta `PASS`; ③.5 MAI produced no actionable finding in 2 calls (format deviation recorded honestly); ④ Codex 3 calls (first `FINDINGS` 1 Medium + 1 Low → re-review blocked only on raw-diff evidence → closing round **`PASS`**). ⑤ 34 mechanical checks all green plus **4 mutation experiments all red with byte-identical restore**; gates `gofmt`/`build`/`vet`/`test` green (14 packages ok, 0 FAIL); all 8 `.md` files pass the BOM+LF encoding check. Evidence: `docs/validation/t027-b3c-contracts-durability.md` (+ `_EN`). **Committed as `7bdfa0c` and pushed to `origin/main` (never to gitee)**; CI run `35575667145` **failed the first attempt** of the Windows `Test` step (`tools/agent-probe/enforcement-proxy`'s `TestObserveModeAllowsUnknownHostAndLogsIt`, `main_test.go:469: target accepts = 0, want 1`; Ubuntu was green), and `gh run rerun --failed` was **green on both legs** with the run ending `success` ⇒ judged **intermittent**; that flake is **not** DR-2's test and is registered as **DR-3** (see the slice list's known-defects table).

T027 prerequisite status update (2026-09-23, B4 AT-23 E2E evidence + independent review): on top of the previous paragraph, B4 is complete (**mechanism-leg** end-to-end evidence plus independent review). **Four things kept apart (never merged)**: ① the B4 slice is ✅ complete; ② **AT-23 is not passed** - the five mechanism faces (isolated workspace, timeout, missing logs, unknown recovery, exit-0 rescan) were executed and evidenced on a real Windows host, but AT-23 also contains the **real AI candidate start and billing** leg, which is **measurably unreachable** because no candidate can legally be started (the frozen B1/B2 conclusion, see **OB-21**), and it **must not** be written as "partially passed"; ③ **the remaining T018 gates** (AT-24, final ZIP binding and re-verification, the EOL notification channel, the product owner's S1-exit approval) are **not closed**, and whether they share this dependency is registered as a T018 open question; ④ **S1 exit** is not done. **Implementation and boundaries**: the assembly harness `tools/b4-e2e/` (reusing the production packages, no `os/exec`) drives the five faces; production dispatch, terminal publication and the production gate port pair are refused fail-closed on Windows, every skipped face's alternative evidence is labelled **non-equivalent substitute** (including **policy port uncovered**), and **this slice claims no equivalent coverage**; the `CONTRACTS` §7 gate/postflight/freeze ordering wording is unified to the measured order (**wording only, no code change**, registered as **OB-22**). Reviews: ③ pre-review F1-F5 remediated and re-verified; ⑥ independent scan `PASS` with 0 findings; ⑦ final review F1-F5 and R-1..R-4 (R-1 tripped the pre-declared red line and forced a stop-and-escalate, then the user ruled), and the Medium findings of the ⑦ re-review were closed deterministically under the OB-11 exception standard (**OB-24**). ⑤ gates `TOTAL_FAIL=0` in the `tree`/`index`/`ci` scopes with `SELFTEST PASS`; ⑨ native validation included a same-command re-run (differences confined to the declared volatile fields) and a full recomputation of `tmp/b4/SHA256SUMS.txt` with 0 mismatches. Evidence: `docs/validation/t027-at23-e2e.md` (+ `_EN`). **Committed as `e8cbff6` and pushed to `origin/main` (never to gitee)**; CI run **`35812311023`** green on both legs, with the `Gates` step really executing on both.

## 3. Requirement Mapping

Unless a task is explicitly marked complete, implementation evidence is planned and does not represent implementation.

| REQ ID | Plan Items | Tasks | Acceptance/implementation evidence |
|---|---|---|---|
| REQ-001 | P0.1,P0.2,P1.6 | T001,T002,T003,T018 | AT-01/15; generic schema/non-C example |
| REQ-002 | P1.2,P1.6 | T009,T018 | AT-03/15; chain/report |
| REQ-003 | P1.2 | T009 | AT-03; ordered scheduling |
| REQ-004 | P0.2,P1.1,P1.2 | T002,T006,T008 | AT-02/04; snapshot/applier |
| REQ-005 | P0.2,P1.3 | T002,T011 | AT-05; review/publish |
| REQ-006 | P0.3,P1.4,P1.6,P1.8 | T004,T013,T017,T026,T027 | AT-08/14/23; adapters/capability/CI |
| REQ-007 | P0.2,P0.3,P1.4,P1.8 | T002,T004,T013,T025,T026,T027,T028 | AT-08/22/23/24; execution/auxiliary channels |
| REQ-008 | P0.2,P1.4,P1.8 | T002,T013,T025,T026,T027 | AT-08/22/23; context/session recovery |
| REQ-009 | P0.3,P1.5,P1.8 | T004,T016,T025,T027,T028 | AT-12/22/23/24; CLI/TUI interaction |
| REQ-010 | P0.2,P1.5 | T002,T016 | AT-01/12; config explanation |
| REQ-011 | P1.5,P1.6 | T015,T018 | AT-11/15; three harnesses |
| REQ-012 | P0.2,P1.2 | T002,T007,T010 | AT-06; gate runner |
| REQ-013 | P0.2,P1.5 | T002,T015 | AT-11; template generation |
| REQ-014 | P0.1,P1.3,P1.6 | T001,T012,T018 | AT-07/15; stage report |
| REQ-015 | P0.3,P1.5,P1.6 | T004,T016,T017 | AT-12/13/14; build/TUI |
| REQ-016 | P1.4,P1.5,P1.8 | T013,T016,T025,T026,T027 | AT-08/12/22/23; no-IDE CLI Agent |
| REQ-017 | P0.1,P0.2,P0.3 | T001,T002,T003,T004 | AT-01; readiness |
| REQ-018 | P0.2,P1.2,P1.5 | T003,T009,T016 | AT-01/03; four kinds |
| REQ-019 | P1.6 | T017 | AT-13/14; two generations |
| REQ-020 | P0.2,P1.5 | T003,T015 | AT-01/11; components/dual language |
| REQ-021 | P0.2,P0.3,P1.4,P1.8 | T003,T004,T014,T025,T028 | AT-09/10/22/24; handoff/black-box return |
| REQ-022 | P0.2,P1.3,P1.5 | T003,T011,T015 | AT-01/05/11; coordinated docs |
| REQ-023 | P0.1,P0.3,P1.1,P1.2,P1.3,P1.4,P1.8 | T001,T004,T005,T007,T010,T011,T012,T014,T025,T026,T027,T028 | AT-05/06/07/09/22/23/24; security |
| REQ-024 | P0.2,P0.3,P1.1,P1.2,P1.3,P1.4,P1.6,P1.8 | T002,T004,T005,T006,T008,T009,T011,T014,T017,T025,T026,T027,T028 | AT-02/03/04/09/13/22/23/24; recovery |
| REQ-025 | P0.2,P1.1,P1.3,P1.4,P1.6,P1.8 | T002,T005,T011,T013,T014,T017,T025,T026,T027,T028 | AT-04/06/10/14/22/23/24; execution evidence |
| REQ-026 | P0.1,P1.2,P1.3,P1.4,P1.8 | T001,T009,T010,T012,T013,T026,T027,T028 | AT-03/06/07/23/24; budgets/unknowns |
| REQ-027 | P0.3,P1.5,P1.8 | T004,T016,T025,T028 | AT-12/22/24; terminal/risk notice |
| REQ-028 | P0.1,P0.2,P1.1,P1.6 | T001,T002,T003,T005,T017 | AT-01/04/14; compatibility |

## 4. Testing and Definition of Done

Take the union of section 3 and this table. Checkpoint product-slice supplements likewise add P0.1/P0.2/P1.7 without new requirement IDs.

| PC | REQ | Plan Items | Tasks | Acceptance |
|---|---|---|---|---|
| PC-01 | REQ-009/010/016/027 | P0.1,P0.2,P1.7 | T001,T002,T003,T019 | AT-16 |
| PC-02 | REQ-002/004/025 | P0.1,P0.2,P1.7 | T001,T002,T003,T020 | AT-17 |
| PC-03 | REQ-005/021/023 | P0.1,P0.2,P1.7 | T001,T002,T003,T021 | AT-18 |
| PC-04 | REQ-012/023/024 | P0.1,P0.2,P1.7 | T001,T002,T003,T022 | AT-19 |
| PC-05 | REQ-006/026 | P0.1,P0.2,P1.7 | T001,T002,T003,T023 | AT-20 |
| PC-06 | REQ-019/025/028 | P0.1,P0.2,P1.7 | T001,T002,T003,T024 | AT-21 |

See [TEST_STRATEGY_EN.md](TEST_STRATEGY_EN.md): Go testing, real local files/processes, independent schema validation, CLI oracle and authorized host smoke; no database/browser dependency. Package tests join go test ./... and report pass/fail/skip counts. Mocks do not prove native Windows process/filesystem or actual adapter capability.

A task needs implementation/tests, requirement/contract links, bilingual affected docs, commands/exit/evidence and no scope violations. Iterations require build/vet/test plus relevant AT. S1 requires all mandatory tests and human sign-off. Plan coverage is not implementation coverage.

## 5. Later Stages and Resources

Detail S2 after S1 exit: independent Ubuntu 24.04 `linux/amd64` development and production support after the Windows AgentRunner is complete, composed dependency ordering/three-language execution, supervised/Web/optional VS Code views, rule-based model selection and intelligent documentation hints. Keep macOS, ARM, and other OS/architecture combinations in an unscheduled platform backlog; create later work only from independent evidence on native environments or GitHub Actions runners. S3 contains script B, more editors and inception. Preserve requirement IDs, extend tasks/AT through RFC/ADR, and never silently move required S1 capabilities out.

Each iteration reports actual human time, calls/cost estimates, failures, accepted requirements and next action. Budget overruns pause for owner choice or written RFC scope change, not automatic models/services.

RFC section 19 later candidates: S2 graphical onboarding/terminal localization, redacted notifications/remote approval identity design, external PR evaluation, external-write compensation, cost trends and explicit storage migration; S3 commercial backend/cloud management requires demand evidence. These are outside T019–T028 and will be planned later. Current totals: 11 plan items, 5 pending tasks, 24 planned acceptance groups, not completion percentages.
