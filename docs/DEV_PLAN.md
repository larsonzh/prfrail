# ProofRail 迭代开发计划

> 2026-09-11 T026 已完成：**10 verified、2 unsupported、0 unknown、blocked**。十次追加授权使用 7 次，确认 6 premium requests，取消费用 unknown，剩余 3 次未使用。Job Object 取消通过；工具别名和 shell 网络 allow-all 对照证明 CLI deny 可绕过，当前候选不兼容。T027/AT-23 仍须等待外部强制边界或兼容候选，不自动追加模型调用。详见 [T026 最终结论](validation/t026-agent-runner-contract.md)。

[English](DEV_PLAN_EN.md)

日期：2026-09-08；S1 实现尚未全部完成。输入：[需求](PRODUCT_REQUIREMENTS.md)、[架构](ARCHITECTURE.md)、[契约](CONTRACTS.md)、[安全](SECURITY.md)、[ADR](ADR_REGISTER.md)。规划遵循修订后的 RFC §14/19；新增 S1 最小范围需重新确认预算，不将 S2/S3 增强移入首版。

## 1. 工作方式与约束

Go 1.22 模块兼容基线、纯 Go、CLI/TUI、内容寻址文件存储，具体依赖由 S0 冻结。不存在框架迁移，本计划不套用 Java/.NET 迁移规则。遵循只读源目录、single writer、first-fail-stop、独立批准、无默认 Git 写操作；不存在架构例外。

建议每个开发会话只处理一个任务；大于一次可验证修改的任务继续按子测试拆分，不一次生成全部模块。采用红测→最小实现→原用例→包测试→全门禁→更新双语文档/证据。先运行离线确定性测试，再申请模型调用。日历估算以首次两个任务实测速度更新，不承诺未经测量的完成日期。

WIP=1；每个任务执行前设定本轮人工认可的费用/时长上限，默认不发收费调用；两次无新证据的同类失败暂停升级。最强模型仅用于协议/安全歧义评审，小模型只执行已冻结契约的小切片。所有 Git commit/push 仍需当轮授权，默认只 origin。

交付执行纪律（切片模型、角色与模型白名单、串行流水线、门禁与证据、审查体系、成本上限）以 [交付执行准则](DELIVERY_DIRECTIVE.md) 为权威；本计划的任务在它约束下执行。**自 2026-09-21 起，T027 旧准则 `docs/t027/FLASH_OPERATING_DIRECTIVE*.md` 与 `FLASH_T027_BRIEFING*.md` 已被取代**（仅作历史对照；节号换算见新准则附录 C.1）。

## 2. 迭代与任务

每行包含责任路径（尚未存在的路径是计划创建位置）、依赖、验收。仅 T001-T004 是 S0；T004 是隔离技术探针，不是 S1 生产实现。T005 之前必须 §17 全绿且人工批准。

### P0.1 文档与授权基线（REQ-001/014/017/023/026/028）

- [x] T001 [Plan:P0.1] 评审 `docs/PRODUCT_REQUIREMENTS*.md`、`docs/SECURITY*.md`、`docs/ADR_REGISTER*.md`；记录所有者/安全评审者、预算、支持/签名和影子范围；依赖：本包；验收：§17.1/6/8 签署，依赖许可决策可查；禁止：代用户批准。完成：2026-09-06，所有者批准 S0 规格冻结及 ADR §4 决策单；独立安全结论仍由 T003/§17.3 单独关闭。

T001 状态：`COMPLETE`。本次仅批准 S0 规格冻结并允许进入 T002；不授权新增付费调用、具体 whois 数据访问、Git 操作、发布或 S1 生产编码。精确依赖及独立安全评审仍按后续门禁落实；2026-09-08 批准的简化发布信任策略不要求 S1 签名主体或公钥。

### P0.2 机器契约冻结（REQ-001/004/005/007/008/010/012/013/017/018/020/021/022/024/025/028）

- [x] T002 [Plan:P0.2] 先修订 `docs/RFC-proofrail-unattended-ai-engineering-product.md`，同步 `docs/CONTRACTS*.md`/ADR，建立 `schemas/*.schema.json`；冻结 ADR-002/004 的字段、版本、canonical 向量、状态表、错误与队列；依赖：T001；验收：结构与本地一致性检查完成，独立 fixtures/validator 由 T003 承接；禁止：以草案字段实现生产代码。
- [x] T003 [Plan:P0.2] 建立 `testdata/contracts/{valid,invalid}/`、`tools/contracts/` 独立 validator/语义 fixture oracle，纳入三任务、四 kind、双/三语言、handoff、doc/waiver、各 receipt/manifest；依赖：T002；验收：AT-01 正反例及 RFC §17.2/4/9-12；禁止：让未来候选自己生成 expected 结果。完成：2026-09-07，Ajv 2020 独立编译 29 份 Schema；当前 82 个手写预期 fixture 全部通过，`canonicalize` 独立复算两条 JCS/摘要向量。

T002/T003 状态：`COMPLETE`。已冻结共同版本/ID/路径/JCS/UTF-8 规则，并建立 chain、hook、target、
workspace、state-event、error、adapter-envelope、adapter-receipt、snapshot-manifest、evidence-manifest、
review-receipt、promotion-receipt、handoff-receipt、hook-result、run-manifest、signature-receipt、error-set、ticket-ledger、repair-transaction、harness、toolchain、change-set、verification-report、plan-preview、export-record、authorization-record、effect-record、cost-ledger、lifecycle-record 二十九份 Draft 2020-12 结构 Schema；首批 canonical 向量、单记录状态转换、37 个错误 code/主错误排序与 CLI 退出码、
文件队列 framing/claim/result/dispatch/takeover receipt、快照边界、评审前证据根及 approve/reject/waive
评审结论和原子 promotion reader contract、manual-handoff step 判别分支与归还 receipt、hook 执行事实/门禁判断/策略处置分层、run 有效配置来源与运行绑定、harness/toolchain 注册表、detached Ed25519 signature receipt、失败指纹/ticket ledger/三段修复预算、四阶段 repair transaction、确定性配置合并、change-set、跨事件/跨记录/信任链 checker 报告及 PC-01–PC-06 持久记录已冻结。T003 的独立结构 validator、静态语义 checker、向量 fixture 与正反 golden 已执行；AT-01 的 S0 契约部分及 RFC §17.2/4/9-13 已有机器证据。真实文件、进程、能力与平台事实仍属 T004/S1 runtime gate，整体 S0 仍为 `NOT_READY`。

T001 同时评审 ADR-008 与 PC-01–PC-06 新增工作量；T002 冻结 RFC §19 的授权/预览/导出/副作用/成本/备份记录；T003 已增加对应独立正反黄金样例，覆盖 §17.13。此处只冻结契约，不提前执行 S1 验收。

### P0.3 技术探针（REQ-006/007/009/015/017/021/023/024/027）

- [x] T004 [Plan:P0.3] 在根 `tmp/` 隔离验证 Go/TUI、Windows 原子替换/进程树停止、Linux 路径、IPC/文件队列；结论入 `docs/validation/s0-spikes.md`，删除实验文件；依赖：T002，S1 前还需 T003/T001 全部门禁；验收：可重复命令、实际平台、失败能力明确；禁止：未授权模型付费、原型冒充生产实现。完成：2026-09-07，Windows 11/NTFS 与 Ubuntu 24.04/ext4 原生探针、Bubble Tea v1.3.4 隔离原型及 SessionBridge 40 项无模型测试通过；限制与 fail-close 规则见报告。

T004 状态：`COMPLETE`。技术可行性已验证，但整体 S0 仍为 `NOT_READY`；依赖供应链、bootstrap、CI 发布证据与 S1 生产故障注入仍按 T017/后续任务关闭，本结果不授权 T005。

### P1.1 证据和快照底座（REQ-004/023/024/025/028）

- [x] T005 [Plan:P1.1] 实现 `internal/evidence/{canonical,event,receipt,verify}.go` 与同名 `_test.go`，覆盖 canonical/事件链/对象引用和只读核验；依赖：S0 全通过；验收：AT-04 证据部分；禁止：自由文本 PASS 或未知版本写入。完成：2026-09-07，冻结向量、严格 JSON/JCS、事件投影链、evidence manifest 及内容存储重读核验通过；报告见 `docs/validation/t005-evidence.md`。

T005 状态：`COMPLETE`。结果只覆盖证据层的 AT-04 子集；journal、写入事务、回滚及各崩溃点仍由 T008 验收，不得由本状态推导为通过。

- [x] T006 [Plan:P1.1] 实现 `internal/snapshot/{capture,restore,store,types,reparse_*}.go` 与测试，路径验证/排除/配额/保留及 GC dry-run；依赖：T005；验收：AT-02；禁止：读 Git 历史恢复、自动清理有引用对象。完成：2026-09-07，未提交文件树捕获、CAS 对象存储、保留名/大小写/长路径/软硬链接检测、捕获中变动校验、配额限制、无 Git 恢复及 Windows/Ubuntu 双平台验证通过；报告见 `docs/validation/t006-snapshot.md`。

T006 状态：`COMPLETE`。AT-02 完整验收通过；无 Git 恢复与不可变快照已闭环；工作区写事务与崩溃回滚点由 T008 承接。

### DG-01 独立产品文档治理门禁

- [x] DG-01 明确 ProofRail 独立产品边界，新增 `BUSINESS_WORKFLOWS*.md` 与 `INSTALLATION_PLAN*.md`，同步 RFC、需求、导航、README 和操作手册；完成：2026-09-07。安装载体、目录、签名主体和升级方式保持待决，不生成虚假安装步骤。T 编号保持稳定，DG-01 是 T009 的文档前置门禁，不占用实施任务编号。

### DG-02 分域权威迁移门禁

- [x] DG-02 将原 RFC 定位为项目建议书与历史设计来源，在 `DOCUMENTATION_PLAN*.md` 建立分域权威矩阵，并把 T009 直接依赖的 chain/task/step 转换、事件投影、顺序调度、暂停取消和崩溃恢复迁入 `CONTRACTS*.md` §2.1；完成：2026-09-07。旧章节保留用于来源追踪，冲突必须阻断并修订，不由实现自行选择。

### P1.2 写入与顺序闭环（REQ-002/003/004/012/018/023/024/026）

- [x] T007 [Plan:P1.2] 实现 `internal/guard/{process,lease}.go`、平台文件及测试；证明进程树停机、接管和租约；依赖：T005、T004；验收：AT-03/06；禁止：杀用户进程、只凭 PID 或过期接管。验证：[中文](validation/t007-process-lease.md) / [English](validation/t007-process-lease_EN.md)。
- [x] T008 [Plan:P1.2] 实现 `internal/taskdef/checker.go`、`internal/applier/{apply,journal,recover}.go` 与测试；内存顺序预验→事务→回滚；依赖：T005/T006/T007；验收：AT-04 每崩溃点；禁止：边验边写或跳过断言。验证：[中文](validation/t008-applier.md) / [English](validation/t008-applier_EN.md)。
- [x] T009 [Plan:P1.2] 实现 `internal/chain/{engine,state,recover}.go` 与测试；三任务、四 kind、两变更模式、事件投影/暂停取消，先 fake agent/runner；依赖：T008、DG-01、DG-02；验收：AT-03；禁止：候选作为下一父快照。完成：2026-09-07。14 个切片及 Windows 11/NTFS、Ubuntu 24.04/ext4 原生验证通过。验证：[中文](validation/t009-chain.md) / [English](validation/t009-chain_EN.md)。
- [x] T010 [Plan:P1.2] 实现 `internal/gates/{runner,policy,result}.go` 与测试并接 chain；依赖：T007/T009；验收：AT-06；禁止：不具备能力时弱化网络/资源策略。完成：2026-09-07。12 个切片及 Windows 11/NTFS、Ubuntu 24.04/ext4 原生验证通过；未实现的网络/资源 enforcement 保持 preflight 阻断。验证：[中文](validation/t010-gates.md) / [English](validation/t010-gates_EN.md)。

### P1.3 独立接受与有限修复（REQ-005/014/022/023/024/025/026）

- [x] T011 [Plan:P1.3] 实现 `internal/chain/{review,publish}.go` 与测试，绑定候选/review/receipt，支持拒绝和有效 waiver；依赖：T010；验收：AT-05；禁止：agent 自批、技术通过即 PASSED。完成：2026-09-07。覆盖拒评、自批、过期/错候选 waiver 拒绝，以及有效独立批准后单次发布。验证：[中文](validation/t011-chain.md) / [English](validation/t011-chain_EN.md)。
- [x] T012 [Plan:P1.3] 实现 `internal/tickets/{ledger,budget,fingerprint}.go`、`internal/repair/transaction.go` 与测试；依赖：T011；验收：AT-07；禁止：重置预算、改写正式定义绕过候选。完成：2026-09-07。覆盖相同指纹耗尽、attempt/墙钟/费用耗尽、假修复、陈旧候选与 Promote 中断。验证：[中文](validation/t012-tickets-repair.md) / [English](validation/t012-tickets-repair_EN.md)。

### P1.4 代理和人工交接（REQ-006/007/008/016/021/023/024/025/026）

- [x] T013 [Plan:P1.4] 实现 `internal/adapters/{filequeue,sessbridge}.go` 与测试，核心端口由消费方定义；依赖：T012；验收：AT-08，先离线后授权实机；禁止：修改 SessionBridge、GUI/auto 回退、重复应用。完成：2026-09-07。覆盖 request/claim/result 信封、file-queue 领取与 fencing、幂等冲突防护，以及 sessbridge silent 派发的 accepted/rejected/uncertain 映射。验证：[中文](validation/t013-adapters.md) / [English](validation/t013-adapters_EN.md)。
- [x] T014 [Plan:P1.4] 实现 `internal/chain/handoff.go`、`internal/console/handoff.go` 与测试；依赖：T013；验收：AT-09/10；禁止：秘密经模型、到期自动通过、自动恢复并发写。完成：2026-09-07。覆盖 handoffPolicy 约束、complete/abort/request-agent 收据语义、WAITING_FOR_OPERATOR 状态转换，以及 structured/secret-direct 输入策略与秘密摘要化。补充验证：2026-09-08 在 Ubuntu VM 通过临时代理链路完成远端全量 build/vet/test 与 race（gcc 全仓、clang 关键包）并全部通过。验证：[中文](validation/t014-handoff.md) / [English](validation/t014-handoff_EN.md)。

### P1.5 工程包与可用 CLI/TUI（REQ-009/010/011/013/015/016/018/020/022/027）

- [x] T015 [Plan:P1.5] 建立 `harnesses/{generic,c,go}/`、`internal/taskdef/documentation.go` 与测试，模板 A、impact rule、生成新鲜度、C+Go task；依赖：T014；验收：AT-11；禁止：whois 专名进入核心或交付任意脚本 B。完成：2026-09-08。覆盖文档协同影响规则、模板 A 锁定与生成物新鲜度检查、自批/手改生成物阻断，以及 C+Go 语言作用域任一失败触发同 task 整体恢复。验证：[中文](validation/t015-taskdef-documentation.md) / [English](validation/t015-taskdef-documentation_EN.md)。
- [x] T016 [Plan:P1.5] 完成 `cmd/prfrail/main.go`、`internal/console/`、配置解析/解释与测试，同步 `README.md`/`docs/OPERATIONS*.md`；依赖：T015；验收：AT-12；禁止：视图直接改状态、占位命令报告成功。完成：2026-09-08。覆盖无 IDE `init/validate/config explain/run/report` 命令、稳定 `--json` 输出、配置搜索顺序（`--chain`→`proofrail.chain.json`→`proofrail.json`）、noop-only 本地运行闭环与执行型 step fail-close 非零退出。验证：[中文](validation/t016-cli-console.md) / [English](validation/t016-cli-console_EN.md)。

### P1.6 可信发布与影子验收（REQ-001/002/006/011/014/015/019/024/025/028）

- [x] T017 [Plan:P1.6] 建立 `.github/workflows/ci.yml`、`testdata/selfhost/`、`docs/validation/s1-selfhost.md`；固定工具链与 Actions 完整 SHA、两代隔离、较早可信 verifier commit、外部 oracle、原生平台、固定 candidate commit、SHA256SUMS、SBOM/许可证检查；依赖：T024 和独立 bootstrap 批准；验收：AT-13/14；禁止：候选构建/覆盖 verifier 或 seed、把校验和冒充身份认证、未授权发布。签名/attestation 属后续增强，不阻断 S1。完成：2026-09-09。baseline `12fa05d` 的 push CI run `34253473081` 在 Windows/Linux 均失败后，CI 修复版经 DeepSeek V4 Pro 与 GPT-5.3 Codex 双主审 `PASS FOR BASELINE REVIEW`，并以 `18c9340c60deb30049554491f8905df51d0a3180` 建立新的 verifier baseline；push CI run [`34268076912`](https://github.com/larsonzh/prfrail/actions/runs/34268076912) 的 Ubuntu/Windows job 均成功。不同的 docs-only candidate `1e7af676e8e84028c7ecfef1ccde91213738c20c` 随后由维护者从 `main` 手工 dispatch；[run `34277671704`](https://github.com/larsonzh/prfrail/actions/runs/34277671704) 的 test、candidate-build、candidate-probe、selfhost Windows/Linux 8 个 job 全部成功，6 个 artifact 的名称、大小和 GitHub archive digest 已归档。验证：[中文](validation/s1-selfhost.md) / [English](validation/s1-selfhost_EN.md)；初始 Codex 主审：[中文](validation/t017-baseline-review.md) / [English](validation/t017-baseline-review_EN.md)；初始 DeepSeek 主审：[中文](validation/t017-deepseek-baseline-review.md) / [English](validation/t017-deepseek-baseline-review_EN.md)；CI 修复版 DeepSeek 复审：[中文](validation/t017-ci-remediation-deepseek-review.md) / [English](validation/t017-ci-remediation-deepseek-review_EN.md)；CI 修复版 Codex 复审：[中文](validation/t017-ci-remediation-codex-review.md) / [English](validation/t017-ci-remediation-codex-review_EN.md)。
- [ ] T018 [Plan:P1.6] 建立 `examples/{whois-shadow,go-minimal}/` 与 `docs/validation/s1-exit.md`；只读固定输入比较结果/失败分类，汇总全部 AT 和剩余风险；依赖：T017、T025、T026、T027、T028 及 whois 输入授权；验收：AT-15/22/23/24、RFC §14 S1 全部 exit；禁止：切换 whois 生产流程或“部分通过”冒充 S1 完成。

T018 状态：`IN_PROGRESS / BLOCKED`。2026-09-09 已在单轮只读授权内导出脱敏 whois 固定夹具并通过 9/9 结果对齐及输入/结果/失败分类漂移负例；generic/C/Go 三个最小 CLI 闭环与 Windows 候选便携包的首次运行、升级、回滚、保留数据卸载演练均通过。产品所有者已批准手工解压便携 ZIP、不修改 PATH 的安装模型，双语教程、支持矩阵和发行说明草案已建立。2026-09-10 T025/AT-22 已完成。AT-01–AT-24 与剩余门禁见 [S1 exit 报告](validation/s1-exit.md)；T026/T027/AT-23、T028/AT-24、最终 ZIP 绑定/复验、EOL 通知方式和产品所有者 S1 exit 批准仍缺失，因此不勾选 T018。

### P1.7 产品闭环与生命周期（在 P1.6 发布验收前执行）

编号保持稳定，实际依赖顺序为 T016 → T019–T024 → T017 → T025 → T026 → T027 → T028 → T018。以下在 S0 授权后执行；T019–T024 已完成。每个任务可再按一个失败反例拆小，不一次生成完整功能。

- [x] T019 [Plan:P1.7] PC-01：实现 `internal/taskdef/preview.go`、`internal/console/preview.go` 与测试、无 AI 示例；依赖：T016；验收：AT-16；禁止：预览启动命令/网络/模型/自动安装。完成：2026-09-08。覆盖离线只读 preview record 生成、unknown 可见、零调用计数与 JSON/文本同事实输出。验证：[中文](validation/t019-preview.md) / [English](validation/t019-preview_EN.md)。
- [x] T020 [Plan:P1.7] PC-02：实现 `internal/snapshot/export.go`、`internal/evidence/delivery.go` 与测试；依赖：T019、T006/T011；验收：AT-17；禁止：导出候选冒充接受、覆盖目标、源树/Git 写入。完成：2026-09-08。覆盖 accepted hash 绑定、目标必须 absent、源/run/store 重叠阻断、secret 路径阻断、缺对象/篡改阻断、写中断无 completed record。验证：[中文](validation/t020-export.md) / [English](validation/t020-export_EN.md)。
- [x] T021 [Plan:P1.7] PC-03：实现 `internal/chain/authorization.go`、`internal/console/approvals.go` 与测试，接 guard 停机；依赖：T020、T007/T011；验收：AT-18；禁止：自授权、硬门禁 waiver、过期自动批准。完成：2026-09-08。覆盖授权/撤销绑定摘要、撤销/过期/错摘要阻断下一动作与接受、在途停止失败保持暂停并可重启可见。验证：[中文](validation/t021-approvals.md) / [English](validation/t021-approvals_EN.md)。
- [x] T022 [Plan:P1.7] PC-04：实现 `internal/gates/effects.go`、`internal/evidence/diagnostics.go` 和 chain 恢复计划测试；依赖：T021、T010；验收：AT-19；禁止：外部写 runner、未知副作用盲重试、诊断删锁/日志。完成：2026-09-08。覆盖副作用分类与准入阻断、unknown/外部写 fail-close、只读脱敏诊断与恢复动作。验证：[中文](validation/t022-effects-diagnostics.md) / [English](validation/t022-effects-diagnostics_EN.md)。
- [x] T023 [Plan:P1.7] PC-05：实现 `internal/tickets/cost.go`、`internal/adapters/cost_usage.go`、`internal/console/cost.go` 与测试；依赖：T022、T012/T013；验收：AT-20；禁止：重启释放未知费用、跨 run 绕上限、默认遥测。完成：2026-09-08。覆盖调用前持久 reservation、settlement 去重、unknown hold 重启保留、跨 run 共享上限阻断与本地无默认遥测成本报告。验证：[中文](validation/t023-cost-ledger.md) / [English](validation/t023-cost-ledger_EN.md)。
- [x] T024 [Plan:P1.7] PC-06：实现 `internal/snapshot/backup.go`、`internal/evidence/disposition.go`、生命周期控制/测试及发布清单工具；依赖：T023；验收：AT-21、SBOM/支持说明；禁止：未知格式迁移、默认删证据/共享工具、擅自签名发布。完成：2026-09-08。覆盖完整对象/事件/引用闭包备份、新 store 复原核验、源 store 不变、活动写者/未知版本/缺闭包阻断、默认保留证据与共享工具、显式删除授权/保留冲突决定，以及离线撤销状态 unknown。验证：[中文](validation/t024-lifecycle.md) / [English](validation/t024-lifecycle_EN.md)。

### P1.8 AI 执行、黑箱候选与操作员闭环（REQ-006/007/008/009/016/021/023/024/025/026/027）

- [x] T025 [Plan:P1.8] 冻结 `operator-interaction` Schema、正反 golden、哈希与重放契约；实现 TUI 待处理交互收件箱/结构化响应、控制权归还和重启恢复；依赖：T014、T016、T021、T024；验收：AT-22；禁止：自由文本触发状态转换、把聊天或 Agent history 当权威状态、依赖 `@sbr-review`、绕过 authorization/handoff、秘密进入模型或证据。完成：2026-09-10。覆盖严格 wire/hash、追加式 JSONL 重放、聚焦无 ANSI 收件箱、结构化响应、Engine 单写者 open/resume、错误绑定/重复/篡改拒绝，以及事件部分写入失败后的 fail-close 收敛。完整统一 TUI 与真实 Agent session 续跑仍属于后续切片。验证：[中文](validation/t025-operator-interaction.md) / [English](validation/t025-operator-interaction_EN.md)。
- [x] T026 [Plan:P1.8] 冻结 `AgentRunner` request/capability/event/completion Schema、canonical/hash、正反 golden、去重/重放规则和 adapter 能力矩阵；对至少一个候选 CLI Agent 固定版本/摘要并实测非交互、cwd、事件/完整日志、session create/resume、取消/进程树停止、工具/网络/权限、用量与确认行为；依赖：T025；验收：AT-23 契约/能力部分；禁止：生产 adapter、付费调用无授权、以帮助文本或提示词代替运行证据。状态：`COMPLETE / CANDIDATE INCOMPATIBLE`。四类基础 Schema、96 个契约用例、RFC 8785 域分隔摘要、严格绑定/重放冲突与 GitHub Copilot CLI 1.0.83 固定摘要探针均已完成。最终矩阵为 10 verified、2 unsupported、0 unknown：受管 Job Object 验证取消；allow-all 对照证明 `gci` 别名绕过工具 deny、shell `Invoke-WebRequest` 绕过 URL deny。十次追加调用授权使用 7 次，确认 6 premium requests 加取消 unknown，3 次未使用。候选整体 blocked，AT-23 未通过。后续已另行冻结 `agent-runner-enforcement` Schema、绑定/freshness/组合准入与 fail-closed 测试，使当前契约门禁增至 35 份 Schema、98 个用例；这不表示真实外部边界存在。验证：[中文](validation/t026-agent-runner-contract.md) / [English](validation/t026-agent-runner-contract_EN.md)。
- [ ] T027 [Plan:P1.8] 实现核心拥有的 `AgentRunner` port、至少一个固定 CLI Agent adapter、隔离 workspace 启停/恢复、事件/日志/用量/manifest/diff 证据和退出后独立 gates/review；依赖：T026、T007、T010、T012；验收：AT-23；禁止：写源/store/策略/接受记录、自行 commit/push/publish、Agent 退出 0 直接 PASS、未知状态盲重试。状态：`BLOCKED / NOT IMPLEMENTED`。首个产品切片仅面向 `windows/amd64`，但端口与 wire 必须保留平台无关；启动前检测并规范化 `os`/`arch`，精确匹配 capability/platform/enforcement 后才选择平台策略。Windows 版本完成后，再在 Ubuntu 24.04 VM 上独立开发和验证 `linux/amd64`；macOS、ARM 及其他 OS/架构组合仅预留扩展点，暂不排期，可在后续使用原生环境或 GitHub Actions runner 独立探测。任何平台均不得继承 Windows 能力结论。外部 enforcement wire 与组合 preflight 已冻结；B2 已完成**外部 OS 强制边界**的实测与证据归档（零网络能力 AppContainer + 宿主侧白名单代理，见[验证报告](validation/B2-EXTERNAL-ENFORCEMENT.md)），但该边界**尚未接入产品**（容器内工具执行路径与 AgentRunner 集成仍未实现）；固定候选仍不得作为 AgentRunner 启动。前置切片已冻结三通道 `ai-availability` Schema、RFC 8785 摘要、重放和 profile/channel/freshness/request-budget admission，使统一契约门禁增至 36 份 Schema、100 个用例；非秘密 ProviderProfile 端口、严格持久配置与脱敏 explain、Windows Credential Manager backend 及无回显 `secret set/status/delete` CLI 已实现，静态 validate/explain/preview 经测试不读取 SecretStore。`agent-runner-cli` 现已有受管进程 live probe 与 `ai check` 入口，固定精确响应、零工具、单请求、结构化 result/usage、环境白名单、凭据清零和脱敏证据均由离线测试覆盖；所有 verdict 可用 `--out` 以 canonical JSON 不可覆盖地发布，并由不读取 SecretStore、不调用模型的 `ai verify` 按配置摘要、通道、新鲜度和请求预算重放准入。尚未执行真实模型请求，因此没有实际 availability verdict。Linux SecretStore 与 silent/visible 真实探针仍未实现，T027 enforcement 和 AgentRunner 本体仍阻断。
- [ ] T028 [Plan:P1.8] 实现 `supervised-black-box` 显式模式：版本化风险/责任提示与绑定确认、隔离 workspace、SessionBridge visible 投递、人工监督和显式归还、后置全量扫描、独立 gates/review 及 reduced/unknown 报告；依赖：T025、T026、T014、T022；验收：AT-24；禁止：自动从 AgentRunner 降级、visible 回执冒充完成、风险确认豁免安全门禁、未知费用/进程/副作用显示为安全。状态：`PLANNED / NOT IMPLEMENTED`。

T027 前置状态最终更新（2026-09-14）：本段取代本节此前两段 2026-09-13 前置说明。获批的离线前置切片现已完成：`allowedEffects` 到 grant `effectClasses` 的 v1 mapping 已冻结并实现，`local-process` 与 `workspace-write` 均映射到 `local-discardable`；request 绑定 mapping version/hash，Schema、canonical vector、semantic validator 与 Go composite admission 对缺失/未知/篡改绑定、未知 operation、grant class 覆盖不足 fail-closed，并在 replay identity 消费或 dispatch 前拒绝，grant class 超集可通过。`AgentRunnerReplayStore` 现离线处理 orphan completion/corruption、bounded reread convergence，以及同一 root 上跨 store 并发 request 的 single first-dispatch（其余请求阻断）；不会用 orphan completion 修复缺失 request，收敛失败保持 uncertain/fail-closed。Windows publication durability 为 `unproven`（**级别与前提必须一起读**：等级一律取 `proven`/`disproven`/`unresolved`，前提 P = 进程级崩溃、无断电、单主机 Windows 11 / NTFS 本卷；本句的 `unproven` 正是在该前提下维持）：任何可能返回 `first-dispatch` 的写入在 pre-write 阶段拒绝，不创建 request record。Unix proven path 只有在已有稳定绝对 replay root 且 parent directory fsync 成功时成立；本次 verifier 在 Windows 执行，没有 Unix 原生 crash-injection evidence。Linux/Darwin 结果仅为 cross-compile，不提升 Unix runtime/crash 状态。replay 尚未接入 `AgentRunnerPort` 或 Engine；terminal receipt（包括 `uncertain`）只证明终局收据存在/阻止重复 dispatch，不是 task `PASS`，也不是 AT-23 证据。最终 verifier 在 Windows 上报告 Node contract 4/4、replay 32/32、adapters、build/vet/test 全部通过；Linux/Darwin cross-compile 通过；未调用真实 CLI、未访问网络、未读取 SecretStore。T027 仍为 `BLOCKED / NOT IMPLEMENTED`，AT-23 未通过。验证：[本次中文报告](validation/t027-replay-convergence.md) / [English](validation/t027-replay-convergence_EN.md)；effect mapping 前置证据仍见[中文](validation/t027-agent-runner-effect-mapping.md) / [English](validation/t027-agent-runner-effect-mapping_EN.md)。

T027 前置状态更新（2026-09-14，A0 replay-root composition）：在上一段基础上，A0 已完成并经 V4 Pro 预审与 Codex 独立终审/收尾复审 PASS。生产 replay store 只由 `NewAgentRunnerReplayStoreForRun(runRoot, runID)` 从 durable run root 推导 `<runRoot>/agent-runner-replay`，不再接受任意 caller root（任意绝对路径构造器改为包内非导出）；run root 必须已存在且含常规文件 `events/state-events.jsonl` 标记；加载 R/C 的 `runId` 不符按 corruption、写入不符按 root conflict fail-closed；写者根/保护根按双向不相交与包含链规则拒绝，覆盖 8.3 短名、大小写与 `\\?\` 别名；symlink/reparse 组件检查在构造期与每次读写加锁后复检，残余 TOCTOU 窗口已写入契约；`ownership.json` 以 no-replace 单调发布、不依赖发布耐久 `proven`，目录移动/复制或所有权不符 fail-closed，构造失败（含 bootstrap 与低层构造）不遗留 ownership 元数据。验证：Windows 原生 `gofmt`/`go build`/`go vet`/`go test -count=1 ./...`、聚焦 replay 测试、Node contract 4/4 与全部平台反例测试（junction 替换、大小写/短名/`\\?\` 别名、运行期换链、并发首开）通过；Unix 侧测试已由 GitHub Actions Ubuntu 原生回归确认（运行 34799747443，commit `7ad1ac0`）。A0 未引入真实 dispatch，replay 仍未接入 `AgentRunnerPort`/Engine；T027 仍为 `BLOCKED / NOT IMPLEMENTED`，AT-23 未通过。验证：[中文](validation/t027-replay-root-composition.md) / [English](validation/t027-replay-root-composition_EN.md)。

T027 前置状态更新（2026-09-14，A1 pure admission）：`AgentRunnerCompositeAdmission` 已收敛为纯 preflight 与不可变绑定，删除 `RequestIndex` 内存消费、`ErrAgentRunnerRequestReplayBlocked` 与 `AgentRunnerRequestIndex` 类型；同键幂等/冲突、first-dispatch 与 replay 身份消费保留给耐久 replay store 与 dispatch 路径，边界已写入 CONTRACTS 双语、`AgentRunnerAdmission` 接口文档与实现结构文档。聚焦测试覆盖同一 admission 重复与并发通过、失败后无消耗状态、同 requestId 异 digest 归属 dispatch/store 及输入不变性断言。V4 Pro 前置分析与预审（补做）及 Codex 终审/整改复审均 PASS；R1 契约补句（admission 非锁、dispatch 须在发布 R 后重确认授权与预算）与 R2–R4 已整改；Linux CI 新增 `-race` 门禁并同步工作流冻结契约；已提交 `452d173` 并推送，GitHub Actions Ubuntu（含 `Race` 步骤）/Windows 全绿（run 34809403487）。T027 仍为 `BLOCKED / NOT IMPLEMENTED`，AT-23 未通过。验证：[中文](validation/t027-pure-admission.md) / [English](validation/t027-pure-admission_EN.md)。

T027 前置状态更新（2026-09-14，A2 dispatch and launch ambiguity）：在上一段基础上，A2 已完成，V4 Pro 预审与 Codex 独立终审（含测试反例逐条审计）两轮均无 High/Medium 遗留，全部整改已闭环。`AgentRunnerReplayDispatcher`（实现 `chain.AgentRunnerPort`）先发布 R，再以 `AgentRunnerLaunchReconfirmer` 窄重确认（授权未撤销、预算 reservation 仍 outstanding；失败 fail-closed：R 保留、无回执、不 spawn、不自动重试），随后用两文件 store-local 回执协议治理崩溃歧义：spawn 前 `launches/intent.<requestId>.jsonl`（no-replace 单赢家、有界重读收敛、unproven 平台 pre-write 拒绝）、spawn 后 `launches/identity.<launchId>.jsonl`（绑定意图摘要与不透明进程身份），不变式为“无意图⇒从未 spawn、身份缺失⇒unproven、两者都不允许盲目重启”；R-only、已有意图、意图+身份、R+C 终态一律阻断且绝不 relaunch，唯一允许的同进程重试（端口契约性未 spawn 失败）本切片选择保守地不自动执行。U1 抽取完成且 A1 行为守护不变；`launches/` 已纳入生产构造器、bootstrap、运行期路径安全复检与所有权发布前置枚举。Windows 原生门禁（gofmt/build/vet/test 全量 + 聚焦）与并发/碰撞/换链/撤销/启动失败反例全通过；已提交 `aeb504e` 并推送，GitHub Actions Ubuntu（含 `Race` 步骤）/Windows 全绿（run 34844216048），Unix 专项与 `-race` 已由该运行实际执行。T027 仍为 `BLOCKED / NOT IMPLEMENTED`，AT-23 未通过。验证：[中文](validation/t027-dispatch-launch-ambiguity.md) / [English](validation/t027-dispatch-launch-ambiguity_EN.md)。

T027 前置状态更新（2026-09-14，A3 terminal publication and settlement）：在上一段基础上，A3 已完成，V4 Pro 前置分析与实现预审、Codex 独立终审（含测试反例审计）均无 High 遗留且全部整改闭环。`AgentRunnerTerminalPublisher` 以 store-local 二文件协议把 completion 发布与费用结算绑定为四阶段链：no-replace 发布 `terminals/terminal-intent.<requestId>.jsonl`（内嵌完整 C 记录与确定性结算计划，幂等键 `settle-<20hex>` 绑定 (requestId, C.recordHash)，证据须含 C/R 摘要）→ 账本 `Settle`（按 idempotencyKey 去重、同 reservation 异键拒绝）→ `RecordCompletion` 发布 C → `terminals/terminal-closure.<requestId>.jsonl` 完成链；崩溃恢复只依据 store-local 记录（intent 为唯一恢复源，账本只作强制者），恢复=幂等补写自有槽位而非接管。拒绝矩阵：C 无意图=orphan fail-closed、reservation 异键占用不发布 C、charged>reserved/证据非法/负数观测在写意图前拒绝、不同 outcome 同 request 冲突；unknown 结算保留占用（`UnknownHoldReservations`/共享上限可观察，不得复用、不得 relaunch），不伪造完成；unproven 平台在任何结算之前拒绝新发布，其中完整链仅允许严格无写重放。Windows 原生门禁全绿；已提交 `c6f8585` 并推送，GitHub Actions Ubuntu（含 `Race` 步骤）/Windows 全绿（run 34900979883），Unix 专项与 `-race` 已由该运行实际执行。T027 仍为 `BLOCKED / NOT IMPLEMENTED`，AT-23 未通过。验证：[中文](validation/t027-terminal-publication.md) / [English](validation/t027-terminal-publication_EN.md)。

T027 前置状态更新（2026-09-15，A4 chain terminal routing and resume）：在上一段基础上，A4 已完成，V4 Pro 前置分析与实现预审、Codex 独立终审（含测试反例审计）全部整改闭环（预审 1 Medium 修后复审无 Medium+；终审 1 Medium 修后 Codex 复审 PASS）。新 step 状态 `TERMINAL_PENDING`（协议先行：CONTRACTS 双语 §2.1 转移行与 §7 “chain terminal 路由”段、schema stepTransition、126 个契约夹具）表示“已派发、等终局”，非可停留调度态；AgentRunner 步骤派发成功后只写入该状态并返回 `ErrAwaitingAgentRunnerTerminal`，不再走 step-passed。终局回执必须经 adapters 单向翻译（`ToChainAgentRunnerTerminal`/`LoadChainAgentRunnerTerminal`，绑定 request→terminal-intent→closure 全部字段含结算槽位，chain 不读 replay store）并经核心 `SubmitAgentRunnerTerminal` 显式路由：completed 只允许 step `TERMINAL_PENDING→PASSED`（任务仍走 freeze/gates/review/promotion，chain 仅在全部任务接受后 COMPLETED），failed 落 task/chain `FAILED`，uncertain 落 task `FAILED` 并保持 chain `PAUSED`（`Run` 拒绝盲目恢复），cancelled 先归档停机证据再写终态，operator-action-required 写 task/step `WAITING_FOR_OPERATOR` 并同步 `RUNNING→PAUSED` 后交给 T025 机器（Open 侧加法式接受，经典路径零变化；账本仍为交互排他所有者）。同一请求重复终局幂等收敛（含 step 已写/task 未写与 task 已写/chain 未写两种部分写入窗口），不同终局为冲突且零写；resume 必须绑定 `priorSessionId` 与 `priorCompletionHash` 且后者须出现于该 step 事件证据历史，无法证明时零写拒绝；`TERMINAL_PENDING` 同时进入 recover 不确定清单与 cancel 清理。Windows 原生门禁（gofmt/build/vet/test 全量 + 聚焦）与契约夹具门禁全绿；已提交 `a21335d` 并推送，GitHub Actions Ubuntu（含 Race 步骤与 Contract fixtures 步骤）/Windows 全绿（run 34920155025），Unix 专项与 `-race` 已由该运行实际执行。T027 仍为 `BLOCKED / NOT IMPLEMENTED`，AT-23 未通过。验证：[中文](validation/t027-terminal-routing.md) / [English](validation/t027-terminal-routing_EN.md)。

T027 前置状态更新（2026-09-15，A5 chain postflight acceptance）：在上一段基础上，A5 已完成，V4 Pro 预审两轮（1 High + 1 Medium + 3 Low 全部整改、复审清零）与 Codex 独立终审三轮（High 1 + Medium 2 + 后续新 Medium 1 全部整改，第三轮 `PASS`）全闭环。新增 chain 自有 postflight 门 `internal/chain/postflight.go`：completed 终局必须随附五枚相互不同且与请求/完成摘要互异的冻结事实摘要（manifest/diff/log/usage/process-stop），路由证据扩展为固定位置布局（前缀 2..6，最短 7 枚）供跨重启重建；全部 step 通过后、写 `REVIEW_PENDING` 之前，chain 要求必填端口 `PostflightPort` 独立重推（重捕 manifest 并与父快照算 diff、范围/秘密/文件类型与副作用检查）并逐项对账，**只有 pass（且携带全部五枚事实摘要）才写 `REVIEW_PENDING`**，随后才走既有 freeze/gates/review/completed promotion；exit 0、completed receipt 或任何 adapter 记录都不再可能单独产生 task PASSED。失败路由：拒绝 → task `REPAIR_PENDING` + chain `PAUSED`（协议先行新增 `STEPS_RUNNING→REPAIR_PENDING`，128 个夹具）；uncertain → task `FAILED` + chain `PAUSED` 且禁止重试；端口错误 → `postflight-failed`；决策非法 → `postflight-invalid`；事实缺失/多步归属歧义/证据过短 → `postflight-facts-missing`。幂等与崩溃窗口：`REVIEW_PENDING` 持久化后门不重跑，门通过后写 review 前崩溃则重入重跑一次并收敛；completed 重放同事实收敛、异事实或证据不可重建按 conflict 零写入拒绝。边界写入契约：无变更执行的 manifest 摘要可与父快照摘要合法相等，该判别只属于端口而 chain 不以摘要关系预拒；A5 前的 completed 路由证据与无门痕迹的 `REVIEW_PENDING` 均零写入拒绝（新增 `ErrUnqualifiedReview`），只能按新 attempt 重执行。Windows 原生门禁（gofmt/build/vet/test 全量 + 聚焦 + 5 项变异检验）与契约夹具门禁全绿；已提交 `b8bad47` 并推送，GitHub Actions Ubuntu（含 Race 步骤与 Contract fixtures 步骤）/Windows 全绿（run 34950623175）。报告：[中文](validation/t027-postflight-acceptance.md) / [English](validation/t027-postflight-acceptance_EN.md)。T027 仍为 `BLOCKED / NOT IMPLEMENTED`，AT-23 未通过。

T027 前置状态更新（2026-09-15，A6 offline pinned CLI run）：在上一段基础上，A6 已完成（已提交 `c2d2819` + 修复提交 `6db5a10`，均已推送 `origin/main`）。新增离线外部运行栈：`internal/adapters/agent_runner_process_registry.go`（live 句柄注册表、阶段阶梯、不透明 `ProcessID`、与 console 同一身份镜像 `managed-process.identity.json`、run root 唯一启动槽 `managed-process.launch.slot`）、`agent_runner_timeout.go`（看门狗**独占唯一截止时间**，超时与取消同刻到达由有界落定窗口收敛为取消获胜）、`agent_runner_evidence.go`（日志原始字节 + 完整性标记、events 严格 JSON(L)、usage 封闭形状、pre/post manifest 与 diff 由端口注入、`FrozenFacts` 缺口门槛）、`agent_runner_pinned_cli.go`（版本预检经 `guard` → 启动槽独占取得 → 单次 spawn 且 `PROOFRAIL_EVIDENCE_DIR` 指向本 run 证据目录 → 身份镜像 → 注册；停机按所有权解析身份；判负后等待运行侧完成对账）、`agent_runner_run.go`（编排与终局映射、结算映射、自然退出后的进程树复验），以及确定性离线 CLI `tools/agent-stub`。协议先行：CONTRACTS 双语 §7 新增 A6 段（工件布局与五枚事实口径、缺口不得以 completed 或空字段表达、非零自然退出优先于缺口归类、`exitCode` 仅自然退出、有界看门狗与停机验证、单胜者落定窗口、身份镜像零新机制、run root 单 launch 的独占创建与新鲜度与令牌所有权、自然退出与被外部停机均需复验子进程集合、版本预检、所有权边界、全程离线）。关键语义：只有“四个 Complete + 五枚摘要 + 已验证消失的进程树”才可 completed；operator 请求压过成功与超时；任何缺口、未归类停机或未对账均为 uncertain 且不携带事实；结算仅 completed+usage 完整时为 observed。审查：V4 Pro 预审 7 轮全部闭环（High 2 + Medium 10 量级，最终 `PASS WITH FIXES` 无阻塞）；④ 按用户同轮授权以低成本组合执行试点——Claude Haiku 4.5 机械扫描 0 条（自标可疑）、MAI-Code-1.1-Flash 独立审出 High 1 + Medium 1，**两项均经实际执行（新增反例 + 200 轮同刻竞态实验）确认为真**并逐项整改，复审 `RE-REVIEW: PASS`（试点阶段 Codex 兜底 0 次）；④ 之后用户同轮授权**追加**一次 Codex 盲审（GPT-5.3-Codex），其后另有两次复审（整改闭环 + 主控改写测试增量的“禁止自审”复核，合计 3 次、超出试点文档 §6 上限并已登记）；盲审**另发现组合漏掉的 High 1**（`refuseForeignSlot` 在“除 NotExist 外的读错误”与“owner 为空”上 fail-open，已改三分支 fail-closed）与 Medium 1（声称“两套停机机制”，主控裁定为“一套 guard 身份机制、两个入口”，契约措辞双语收紧），复审 `RE-REVIEW: PASS`——试点残差由此可量：本片组合漏 1 High、③ 预审漏项 0，故记录为**组合可作常规切片的低成本补充扫描层、硬门切片 A7/B2/B3/B4 仍保留 Codex 终审**（最终是否推广由用户决定）。52 项变异检验全部 RED / SURVIVED 0（第一部分 48 项 + Codex 轮 M52–M55，测试两次加强后均重跑，还原均经 SHA-256 校验），原生实验 E1–E8（真实进程；仅 Windows 本机验证，`a6native` 不进 CI）全绿，Windows 门禁（gofmt/build/vet/test 全量）与契约夹具（128）门禁全绿。报告：[中文](validation/t027-offline-process.md) / [English](validation/t027-offline-process_EN.md)。CI 证据：首跑 run `35074301616` 的 Ubuntu 腿 Test 失败——12 个 pinned-CLI 用例暴露 **Linux 僵尸语义**缺陷（存活判定把已退出但父进程尚未回收的僵尸读为存活，使按持久身份停机的路径在 grace 用尽后误报 `uncertain`；Windows 无僵尸概念故该腿全绿）；修复 `internal/guard/process_linux.go`（`/proc` 同时解析 state 与 pgrp：`Z`/`X`/`x` 视为不在运行；进程组在信号 0 仍成功时扫 `/proc` 判定是否存在非终态成员，EPERM 保持保守语义）并新增 Linux-only 回归测试 `TestLinuxStopProvesAnUnreapedTerminatedChild`（子进程故意不回收）；修复提交 `6db5a10` 推送后 run `35076045445` **全绿**——Ubuntu 实际执行 `Race` 与 `Contract fixtures` 步骤，Windows 依 `runner.os` 条件跳过这两步。本地门禁补强：新增 `GOOS=linux`（`CGO_ENABLED=0`）交叉 `build`/`vet`。T027 仍为 `BLOCKED / NOT IMPLEMENTED`，AT-23 未通过。

T027 前置状态更新（2026-09-16，A7 publication durability candidates and their falsification）：在上一段基础上，A7 已完成（本轮为**离线反证与 API 可观测性**切片，不改变生产发布原语与冻结契约句）。**级别与前提必须一起读**：等级一律取 `proven` / `disproven` / `unresolved`（`observed`/`falsified`/`disproven-by-policy` 仅作证据注释，不作为等级）；前提 **P = 进程级崩溃、无断电、单主机 Windows 11 / NTFS 本卷**。在该前提下：可见性与仲裁层 `proven`（P1–P5，构造性论证 + 可复现观测）；断电耐久 `unresolved`（U1–U5，交 B3）；候选级 D1–D6 是被证伪的**断言**集合（一律 `disproven`；候选 C2/C3 的断电耐久仍 `unresolved` 并移交 B3；C1 为现行基线；C5 marker 作为耐久机制 `disproven`，C6 待契约修订）；**Windows 发布耐久保持 `unproven` 且 first-dispatch 仍被拒绝**，T027 仍为 `BLOCKED / NOT IMPLEMENTED`，AT-23 未通过。生产侧仅新增零语义测试接缝 `replayStorePublishStageHook`（默认 nil，不读环境变量、不写盘、不改默认行为），用于在统一 no-replace 发布原语的五个阶段（`temp-written`/`temp-synced`/`temp-closed`/`record-linked`/`parent-synced`）插入崩溃点。实测要点：请求矩阵在真 kill 下前 3 阶段 `absent`×5、后 2 阶段 `dispatched-unknown`×5，且 CP4 与 CP5 逐键等价（⇒ 耐久步是否生效**不可由 kill 判定**，这是任一候选都不能升 `proven` 的原因）；完成记录矩阵在 `completions/` 路径被杀时前 3 阶段 `dispatched-unknown`（C 不可见、R 存活、零孤儿）、后 2 阶段 `terminal-receipt-present`；进程内 8 路真并发恰一 `first-dispatch`（被杀的赢家仍 `dispatched-unknown` 且再派发被 `unknown-block` 拒绝）；双进程同轮并发以**双向就绪栅栏**（两子进程各自 ready，父进程确认双 ready 后只放行一次）在 5 轮中均恰一 first-dispatch（并发尝试，**非**跨进程强同步已证）；E4 目录句柄 `FlushFileBuffers` 探针推翻了本切片初始假设（读写/只写句柄在无提权下返回成功；只读句柄 `Access is denied`），故 C3 与 C2 并列为 B3 注入候选，但“目录项是否真已耐久”仍 `unresolved`；E6 `MoveFileEx(MOVEFILE_WRITE_THROUGH)` 无提权发布成功、目标已存在（有无 open reader 均然）以 `fs.ErrExist` **硬断言**拒绝（未隔离的因果不写入报告）；E7 两阶段 marker 不引入任何耐久等级变化也不产生新状态（作为耐久机制 `disproven`）；E8 `.tmp` 殘留对可见性/仲裁完全惰性但会累积（**本切片刻意不修**，登记为待办）。审查：V4 Pro 前置分析 + 预审 `PASS WITH FIXES` + 复审两轮至 `RE-REVIEW: PASS`；③.5 低成本组合独立扫描两轮至 `INDEPENDENT SCAN: PASS`；④ Codex 独立终审逐轮整改闭环（**轮次与每轮结论以验证报告 §9 为单一权威**，本节不自行枚举轮数；每轮发现均在下一轮开始前整改，闭环状态以该报告 §9 为准）。已提交 `cc9adf4`（生产接缝 + 4 个测试文件）与 `dee0066`（报告/ADR-013/DEV_PLAN）并推送 `origin/main`；GitHub Actions 全绿（run `35118531930`，约 2m04s）：Ubuntu 腿执行 Race 步骤（`CGO_ENABLED=1 go test -race ./internal/adapters/... ./internal/chain/...`）与 Contract fixtures 步骤（4/4），Windows 腿按 `runner.os` 跳过这两步，两腿 Build/Vet/Test 均 success —— 即**进套件的 5 项 A7 测试已在 Linux `-race` 下实际执行**。边界：**仅** `a7native` 实验不进 CI（需在 Windows 主机手工重跑，本报告证据为单主机）；E4/E6/E9/E10 仅 Windows；U1–U4（真断电 + 异物理设备逐轮崩溃日志 + 重启清单）交 B3。验证：[中文](validation/t027-windows-publication-durability.md) / [English](validation/t027-windows-publication-durability_EN.md)。T027 仍为 `BLOCKED / NOT IMPLEMENTED`，AT-23 未通过。

T027 前置状态更新（2026-09-17，B1 candidate live capability + availability discovery）：在上一段基础上，B1 已完成（用户同轮授予 **10 次探针额度**，其中 1–2 次允许用于“是否存在能覆盖 T026 两项 unsupported 的新版本候选”）。本片**无生产改动**，交付物为真实探针证据 + 双语报告。**结论：在已评估范围（GitHub 托管 Copilot CLI 稳定通道 1.0.83 与 1.0.85）内无可接受候选**——T026 的两项 unsupported（`toolControl`：`gci` 别名绕过 `shell(Get-ChildItem)` deny；`networkControl`：shell 绕过精确 URL deny）在 **1.0.85 上仍复现**，且 1.0.83 对照同结果；为回应 ④ 首轮 High（转录不保留 argv），两条决定场景已**重放并新增运行时 argv 捕获**：用 `Get-CimInstance Win32_Process` 轮询到的**进程实际命令行**含 `--deny-tool=shell(Get-ChildItem)` / `--deny-url=https://example.com`，而被覆盖命令仍由 CLI 自启子进程（`pwsh.exe` 命令体为 `gci -Name` / `[Net.WebClient]::new().DownloadString('https://example.com')`）执行。⇒ **B1 按验收分支保持 blocked**（不进入 B2 的 candidate-native 路线）；**B2 应转外部 OS enforcement 证明**，以本片场景为反例基线。**未评估、未排除**：BYOK `deepseek-anthropic` 与非 Copilot CLI 候选族。**登记产品缺陷 DR-1（未修）**：`ai check` 把 `--max-requests` 直通为 CLI 的 `--max-ai-credits`（`internal/adapters/ai_probe_copilot.go:111`）而 CLI 要求 ≥30、`ai check` 又强制 `--max-requests == 1`（`internal/console/ai.go:46`），使产品可用性探针**无法驱动该候选**（记录恒为 `unknown`、`requestsUsed=0`）⇒ **真实 availability verdict 仍不存在**；DR-1 的修复属生产行为变更，需另立切片 + 协议先行。探针计量：**9/10 尝试、6 个付费 premium request**（#3/#4/#5 与一个排除工件为零付费）；审查：③ V4 Pro 预审 2 轮（`PASS WITH FIXES`：1 High + 2 Medium + 5 Low → `PRE-REVIEW: PASS`）、③.5 MAI `INDEPENDENT SCAN: PASS`、④ Codex 终审 3 轮（第 1 轮 1 High + 1 Low、第 2 轮 2 Medium + 1 Low 均整改 → 第 3 轮 **`RE-REVIEW: PASS`**）；**工具收编**：探针 runner 已从 `tmp/` 收编为仓库工具 `tools/agent-probe/Invoke-CandidateProbe.ps1`（默认 dry-run；**仅 `-Run` 执行候选**；SHA-256 pin；工件根限仓 `tmp/` 子树并拒逃逸/已存在/reparse）+ 密自检 `tools/agent-probe/Test-CandidateProbe.ps1`（stub 候选、零模型调用；最近一次全通过 `selftest: checks=35 failures=0`），收编另经 ④ 6 轮复审收至 `RE-REVIEW: PASS`（无 Medium+），决定化工件脱敏后入库 `docs/validation/evidence/b1-20260917/`；⑤ 门禁（无生产改动，仅确认树仍绿）：`gofmt -l` 空、`go build`/`go vet` 干净、`go test -count=1 ./...` 13 包 ok、contract 4/4。边界：单主机/单账号/host 认证；未跑 T026 全分析器；未调用 BYOK 通道；不解除任何 `unproven`，不构成 AT-23 证据。验证：[中文](validation/t027-b1-candidate-live-discovery.md) / [English](validation/t027-b1-candidate-live-discovery_EN.md)。T027 仍为 `BLOCKED / NOT IMPLEMENTED`，AT-23 未通过。

T027 前置状态更新（2026-09-18，B2 external OS enforcement decision + proof）：在上一段基础上，B2 已完成。**定案：采用方案 B「外部 OS 强制」**——受管进程运行在零网络能力 AppContainer 中（内核阻断公网/LAN/DNS），容器持有全回环豁免，唯一可用出口是宿主侧白名单代理 `127.0.0.1:39877`（默认拒绝、审计写失败 fail-closed、占用端口即退出、监听仅限回环）；策略判定位于 OS 边界与代理两处，均不归受管进程控制。边界定义、术语澄清（“唯一出口”= 受管进程只能经代理出网，代理自身出网属边界外）与三项过程要求见双语报告 `validation/B2-EXTERNAL-ENFORCEMENT.md`（13 节）；脱敏证据包 `validation/evidence/b2-2026-09-17/`（41 工件 + `MANIFEST.md` + `SHA256SUMS.txt`，校验 0 失配），受管候选身份摘要 `sha256 d3f3bb7b…0f671ee2`，基线 `schemaVersion 2` + 豁免表快照，机器纪律脚本收编为仓库工具 `tools/agent-probe/appcontainer-b2/`（密闭自检 `Test-B2Tooling.ps1`：`checks=8 failures=0`）与 `tools/agent-probe/enforcement-proxy/`（纯标准库 Go 代理 + 15 项测试，端口 39877 冻结）。**排练与恢复**：2026-09-18 由用户在管理员会话中实跑 `Invoke-B2LoopbackRehearsal.ps1`，六步全 `exit=0`、`rehearsal-ok=True`、revert 后 `restoral: zero-difference`；收尾 revert 零差异、容器 profile 已删除、只读确认 `expect=absent ok=True`；转录已归档 `restorability/rehearsal.txt`，报告 §13 的未闭环项由此清零。**审查**：④ 独立第三方模型（Codex）逐轮终审至 `RE-REVIEW: PASS`（0 blockers），整改项含基线身份闸门、旧基线 schema 兼容、profile 存在性改读注册表（名称推导不构成存在性证据）、PowerShell 5.1 读取无 BOM UTF-8 的编码缺陷，以及 `normalize-encoding.ps1` 的遍历/输出缺陷（33.7 分钟无输出 → 17 秒且只列违规）。**门禁**：`gofmt`/`go build`/`go vet`/`go test` 全绿（含代理 15 项测试）、工具自检 8/8、`normalize-encoding.ps1` 报 `violations=0`。**边界**：本片只证明**外部 OS 强制边界**已实测且不可轻易绕过，不构成 AT-23 证据、不解除任何 `unproven`、不代表产品侧已接线 AgentRunner 或容器内工具执行路径；B4 必须在本边界之上完成 AT-23 E2E 与独立审查，且复用同一端口前重做基线快照与只读确认（报告 §9）。**入库**：本片交付物（报告 + 证据包 + `tools/agent-probe/` 两个工具目录 + `.gitignore`）已按用户同轮授权提交并推送（见下）。**提交与 CI**：`58757e8`（工具收编 + `.gitignore` 的 `/tools/tmp/` 守卫）、`fd99dc3`（报告 + 证据包 + 台账回写）、`408bd99`（`*.raw.txt` 字节保真规则收窄）已推送 `origin/main`（`c5c22ad..408bd99`）；GitHub Actions **两跑全绿**——run `35259806285`（head `fd99dc3`，约 1m48s）与 run `35261204021`（head `408bd99`，约 1m52s）：Windows 腿 Build/Vet/Test success，Ubuntu 腿 Build/Vet/Test + Race（`internal/adapters`/`internal/chain`）+ Contract fixtures success；新增代理包 15 项测试进入**两腿**默认套件（不在 `-race` 子集）。推送前另修一处入库保真缺陷：`* text=auto eol=lf` 会把原始捕获 `measuring/probe10-dir-listing.raw.txt` 归一为 LF、致克隆后 `SHA256SUMS.txt` 失配，已加 `docs/validation/evidence/**/*.raw.txt -text` 并以 `git worktree` 克隆语义复核 **41/41 条 0 失配**。T027 仍为 `BLOCKED / NOT IMPLEMENTED`，AT-23 未通过。验证：[中文](validation/B2-EXTERNAL-ENFORCEMENT.md) / [English](validation/B2-EXTERNAL-ENFORCEMENT_EN.md)。

T027 前置状态更新（2026-09-20，B3a power-loss injection rig + two-way calibration）：在上一段基础上，B3a 已完成，机器判定 **`CALIBRATED`**（`docs/validation/evidence/b3a-2026-09-20/calibration-verdict.json`，`gate = CALIBRATED`）——装置能**双向分辨**“丢失”与“存活”，B3b 可以开始。关键实测（**均取自 `docs/validation/B3A-CALIBRATION.md`**）：负对照（故意不 flush）5 个有效轮 **4 次丢失**（`lost-zero-filled` ×2、`lost-absent` ×2）+ 1 次存活（r03 `temp-written` `survived`）；正对照（显式 flush + 目录项提交）5 个有效轮**全部 `survived`**（`positiveLosses = 0`）；分辨力 0.8 / 0.0；**12 轮全部 `auditVerdict = HARD-POWER-LOSS`**（无一轮被干净关机伪造存活）；**每轮两次盘点 stdout 逐字节一致**（`inventoryIdentical = true` ×12；对账只比内容与大小、从不比时间）；journal 全链 **36 条**（`plan`/`cut`/`reconcile` 各 12）且重启后可逐轮对账；卷不脏。协议冻结：S1–S9（S7 硬断电 + `cut` 记录 → S8 重启 + 断电审计 + 盘点 ×2 → S9 对账）与 **2.0 s 注入延时**；**改动任一 ⇒ 需重新标定**（B3b 已沿用）。本片自查出并修复 1 项 Critical（`Get-B3aReconcile.ps1` 带顶层 `param()` 却被点源 ⇒ 调用方同名变量被重绑、标定整体失效，首跑卡在 `r00`）⇒ 模块不得有顶层参数、CLI 拆到 `Invoke-B3aReconcile.ps1`，并以自检 `module-load-preserves-vars` 钉住。**边界（报告原文）**：**丢失率不是候选结论**——负对照 4/5 只说明“装置能制造可观测丢失”，5 阶段 × N × 候选的耐久定案属 B3b。证据包 `docs/validation/evidence/b3a-2026-09-20/`（128 文件、`SHA256SUMS.txt` 126 条、**0 失配**）；活体数据在仓库外 `D:\VirtualBox VMs\Win11B3\b3a-runs\2026-09-20\`。提交 `136352b`（装置与自检）与 `babc041`（T027 工作文档）已推送 `origin/main`（`815c03f..babc041`，**未推 gitee**），CI run `35482066033`（head `babc041`，1 分 48 秒）全绿。（本段字段均取自上述报告；未在该报告中出现的字段不写入、不推测。）

T027 前置状态更新（2026-09-21，B3b candidate injection matrix and durability verdict）：在上一段基础上，B3b 已完成（真实断电注入矩阵与三档定案）。**级别与前提必须一起读**：等级一律取 `proven` / `unresolved` / `disproven`；前提 **P′ = VirtualBox 7.2.18 r175117 / guest Windows 11 25H2 build 26200.8037 / NTFS on SATA-AHCI `useHostIOCache=false` / 注入卷 `R:`=INJECT（data.vdi 7.98 GB、4096 B 群集）/ 记录级 ~1 KiB / 2.0 s cut 冻结窗口 / N=5**；注入方式 = guest 硬断电（`controlvm poweroff`）。结果：主矩阵 **17 格×5 = 85 轮全有效、0 void**；U4 排序子矩阵 **6 格×3 = 18 轮全有效、0 真孤儿**；判别力门成立（负对照 5/5 丢失、正对照 5/5 存活）；三档定案 **C2 `proven`**、C1/C3/C2C3 `unresolved`（C3/C2C3 的逐字理由为 `mechanism evidence missing in rounds 77 / 81`——决定阶段“机制未被证明执行过”的存活轮按 fail-closed **不得**兑现为耐久主张，**不等同于“不耐久”**）；U4 `unresolved`（有限样本只能证伪有序，不能证有序）。**旧结论保持**：Windows 发布耐久仍 `unproven`、first-dispatch 仍被拒绝、T027 仍 `BLOCKED / NOT IMPLEMENTED`、AT-23 未通过；升 `ACCEPTED` 与解除解锁的前置仍是**先修订 CONTRACTS 耐久冻结句**（协议先行，独立切片 **B3c**——该口径修订已于 2026-09-21 完成，见下段；解除仍受该条款**解锁边界**约束）。生产侧仅新增两个**零语义测试接缝**（`replayStorePublishPrimitiveHook`、`replayStoreParentSyncHook`，默认 nil、不读环境变量、不改默认行为）；另增“变异臂（装置自证伪）”**人工臂**（降级原语下装置拒绝兑现 `proven`，并有离线可复现互证）。审查：V4 Pro 预审 `changes-required` → 收口 `PASS`；③.5 MAI `PASS`；④ Codex **6 次**（1 终审 + 1 复审 + 2 收口 + 2 delta，超 §2.3 上限的原因与逐次发现以验证报告 §8 为单一权威）。⑤ 期间发现并就地修复 9 项实机缺陷（P1–P9，逐项带反证实验），均已入设计说明。证据：`docs/validation/t027-b3b-durability.md`（+ `_EN`）、证据包 `docs/validation/evidence/b3b-2026-09-21/`（2157 文件、`rigMismatches=0`）。**已提交 `abfcecc` 并推送 `origin/main`（未推 gitee）**，CI run **`35562559401`** 双腿全绿（Ubuntu 含 Race/Contract fixtures、Windows）。台账注记：B3a 与 B3b 的 DEV_PLAN 段落此前均缺失，本段为自 A7 以来首条追加；**B3a 段落待按 `docs/validation/B3A-CALIBRATION.md` 回填**。

T027 前置状态更新（2026-09-21，B3c CONTRACTS durability-upgrade clause revision）：B3c 已完成（纯文档、协议先行；不含任何 `.go`/`.json`/schema/fixtures 改动）。**新增条款**：`docs/CONTRACTS{,_EN}.md` §7 新增「**发布耐久等级与升级条件**」——耐久等级为三态 `proven` / `disproven` / `unresolved`（与 A7 一致，`observed`/`falsified` 只作证据注释）；判定单元为**（平台 × 前提 P）**，任何等级表述必须同时携带平台、前提与证据，禁止把某前提下的等级改述为无前提的平台级等级；记 `proven` 须在**同一前提下**同时具备 (a) 可复现注入或对照证据、(b) **区分力门**（同一装置、同一写入器与同一记录级载荷下，负对照出现 ≥1 次丢失、正对照零丢失；门不成立即封顶 `unresolved`）、(c) **机制证据**；任一缺失即 `unresolved`（且 `unresolved` 不得读作“不耐久”或“已被证伪”），可归属反例即 `disproven`；候选机制等级与**平台发布耐久等级**是不同对象（排除“候选 `proven` ⇒ 平台 `proven`”的短路读法）；运行期 `unresolved`/`disproven` 一律按 `unproven` 行为执行；禁止“永久耐久 / 长期保证”类句式。**三条冻结句参数化**（replay store 发布耐久句 / launch-intent 与 R 同规则句 / 终局发布拒绝句）：由**平台常量**改为**按平台耐久等级参数化**，并写明升级只改变**该平台在该前提下的**规则取值。**解锁边界**：升级只在该平台、该前提覆盖的部署环境内解除对应拒绝；不豁免 AT-23、不豁免独立评审与协议先行、不放宽 `first-dispatch` 之外的任何门禁。**边界铁律（已遵守）**：本片只固化口径，**不解除 Windows `unproven`**、不解除 first-dispatch、T027 仍 `BLOCKED / NOT IMPLEMENTED`、AT-23 未通过；依据 B3b 结论账本，Windows 结论是**分级**的（C2 `proven`，其余 `unresolved`），不是整体升级。ADR-013 行完成条件与 §5 采纳前置已回写指向该条款。审查：① V4 Pro 裁定 U1–U14；③ 预审 `PASS WITH FIXES` → 整改 → 复审 `PASS` → delta `PASS`；③.5 MAI 2 次均未产出可执行发现（格式偏离，已如实留痕）；④ Codex 3 次（首轮 `FINDINGS` 1 Medium + 1 Low → 复审唯一阻断项为“缺原始 diff 证据” → 收口 **`PASS`**）。⑤ 34 项机械检查全绿 + **4 项变异检验全红且逐字节还原**；门禁 `gofmt`/`build`/`vet`/`test` 全绿（14 包 ok、0 FAIL）；8 个 `.md` 编码（BOM+LF）PASS。证据：`docs/validation/t027-b3c-contracts-durability.md`（+ `_EN`）。**已提交 `7bdfa0c` 并推送 `origin/main`（未推 gitee）**；CI run `35575667145` **首跑** Windows 腿 `Test` 步骤红（`tools/agent-probe/enforcement-proxy` 的 `TestObserveModeAllowsUnknownHostAndLogsIt`，`main_test.go:469: target accepts = 0, want 1`；Ubuntu 腿全绿），`gh run rerun --failed` **复跑后双腿全绿**、运行 `success` ⇒ 判定**偶发**；该 flake 与 DR-2 **不是同一测试**，已另立 **DR-3**（见切片清单的已知缺陷表）。

## 3. 需求映射

除明确标记完成的任务外，Implementation evidence 是计划证据，不代表实现。

| REQ ID | Plan Items | 任务 | 验收/实现证据 |
|---|---|---|---|
| REQ-001 | P0.1,P0.2,P1.6 | T001,T002,T003,T018 | AT-01/15；通用 Schema/非 C 样例 |
| REQ-002 | P1.2,P1.6 | T009,T018 | AT-03/15；chain/报告 |
| REQ-003 | P1.2 | T009 | AT-03；有序调度 |
| REQ-004 | P0.2,P1.1,P1.2 | T002,T006,T008 | AT-02/04；snapshot/applier |
| REQ-005 | P0.2,P1.3 | T002,T011 | AT-05；review/publish |
| REQ-006 | P0.3,P1.4,P1.6,P1.8 | T004,T013,T017,T026,T027 | AT-08/14/23；adapter/能力/CI |
| REQ-007 | P0.2,P0.3,P1.4,P1.8 | T002,T004,T013,T025,T026,T027,T028 | AT-08/22/23/24；执行与辅助通道 |
| REQ-008 | P0.2,P1.4,P1.8 | T002,T013,T025,T026,T027 | AT-08/22/23；context/session 恢复 |
| REQ-009 | P0.3,P1.5,P1.8 | T004,T016,T025,T027,T028 | AT-12/22/23/24；CLI/TUI 交互 |
| REQ-010 | P0.2,P1.5 | T002,T016 | AT-01/12；配置解释 |
| REQ-011 | P1.5,P1.6 | T015,T018 | AT-11/15；三 harness |
| REQ-012 | P0.2,P1.2 | T002,T007,T010 | AT-06；gate runner |
| REQ-013 | P0.2,P1.5 | T002,T015 | AT-11；模板生成 |
| REQ-014 | P0.1,P1.3,P1.6 | T001,T012,T018 | AT-07/15；阶段报告 |
| REQ-015 | P0.3,P1.5,P1.6 | T004,T016,T017 | AT-12/13/14；构建/TUI |
| REQ-016 | P1.4,P1.5,P1.8 | T013,T016,T025,T026,T027 | AT-08/12/22/23；无 IDE CLI Agent |
| REQ-017 | P0.1,P0.2,P0.3 | T001,T002,T003,T004 | AT-01；readiness |
| REQ-018 | P0.2,P1.2,P1.5 | T003,T009,T016 | AT-01/03；四 kind |
| REQ-019 | P1.6 | T017 | AT-13/14；两代自托管 |
| REQ-020 | P0.2,P1.5 | T003,T015 | AT-01/11；组件/双语言 |
| REQ-021 | P0.2,P0.3,P1.4,P1.8 | T003,T004,T014,T025,T028 | AT-09/10/22/24；handoff/黑箱归还 |
| REQ-022 | P0.2,P1.3,P1.5 | T003,T011,T015 | AT-01/05/11；文档协同 |
| REQ-023 | P0.1,P0.3,P1.1,P1.2,P1.3,P1.4,P1.8 | T001,T004,T005,T007,T010,T011,T012,T014,T025,T026,T027,T028 | AT-05/06/07/09/22/23/24；安全 |
| REQ-024 | P0.2,P0.3,P1.1,P1.2,P1.3,P1.4,P1.6,P1.8 | T002,T004,T005,T006,T008,T009,T011,T014,T017,T025,T026,T027,T028 | AT-02/03/04/09/13/22/23/24；恢复 |
| REQ-025 | P0.2,P1.1,P1.3,P1.4,P1.6,P1.8 | T002,T005,T011,T013,T014,T017,T025,T026,T027,T028 | AT-04/06/10/14/22/23/24；执行证据 |
| REQ-026 | P0.1,P1.2,P1.3,P1.4,P1.8 | T001,T009,T010,T012,T013,T026,T027,T028 | AT-03/06/07/23/24；预算/unknown |
| REQ-027 | P0.3,P1.5,P1.8 | T004,T016,T025,T028 | AT-12/22/24；终端/风险提示 |
| REQ-028 | P0.1,P0.2,P1.1,P1.6 | T001,T002,T003,T005,T017 | AT-01/04/14；兼容 |

## 4. 测试策略与完成定义

§3 原映射与下表取并集；checkpoint 的产品切片补充记录同样加入 P0.1/P0.2/P1.7，不另编号需求。

| PC | REQ | Plan Items | 任务 | 验收 |
|---|---|---|---|---|
| PC-01 | REQ-009/010/016/027 | P0.1,P0.2,P1.7 | T001,T002,T003,T019 | AT-16 |
| PC-02 | REQ-002/004/025 | P0.1,P0.2,P1.7 | T001,T002,T003,T020 | AT-17 |
| PC-03 | REQ-005/021/023 | P0.1,P0.2,P1.7 | T001,T002,T003,T021 | AT-18 |
| PC-04 | REQ-012/023/024 | P0.1,P0.2,P1.7 | T001,T002,T003,T022 | AT-19 |
| PC-05 | REQ-006/026 | P0.1,P0.2,P1.7 | T001,T002,T003,T023 | AT-20 |
| PC-06 | REQ-019/025/028 | P0.1,P0.2,P1.7 | T001,T002,T003,T024 | AT-21 |

完整方案见 [TEST_STRATEGY.md](TEST_STRATEGY.md)：Go testing、真实本地文件/子进程、独立 Schema validator、CLI oracle、授权宿主 smoke；无数据库/浏览器依赖。模块测试必须纳入 go test ./...；每任务报告测试数/失败/跳过。mock 通过不代替原生 Windows 进程树/文件系统或真实 adapter 能力。

完成任务须有：实现与测试、对应需求/契约、双语影响文档、命令/退出码/证据、范围无越界。完成迭代还须全 build/vet/test 与相关 AT；完成 S1 必须全强制测试和人审签署。计划覆盖率不是实现覆盖率。

## 5. 后续阶段和资源决策

S2 backlog 在 S1 exit 后细化，不预生成空实现：Windows AgentRunner 完成后的 Ubuntu 24.04 `linux/amd64` 独立开发与正式化、多语言组件依赖编排/三语言实跑、supervised/Web/可选 VS Code 视图、模型规则切换和智能文档影响提示。macOS、ARM 及其他 OS/架构组合保留在无排期平台 backlog，后续按原生环境或 GitHub Actions runner 的独立证据再立项；S3 为生成脚本 B、多编辑器、inception。需求 ID 保持不变，通过 RFC/ADR 扩展任务和 AT；不得从 S1 悄悄搬走强制能力。

每次迭代报告实际人工时间、模型调用/费用估算、故障数、已验收需求和下一步；若超预算，暂停并由用户选择继续或经 RFC 缩范围，不自动加模型/服务。

RFC §19 的后续候选：S2 图形上手/终端本地化、脱敏通知/远程审批身份设计、外部 PR 集成评估、外部写补偿、成本趋势和显式存储迁移；S3 商业后台/云管理须先验证需求。这些不在 T019–T028 的范围，后续再拆任务。当前共 11 个 plan items、5 个未完成任务、24 组计划验收；不是完成率。
