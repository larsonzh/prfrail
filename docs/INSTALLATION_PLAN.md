# ProofRail 安装与部署规划

[English](INSTALLATION_PLAN_EN.md)

日期：2026-09-07。状态：规划中，非安装手册。当前没有正式发行包、稳定安装命令或受支持的生产部署方式；本文件记录待决事项和发布前门禁，避免把目标方案误写为现状。

## 1. 当前可用方式

当前仅支持开发者从源码构建和运行测试：Go 1.22+、仓库源码及目标项目自身工具链。`cmd/prfrail` 仍是开发中入口，不能据此宣称已安装产品。准确的当前命令见 [操作手册](OPERATIONS.md)。

## 2. 目标部署模型

S1 目标是本地单用户、单写者、无常驻云服务的 CLI/TUI：ProofRail 二进制、状态/store、隔离 run-workspace 和只读源目录彼此边界明确。SessionBridge 是可选 adapter，不是核心安装依赖；目标项目编译器、解释器和测试工具由 harness 声明，ProofRail 不静默安装。

## 3. 待决决策

| 决策 | 候选 | 冻结条件/责任 | 当前状态 |
|---|---|---|---|
| S1 发行载体 | Windows amd64 压缩包；后续评估 winget | T017 原生验收、SBOM、校验和和签名完成 | 待定 |
| Linux 支持 | S1 核心 CI/预览；正式支持候选在 S2 | 原生兼容、路径/权限/升级测试通过 | 待定 |
| 安装目录与 PATH | 用户级独立目录或包管理器目录 | 不需管理员权限、可并存升级和回滚 | 待定 |
| state/store 默认位置 | 用户数据目录或显式 workspace 配置 | 不与源/run 嵌套，权限与备份策略可验证 | 待定 |
| 配置搜索顺序 | 显式参数、workspace、用户 profile | T016 冻结 CLI 与 config explain 行为 | 待定 |
| 签名与信任根 | 离线 Ed25519 已定方向，主体/公钥待定 | ADR、密钥治理、撤销与轮换演练 | 待定 |
| 更新方式 | 手工下载核验；未来包管理器 | 支持矩阵、schema 兼容、备份恢复门禁 | 待定 |
| 卸载与数据保留 | 二进制与 run/store 分离处理 | T024 生命周期记录和显式删除授权 | 待定 |
| VS Code 集成 | 可选扩展/SessionBridge adapter | CLI 独立闭环通过，权限不高于核心 API | 待定 |

未形成 ADR 和可执行验收证据前，不选择默认目录、不发布一键安装脚本、不声明自动更新，也不要求管理员权限。

## 4. 规划的用户路径

1. 从受信发布页选择与 OS/架构匹配的发行物。
2. 独立核验校验和、签名、签名主体、支持状态和撤销新鲜度。
3. 解压/安装到独立目录，不覆盖运行中的 host；执行版本与只读自检。
4. 初始化显式 state/store 与 workspace 配置，确认它们不和源目录或 run-workspace 重叠。
5. 先运行无副作用静态预览，再单独授权能力探测和实际运行。
6. 升级前停止写者并建立完整、可恢复的新 store 备份；失败时保留旧二进制和原 store。
7. 卸载时默认保留证据和配置；数据删除是单独授权动作。

以上步骤是验收目标，不是当前可复制教程。具体命令、文件名、目录和下载地址由 T016、T017、T024 完成后填入。

## 5. 发布前安装门禁

- Windows 11 amd64 原生安装、首次运行、升级、回滚和卸载通过；Linux 仅按当期支持矩阵声明。
- 发布物可离线核验 checksum、签名、SBOM、许可证和精确版本；未知或撤销信息过期不得显示 fully verified。
- 全新用户环境无需 Go 即可运行 ProofRail；项目任务需要的外部工具明确列出且不自动安装。
- 安装不修改目标源码或 Git，不删除 SessionBridge/用户工具链，不把 secret 写入配置、日志或备份。
- state/store 备份覆盖完整引用闭包，并已在新目录完成恢复演练。
- README 与操作文档中的每条安装命令均由对应发行物在受支持平台实测。

## 6. 文档完成条件

T016 冻结 CLI、配置和当前源码运行方式；T017 提供候选发行物、CI、签名与支持矩阵；T024 冻结备份、升级、停用和数据保留。三者完成前本文保持“规划”，[操作手册](OPERATIONS.md) 继续区分当前可执行与未来设计。